# 다음 작업 — 여기서부터 시작

마지막 갱신: 2026-08-23 (S2b 완료 시점)

> **새 세션의 Claude는 이 문서를 먼저 읽어라.** 무엇이 되고, 무엇이 안 되고,
> 다음에 뭘 할지가 여기 있다. 상세는 각 항목이 가리키는 문서에 있다.

---

## 지금 상태 한 줄

**PC 웹에서 용역을 만들고 도면을 올리고 결함을 찍고 정보를 입력하는 것까지 된다.
사진과 출력이 아직 없다.**

```
cd C:\Users\samsung\Desktop\OnSpect
npm install
npm run dev      →  http://localhost:5173/
```

테스트 **349개** · 타입 검사 · 프로덕션 빌드 전부 통과.
GitHub: https://github.com/uk2y-getitgit/OnSpect_build (공개)

---

## 다음 작업 — 이 순서

### 1️⃣ 직전 입력 기억 (권장 — 가장 값이 크다)

같은 부재로 20건을 입력해도 **매번 다시 고른다.**
상세기획 §2-B 입력 3원칙 #3 — *"직전 입력을 기억한다. 위치만 새로 찍으면 됨"*.

**현장 입력 속도에 가장 크게 걸리는 지점**이다. S5(사진)보다 먼저 할 가치가 있다.
같은 층에서 연속 입력 시 구조유형·부재·결함유형을 유지하면 된다.

착수: `_workspace/10_plan-reviewer_spec_S3_S4.md` §4-3 에 설계가 있다.

### 2️⃣ S5 — 사진 추가 (PC 폴더 선택)

`§0-A` 의 마지막 단계. 상세기획 **§Phase 2-C** 가 스펙이다.

- 결함 1건 : 사진 N장, **대표 1장(`isPrimary`)** — 불변식 #8
- 사진첩·손상결함표의 사진번호는 **대표사진 기준**
- 보정(자르기·회전)·주석·교체·삭제
- `photos` IndexedDB 스토어는 **S1 에서 이미 빈 채로 만들어져 있다.** 그대로 쓴다
- 샘플 사진 8장: `샘플사진\` (git 에 없음, 로컬에만)

### 3️⃣ Phase 4 — 산출물 출력 (제품의 존재 이유)

| 산출물 | 상태 |
|---|---|
| 손상결함표 (엑셀) | **실물 서식 아직 못 받음** — 받으면 열 매핑만 조정 |
| 결함 리스트 (A4 세로) | |
| 사진첩 (6장/페이지) | S5 이후 |
| 조사위치도 | **재료 준비됨** — 도곽·범례가 F5 에서 완성 |

출력 ON/OFF 체크박스(자유그리기·메모·도곽)도 여기서 붙인다.

**번호 부여는 이미 설계돼 있다** — 상세기획 §4. 저장하지 않고 출력 시점에 계산한다.

### 그 뒤

Phase 5 오프라인 동기화·병합 → 모바일(React Native). 지금은 PC 웹만 있다.

---

## 답을 못 받은 질문 8건

전부 **비차단**이고 안전한 쪽으로 구현돼 있다. 사용자가 화면을 보고 판단하면 된다.
상세는 `_workspace/QUESTIONS.md`.

| # | 요지 | 지금 구현 |
|---|---|---|
| Q24 | 새 부재의 구조체 구분 기본값 | `구조체` |
| Q25 | `[기본 항목 다시 불러오기]` 범위 | 새 마스터가 낀 링크만 |
| Q26 | 캔버스 `[설정]` 버튼 자리 | 상단바 |
| Q27 | 같은 이름 항목 2개 허용 여부 | 막음 |
| **Q29** | **배율 변경 시 표기 좌표** | **안 옮김 + 경고** ← 가장 확인이 필요 |
| Q30 | 옛 텍스트 메모 계속 지원 | 읽기·편집 유지, 새로 못 만듦 |
| Q31 | `[A4로 맞추기]` 후 썸네일 옛 비율 | 그대로 둠 |

**Q29 가 제일 중요하다.** A4 정규화된 도면에서 배율을 바꾸면 그림만 커지고
표기는 지면 기준 제자리에 남는다. 결함을 찍은 뒤 배율을 바꿀 일이 있는지가 관건.

---

## 절대 어기면 안 되는 것

`CLAUDE.md` 와 `.claude/skills/onspect-build/SKILL.md` 에 전문이 있다. 요약:

1. 도면 좌표는 **0~1 정규화** 저장
2. 출력 결함번호·사진번호는 **저장하지 않는다** (출력 시 계산)
3. 모든 쓰기는 **로컬 IndexedDB 우선**
4. 면적 = `(폭mm/1000)×(길이mm/1000)`, 소수 4자리 **절사**, 개소는 곱하지 않음
5. 층 `sortOrder` 정수, 지하 음수
6. 항목 계층은 **마스터+연결**
7. 용역 생성 시 항목설정 **스냅샷 복사**
8. 사진 `isPrimary` 정확히 1장

추가:
- **기하 판정은 스크린 px.** 정규화 공간에서 각도를 재면 종횡비 때문에 틀린다
- 위치·크기는 `geometry`, 색·모양만 `style`
- `canvas-core` 는 `window`/`document`/`Image`/`rAF`/React 참조 금지
- `ui/defectForm/*` 은 store·repo·캔버스 import 금지 (모바일 재사용 경계)
- **IndexedDB 마이그레이션 금지.** DB 버전 1 유지
- 색 예약: 빨강=현회차 / 보라=전회차 / 회색=보수완료 / 파랑=UI / 시안=가이드선

---

## 일하는 방식

- **에이전트는 브라우저를 띄우지 않는다.** 코드·타입·테스트·빌드까지가 검증
- **실행 확인은 사용자가 한다.** 클릭해봐야 아는 것은 체크리스트로 넘긴다
- 완료 시 리더가 서버를 띄우고 URL 을 준다
- 오류 신고가 오면 `onspect-fix` 로 **그 오류 하나만** 좁게 고친다
- 코드 탐색은 **Serena 심볼 도구로.** 전체 파일 읽기가 예산을 가장 빨리 태운다
- 큰 작업은 쪼개고, **항목마다 커밋·푸시** (한도로 두 번 끊긴 적이 있다)

---

## 문서 지도

| 문서 | 역할 |
|---|---|
| `CLAUDE.md` | 프로젝트 규칙 · 하네스 트리거 · 변경 이력 |
| `OnSpect_상세기획.md` | **1차 기준.** 데이터 모델 · 번호체계 · 구현 순서(§0-A) |
| `docs/OnSpect_디자인시스템.md` | 색 예약 · 토큰 · 밀도 |
| `docs/benchmark/젠트릭스_분석.md` | 경쟁 제품 실측 |
| `_workspace/12_수정사항_S3중간.md` | 사용자 수정사항 F1~F7 + Numdraw 이식 명세 |
| `_workspace/DECISIONS.md` | **사용자 확정 사항 D1~D8** — 뒤집지 말 것 |
| `_workspace/QUESTIONS.md` | 미답변 질문 |
| `_workspace/ASSUMPTIONS.md` | 에이전트 가정 (접두사별) |
| `_workspace/1*_builder_log_*.md` | 구현 로그 + 확인 체크리스트 |
| `.claude/` | 하네스 — 에이전트 3 + 스킬 5 |

**로컬에만 있는 것** (공개 저장소라 제외):
`샘플도면\` `샘플사진\` `벤치마킹 스크린샷\` `C:\Users\samsung\Desktop\Numdraw\`

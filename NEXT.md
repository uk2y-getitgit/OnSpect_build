# 다음 작업 — 여기서부터 시작

마지막 갱신: 2026-08-30 (사용자 수정사항(0828) 4개 배치 전부 완료 시점)

> **새 세션의 Claude는 이 문서를 먼저 읽어라.** 무엇이 되고, 무엇이 안 되고,
> 다음에 뭘 할지가 여기 있다. 상세는 각 항목이 가리키는 문서에 있다.

---

## 지금 상태 한 줄

**PC 웹에서 용역을 만들고 도면을 올리고 결함을 찍고 정보·사진을 입력한 뒤,
손상결함표·결함리스트·사진첩·조사위치도까지 뽑을 수 있다. 사진 자르기·주석,
층 접두 번호, 도곽·범례 프로젝트 스코프, 상태 범례, 유사결함 불러오기,
필기메모 지우개까지 이번 라운드로 추가됐다. ⚠️ **아직 `main`에 병합 전 —
브랜치 `feat/photo-polish`에서 사용자 실행 검증 대기 중.** `canvas-core`에는
태블릿 터치(핀치줌 등)를 위한 코어 배선도 이미 들어가 있다(화면엔 아직 안 붙음).**

```
cd C:\Users\samsung\Desktop\OnSpect
git checkout feat/photo-polish   # 아직 main이 아니다
npm install
npm run dev      →  http://localhost:5173/
```

테스트 **644개**(canvas-core 337 · project-core 307) · 타입 검사 · 프로덕션 빌드 전부 통과.
GitHub: https://github.com/uk2y-getitgit/OnSpect_build (공개, `main` — 이번 라운드는 아직 `feat/photo-polish`에만 있음)

### 2026-08-30 완료 — 사용자 수정사항(0828) 4개 배치 (`feat/photo-polish`)

세션이 한 번 만료돼 중단됐다가 이어서 진행했다. 범위는 사용자가 직접 써보고 준 원문 피드백
2건(`Onspect 수정사항1.txt`·`2.txt` → `_workspace/00_input/scope_UserFeedback0828.md`로 정리) —
plan-reviewer가 `30_plan-reviewer_spec_UserFeedback0828.md`로 스펙화하고 위험도 순 4배치로 쪼갰다.
팀 도구(`TeamCreate`) 미가용 → `Agent` + `SendMessage` 폴백, `_workspace/TASKS.md`가 작업표 역할.
각 배치 = builder 구현 → code-reviewer 검수 → builder 수정, 순차 진행(위험도 낮은 것부터).

| 배치 | 내용 | 커밋 | 검수 |
|---|---|---|---|
| 1 | F-1(모달 포커스 버그) · F-2(인쇄뷰 자동인쇄 제거) · R-3(사진 합성렌더러) | `634a6f7`·`82354d6`·`ac9b045`(+수정 `f824bf8`) | `32_code-reviewer_findings_Batch1.md` 보통3·경미4 |
| 2 | R-4(캡션·EXIF촬영정보) · R-5+R-6+R-8+F-4(사진 자르기·주석·사진첩반영·사진번호숨김) · R-9(손상결함표 인쇄뷰) | `ad820c6`·`7f6ef29`·`62cbaff`·`4d44354`(+수정 `194cef8`) | `34_code-reviewer_findings_Batch2.md` 보통3·경미6 |
| 3 | G-6(층 접두번호+사진번호전체연속) · G-3(도곽·범례 프로젝트스코프+버그B2) · G-2(상태범례 신설) | `fc12efa`·`efd9e00`·`8770c42`(+수정 `2574787`) | `36_code-reviewer_findings_Batch3.md` **심각2**·보통3 → 전부 해소 |
| 4 | G-5(D9폐기→유사결함불러오기) · G-1(필기메모 점선상자숨김·획히트·지우개) | `f358ae1`·`8e92157`(+수정 `fd10543`) | `39_code-reviewer_findings_Batch4.md` **심각1**·보통2 → 전부 해소 |

**차단 질문 1건** — Q54(배치3 검수 中): 층 접두어 칸을 비워두면 층 이름에서 자동으로 접두어를
파생해(`지상1층`→`1F`) **손대지 않은 기존 용역의 산출물까지 전부 바뀌는** 문제. 사용자가
**옵트인**(직접 입력한 층만 접두어 적용, 비우면 예전과 완전 동일)으로 답변 → `DECISIONS.md` **D20**.

**심각 버그 2건, 전부 검수→수정 사이클로 해소됨(코드에 안 남아 있음):**
- 배치3: 조사위치도 번호 풍선이 고정 원이라 `1F-01`같은 5글자 접두어가 원 밖으로 넘침 → 텍스트 폭에
  맞춰 늘어나는 스타디움(pill) 도형으로 교체, 렌더·히트·자동배치 3곳 동시 수정
- 배치4: 지우개로 필기메모 획을 지운 뒤 그 메모를 드래그하면 엉뚱한 위치로 튐(앵커`Memo.pos`와
  실제 bbox가 어긋남) → `MOVE_MEMO`를 상자 기준이 아니라 `memo.pos` 기준 순수 델타로 재계산

구현 로그: `31·33·35·37·38·40_builder_log_*.md` · 검수: `32·34·36·39_code-reviewer_findings_*.md` ·
확정 스펙: `30_plan-reviewer_spec_UserFeedback0828.md` · 결정: `DECISIONS.md` D14~D20 ·
가정: `ASSUMPTIONS.md` U17~U28 · 작업표: `TASKS.md`(전 배치 완료 기록).

**통합 판정(Z, 리더):** 타입검사 3워크스페이스 0오류 · 단위테스트 **644개 전부 통과**
(canvas-core 337 · project-core 307, 이전 라운드 597 대비 +47) · 프로덕션 빌드 통과 ·
개발서버 `http://localhost:5173/` 기동 확인. **`main` 병합은 아직 안 함** — 아래 체크리스트를
사용자가 직접 확인한 뒤 병합 여부를 판단한다.

### 2026-08-25 완료 — Phase 5 착수 전 점검 + 태블릿 UI 목업 + 트랙A(코어 터치 지원)

1. **태블릿 UI 목업 13화면** — https://claude.ai/code/artifact/b2a7f632-018a-48d4-8824-358250f7b6f5
   소스: `_workspace/mobile_mockup/*.dc.html`. 과업목록·오프라인준비(G3)·캔버스·정밀표기 두 안 비교(Q6)·
   결함입력(사이드시트/바텀시트3단/면적모드)·사진촬영·전회차대조·결함리스트.
2. **Phase 5 스펙 확정** — `_workspace/26_plan-reviewer_spec_Phase5_Mobile.md`. 사용자 결정 D10~D13
   (`_workspace/DECISIONS.md`): 태블릿 가로=우측 사이드시트 / 런타임 PWA 먼저 / 동기화 자체서버 구축 /
   정밀표기(Q6)는 목업 두 안(조준 크로스헤어 vs 롱프레스 루페) 보고 최종 선택 대기 중.
3. **트랙A 구현 완료** — `packages/canvas-core`에 핀치줌 이벤트 3종(`GESTURE_PINCH_*`)·
   두 번째 포인터 드래그취소 버그 수정(마우스에선 안 드러나던 기존 결함)·`CENTER_ON_NORM`(미니맵 이동)·
   터치용 히트 프로파일(`ReduceContext.hitProfile`, optional) 추가. **PC 웹 동작은 전혀 안 바뀐다**
   (기존 251개 테스트 그대로 통과 + 신규 26개). `apps/web`은 한 줄도 안 건드림 — 화면엔 아직 안 붙어 있다.
   로그: `_workspace/27_builder_log_Phase5_TrackA.md` · 검수: `_workspace/28_code-reviewer_findings_Phase5_TrackA.md`

**다음 트랙(T1 터치 어댑터·T6~T8 화면 배선)은 착수 안 함** — 런타임은 PWA로 정했지만
서버·동기화 프로토콜(D12)이 아직 설계 전이고, 정밀표기(Q6)도 최종 확정 전이라 화면에 실제로
붙이는 작업은 그 다음이다.

### 2026-08-25 완료 — S5(사진) + Phase 4(산출물 4종) · 원격/비대화형 라운드

이번 라운드는 사용자 부재 중 원격으로 진행됐다. `onspect-orchestrator` 스킬로
plan-reviewer → builder(Phase A: 공통기반+S5) → code-reviewer → builder(수정) →
builder(Phase B: 출력 4종) → code-reviewer → builder(수정) → 리더 통합·병합 순으로 진행했고,
**차단 질문은 0건**이었다(전부 안전한 기본값 + 낮은 되돌리기 비용으로 진행).

과정 문서: `_workspace/21_plan-reviewer_spec_S5_Phase4.md`(스펙) ·
`_workspace/22~24_builder_log_*.md`(구현 로그) · `_workspace/23·25_code-reviewer_findings_*.md`(검수).
브랜치 `feat/s5-phase4-a`(커밋 8개) → `main` fast-forward 병합 완료(`a6e5ab6`).

#### ✅ S5 — 사진 추가

- 결함 우측 패널에 사진 섹션 추가. `<input type=file multiple>` 폴더/파일 선택(PC 웹이라 촬영 없음)
- 결함 1건 : 사진 N장, 대표 1장(`isPrimary`) 정확히 1장 — 불변식 #8, 단위테스트로 고정
- Blob 3종 저장(원본·2048px 렌더본·320px 썸네일), EXIF 방향 자동 적용(세로 사진이 안 눕는다)
- 썸네일 드래그 정렬·대표 지정·**회전**·교체·삭제(+되돌리기 토스트)
- **자르기·주석은 이번 범위에서 뺐다** — 필드(`edits.crop`, `annotations`)만 예약, 버튼은 `준비 중`
- **결함 삭제 시 사진도 정리된다** — 단, Ctrl+Z로 결함을 되돌리면 사진도 함께 살아난다
  (연쇄삭제를 즉시가 아니라 "용역을 다시 열 때"로 미뤄서 해결)

#### ✅ Phase 4 — 산출물 출력 4종 + 공통 기반

| 산출물 | 형식 | 상태 |
|---|---|---|
| 손상결함표 | 엑셀 | **표준 13열로 구현.** 실물 서식 오면 `damageTable.ts`의 열 정의만 조정 |
| 결함 리스트 | 엑셀 + 인쇄(PDF) | 9열 축약(현장 대조용, 손상결함표가 정식 제출용) |
| 사진첩 | 인쇄(PDF) | 6장/페이지, 대표사진 기준 |
| 조사위치도 | PNG + 인쇄(PDF) | 도면+번호, 자유그리기·메모·도곽 ON/OFF 반영 |

- **번호는 저장하지 않는다.** 출력 화면(P6, `#/p/:pid/export`)에서 층 칩을 고른 순서가 번호 순서(젠트릭스 방식),
  번호모드(층별리셋/전체연속)·조사구분 필터·보수완료 포함여부로 그 자리에서 계산(`numbering.ts`)
- **4종을 함께 뽑으면 같은 `ExportRun`을 공유** — 손상결함표 사진번호 = 사진첩 사진번호 = 조사위치도 번호가
  항상 같은 결함을 가리킨다 (code-reviewer가 코드에서 직접 확인함)
- **`[같은 번호로 다시 받기]`는 재계산하지 않는다** — 저장된 매핑을 그대로 재사용해 번호 재현성을 보장
- **`ExportRun`은 새 IndexedDB 스토어를 안 만든다** — 기존 `meta` KV를 `exportRun:{id}` 키로 재사용.
  **DB_VERSION은 1 그대로**(마이그레이션 0건)
- **PDF는 라이브러리 없이 브라우저 인쇄**(`window.print()` + `@page`)로 만든다. 버튼은 `[PDF로 인쇄]`
- 엑셀은 `write-excel-file`(동적 import, 71.68KB 별도 청크 — 출력 화면 밖에서는 안 실림)
- 미완성 결함(D3)은 자동 제외되지 않는다 — 포함하고 경고만
- 조사위치도는 선택 하이라이트·hover·가이드선이 전부 빠진 깨끗한 렌더(code-reviewer가 필드 단위로 확인)

---

## ⚠️ 사용자가 직접 확인해주실 것 (아직 아무도 브라우저로 안 눌러봤다)

이번 라운드(0828 피드백 4배치)는 **에이전트가 타입검사·단위테스트·빌드까지만** 검증했다.
아래는 실제로 눌러봐야 아는 것만 핵심으로 모았다. 배치별 전체 체크리스트는
`31·33·35·37·38·40_builder_log_*.md` 하단에 각각 더 있다. **브랜치 `feat/photo-polish`에서 확인.**

### 사진 자르기·주석 (배치2 · R-5·R-6)
- [ ] 사진 `[자르기]` — 새 창이 아니라 같은 모달 안에서 바뀌고, 8개 핸들로 크기 조절되는가
- [ ] **세로 사진(EXIF 90° 회전)에서 자르기 사각형이 손가락을 따라오는가** — 어긋나면 좌표 버그
- [ ] `[주석]` — 화살표·자유획, 지우개로 획 하나가 통째로 지워지는가. **`Ctrl+Z`가 주석만 되돌리고 뒤쪽 도면 결함은 안 건드리는가** ⭐
- [ ] **자르기·주석이 사진첩 PDF에 화면과 같은 모양으로 인쇄되는가** ⭐

### 출력 — 층 접두 번호 (배치3 · G-6, D20 옵트인 확정 후)
- [ ] **접두어를 한 곳도 입력하지 않은 기존 용역**을 열어 뽑으면 NO열이 예전 그대로 `1`·`2`·`3`인가
      (번호모드 기본값도 `전체 이어서` 그대로여야 한다 — 바뀌면 안 됨)
- [ ] 층 접두어 칸에 `w` 입력 → 대문자 `W` 저장, `Esc`로 **취소**되는가(저장되면 안 됨)
- [ ] 접두어를 입력한 용역만 NO열이 `1F-01` 형태로 나오고, 조사위치도 번호 풍선도 **원 밖으로 안 넘치는가**(스타디움 도형)
- [ ] `[같은 번호로 다시 받기]` — 접두어 입력 **전**에 낸 옛 산출물을 다시 받아도 그때 그 파일과 완전히 같은가

### 출력 — 도곽·범례 (배치3 · G-3·G-2)
- [ ] **도곽을 한 번도 설정한 적 없는 도면**을 열어도 이제 도곽이 바로 보이는가(예전엔 설정 열고 저장해야 나왔음)
- [ ] 도곽 크기 슬라이더를 끄는 동안 뒤 캔버스에서 실시간으로 커졌다 작아지는가
- [ ] 범례에 상태 체크박스 4개(결함유형/신규/미보수/보수완료) — 기본은 결함유형만 켜져 있어 **기존 출력물이 그대로인가**
- [ ] `보수완료`를 켜도 그 도면에 보수완료 결함이 없으면 행이 안 나오는가

### 유사결함 불러오기 (배치4 · G-5, D9 폐기)
- [ ] 결함을 새로 찍으면 이제 **빈 폼**으로 뜨는가(예전처럼 직전 값이 자동으로 안 채워짐)
- [ ] `[유사결함 불러오기]` → 목록에서 하나 고르면 분류·판정 14필드만 오고, **규모·개소·메모는 안 오는가**
- [ ] 이미 규모(폭×길이)를 입력해둔 결함에 면적모드 결함을 불러오면, 방식이 바뀐다는 **안내 문구**가 뜨는가
- [ ] **잠긴 결함(전회차)**에는 버튼이 비활성인가

### 필기메모 지우개 (배치4 · G-1)
- [ ] 필기메모 점선 상자가 평소엔 안 보이고 **선택·hover할 때만** 보이는가
- [ ] 지우개로 문지르면 **필기메모 획만** 지워지고 점·화살표·영역·자유그리기·번호풍선·리더선은 **절대 안 지워지는가** ⭐
- [ ] **획 3개 이상인 메모에서 왼쪽 끝 획을 지운 뒤 남은 글씨를 드래그** — 끈 만큼만 움직이는가(엉뚱한 곳으로 튀면 버그)
- [ ] 결함 선택 → 지우개 도구로 바꾸고 `Delete` → 결함이 안 지워지는가
- [ ] 한 번의 드래그로 여러 획을 지운 뒤 `Ctrl+Z` → 전부 원래 자리로 돌아오는가

### 회귀 확인 (안 바뀌어야 하는 것)
- [ ] 접두어를 아무 층에도 안 넣고 뽑은 산출물이 예전과 완전히 똑같은가
- [ ] 옛 텍스트 메모(노란 상자)를 끌어보면 예전과 동일하게 동작하는가

---

### 이전 라운드(S5+Phase4, 2026-08-25 이전) 체크리스트 — 참고용, 이미 지나간 라운드

이번 0828 피드백 자체가 사용자가 아래 항목들을 써보고 준 것이므로 **사실상 이미 확인된 것으로 간주한다.**
남은 의문이 있으면 알려주면 된다. 원문은 `21_plan-reviewer_spec_S5_Phase4.md` §8 참고.

---

## 이렇게 가정하고 만들었습니다 (이번 라운드 · 전부 비차단 · 확인 부탁)

되돌리는 비용이 낮은 순. 전문은 `_workspace/ASSUMPTIONS.md` U17~U28.

| # | 요지 | 지금 구현 | 바꾸려면 |
|---|---|---|---|
| U19 | 접두어 있는 층은 NO셀이 문자열(`1F-01`) | 접두어 없는 층은 기존처럼 숫자 그대로 — 셀 타입 안 바뀜 | 낮음 |
| U21 | 도곽 다이얼로그 우측 붙임 + 스크림 투명화 둘 다 적용 | 720px 이하는 기존대로 가운데 | 낮음 |
| U23 | `DELETE_MEMO_PATH` 커맨드가 스펙 원문(단수)과 다르게 배치(복수) payload | "한 드래그 = Undo 1스텝" 요구와 단수 payload가 양립 불가해서 — code-reviewer가 타당하다고 승인함 | 낮음(내부 자료구조만, 사용자 동작은 스펙 그대로) |
| U24 | 지우개는 커서에 가장 가까운 획 1개만 지운다 | "다른 게 지워지면 안 된다"는 요구 반영 | 낮음 |
| U26 | 유사결함 후보는 용역 전체·`seq` 내림차순·자기 자신 제외 | — | 낮음 |
| [보통2] 배치4 검수 | `sizeMode` 복사 시 측정값(폭·길이·면적)은 안 와서 규모 입력방식만 바뀜 | **의도된 동작으로 확정**(D9 표 그대로) — 토스트 문구로 "규모 입력 방식도 함께 바뀝니다" 안내만 보강 | 되돌리려면 대상에 측정값 있을 때 `sizeMode` 복사 제외(비쌈) |

이전 라운드(S5+Phase4, Q32~Q37 — PDF 브라우저 인쇄 방식 등)는 이번 0828 피드백이 그 라운드를
써보고 나온 것이므로 **사실상 확인된 것으로 간주**한다. 전문은 `_workspace/QUESTIONS.md`.

---

## 남은 것 / 못 한 것

- **D20(층 접두어 옵트인) 이후 확인 안 됨** — 코드 경로는 검수자가 추적했으나 실제 옛 이력 IndexedDB로 재현은 안 해봄
- 사진첩 캡션에는 결함번호(층 접두어 포함)가 안 들어간다 — 배선 지점 자체가 없음(builder 확인)
- 지우개가 포인터 샘플 지점만 검사 — 매우 빠르게 문지르면 획을 건너뛸 수 있음(경미, 미수정)
- **손상결함표 실물 서식 미확보** — 표준 13열로 구현. 서식 오면 `damageTable.ts`의 `DAMAGE_COLUMNS` 조정만 필요
- EXIF 촬영시각·촬영기기는 배치2에서 파싱 추가됐으나 일부 기기 포맷은 미검증
- 조사구분(`surveyKind`) 필터 UI는 있으나 실제로 걸리는 결함이 여전히 적음
- `apps/web`에는 테스트 러너가 없어 UI·훅은 타입검사로만 검증. 위험한 로직은 전부 `project-core`/`canvas-core`로 빼서 단위테스트 644개로 고정
- **`main` 미병합** — `feat/photo-polish`에 전부 있고 아직 병합 안 함

---

## 다음 작업 — 이 순서

### 1️⃣ 지금 — 사용자 실행 검증 (`feat/photo-polish` 브랜치)

위 체크리스트를 처음부터 끝까지 직접 눌러보고, 발견한 오류는 하나씩 `onspect-fix`로 좁게 고친다.
문제 없으면 `main`으로 병합해달라고 요청하면 된다(리더가 병합 진행).

### 2️⃣ 그 뒤 — Phase 5 오프라인 동기화·병합 → 모바일(React Native)

지금은 PC 웹만 있다. `canvas-core`는 이미 플랫폼 독립으로 짜여 있어(D1) RN이 재사용한다.

---

## 절대 어기면 안 되는 것

`CLAUDE.md`와 `.claude/skills/onspect-build/SKILL.md`에 전문이 있다. 요약:

1. 도면 좌표는 **0~1 정규화** 저장
2. 출력 결함번호·사진번호는 **저장하지 않는다** (출력 시 계산) — 이번 라운드로 실제 구현됨
3. 모든 쓰기는 **로컬 IndexedDB 우선**
4. 면적 = `(폭mm/1000)×(길이mm/1000)`, 소수 4자리 **절사**, 개소는 곱하지 않음
5. 층 `sortOrder` 정수, 지하 음수
6. 항목 계층은 **마스터+연결**
7. 용역 생성 시 항목설정 **스냅샷 복사**
8. 사진 `isPrimary` **정확히 1장** — 이번 라운드로 저장 계층 완성

추가:
- **기하 판정은 스크린 px.** 정규화 공간에서 각도를 재면 종횡비 때문에 틀린다
- 위치·크기는 `geometry`, 색·모양만 `style`
- `canvas-core`는 `window`/`document`/`Image`/`rAF`/React 참조 금지
- `ui/defectForm/*`은 store·repo·캔버스 import 금지 (모바일 재사용 경계)
- **IndexedDB 마이그레이션 금지.** DB 버전 1 유지 — `ExportRun`도 `meta` KV 재사용으로 지켰다
- 색 예약: 빨강=현회차 / 보라=전회차 / 회색=보수완료 / 파랑=UI / 시안=가이드선
- **PDF는 브라우저 인쇄로.** PDF 라이브러리(jspdf 등)를 넣지 않는다 — 한글 폰트 임베딩 비용 회피

---

## 일하는 방식

- **에이전트는 브라우저를 띄우지 않는다.** 코드·타입·테스트·빌드까지가 검증
- **실행 확인은 사용자가 한다.** 클릭해봐야 아는 것은 체크리스트로 넘긴다
- 완료 시 리더가 서버를 띄우고 URL을 준다
- 오류 신고가 오면 `onspect-fix`로 **그 오류 하나만** 좁게 고친다
- 코드 탐색은 **Serena 심볼 도구로.** 전체 파일 읽기가 예산을 가장 빨리 태운다
- 큰 작업은 쪼개고, **항목마다 커밋·푸시**
- **팀 도구(TeamCreate/TaskCreate) 미가용 시 폴백:** `Agent`(백그라운드) + `SendMessage`로 조율,
  작업 목록은 `_workspace/TASKS.md` 파일로 대체 (이번 라운드에 처음 실사용, 잘 작동했다)

---

## 문서 지도

| 문서 | 역할 |
|---|---|
| `CLAUDE.md` | 프로젝트 규칙 · 하네스 트리거 · 변경 이력 |
| `OnSpect_상세기획.md` | **1차 기준.** 데이터 모델 · 번호체계 · 구현 순서(§0-A) |
| `docs/OnSpect_디자인시스템.md` | 색 예약 · 토큰 · 밀도 |
| `docs/benchmark/젠트릭스_분석.md` | 경쟁 제품 실측 |
| `_workspace/21_plan-reviewer_spec_S5_Phase4.md` | S5+Phase4 확정 스펙 — 번호부여·ExportRun·4종 산출물 설계 전문 |
| `_workspace/22~24_builder_log_*.md` | S5+Phase4 구현 로그 |
| `_workspace/23·25_code-reviewer_findings_*.md` | S5+Phase4 검수 결과 |
| `_workspace/00_input/scope_UserFeedback0828.md` | 0828 사용자 피드백 원문 정리 |
| `_workspace/30_plan-reviewer_spec_UserFeedback0828.md` | 0828 피드백 확정 스펙(§5-1~5-5) — 이번 라운드 1차 기준 |
| `_workspace/31·33·35·37·38·40_builder_log_*.md` | 배치1~4 구현/수정 로그 |
| `_workspace/32·34·36·39_code-reviewer_findings_*.md` | 배치1~4 검수 결과 |
| `_workspace/TASKS.md` | 팀 도구 폴백 작업 목록 (배치1~4+통합 전부 완료 기록) |
| `_workspace/DECISIONS.md` | **사용자 확정 사항 D1~D20** — 뒤집지 말 것 |
| `_workspace/QUESTIONS.md` | 질문 이력 (Q1~Q54, 전부 답변됨) |
| `_workspace/ASSUMPTIONS.md` | 에이전트 가정 (접두사별, U17~U28이 이번 라운드) |
| `.claude/` | 하네스 — 에이전트 3 + 스킬 5 |

**로컬에만 있는 것** (공개 저장소라 제외):
`샘플도면\` `샘플사진\` `벤치마킹 스크린샷\` `C:\Users\samsung\Desktop\Numdraw\`

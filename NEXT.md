# 다음 작업 — 여기서부터 시작

마지막 갱신: 2026-09-03 (Supabase 서버 스키마 적용 완료 + 로컬 삭제전파 장치 완료)

> **새 세션의 Claude는 이 문서를 먼저 읽어라.** 무엇이 되고, 무엇이 안 되고,
> 다음에 뭘 할지가 여기 있다. 상세는 각 항목이 가리키는 문서에 있다.

---

## 🌐 배포 주소 (테스트용 · 2026-09-03 기록)

| 대상 | URL |
|---|---|
| **PC 버전** | https://on-spect-build-web.vercel.app/pc |
| **태블릿 버전** | https://on-spect-build-web.vercel.app/tablet |
| 실기기 자동판정 | https://on-spect-build-web.vercel.app/ |

`/pc`·`/tablet` 는 U-4에서 만든 강제전환 경로다 — 같은 배포에서 UI만 바꿔 연다
(`apps/web/index.html` 부트스트랩 → `<html data-ui-mode>` → `useUiMode.ts`, rewrite 는 `vercel.json`).
`?ui=pc|tablet` 쿼리도 같다. 한 번 들어가면 그 선택이 localStorage 에 남는다.

---

## 지금 상태 한 줄

**PC 웹에서 용역을 만들고 도면을 올리고 결함을 찍고 정보·사진을 입력한 뒤,
손상결함표·결함리스트·사진첩·조사위치도까지 뽑을 수 있다. 사진 자르기·주석,
층 접두 번호, 도곽·범례 프로젝트 스코프, 상태 범례, 유사결함 불러오기,
필기메모 지우개까지 나왔고, 이번 라운드로 **태블릿에서 핀치줌·필기·전차결함→금차 전환**까지
됐다. ✅ **`main`에 병합 완료. Vercel 실배포도 성공** —
https://on-spect-build-web.vercel.app/ (PC·태블릿 어디서나 접속 가능, 인터넷 필요).

```
cd C:\Users\samsung\Desktop\OnSpect
git checkout main
npm install
npm run dev -- --host 0.0.0.0   →  PC: http://localhost:5173/  ·  태블릿(같은 Wi-Fi): http://<이 PC의 LAN IP>:5173/
```

테스트 **667개**(canvas-core 360 · project-core 307) · 타입 검사 · 프로덕션 빌드 전부 통과.
GitHub: https://github.com/uk2y-getitgit/OnSpect_build (공개, `main`)

### 2026-09-03 — 팀 동기화 서버 착수 (T1-1~T1-3)

사용자가 Supabase 프로젝트(`jaglifijobsjugmsnuvl`)를 만들고 URL·publishable 키를 전달 →
D26(Q59, 태블릿 다중사용자 로그아웃)까지 마지막 남은 블로킹 질문을 답변받아 트랙1 착수 조건이
전부 갖춰졌다.

| # | 내용 | 커밋 |
|---|---|---|
| T1-1 | Supabase 스키마 적용 — `teams`·`team_members`·`projects`·`records`(결함 등 6종 통합)·`blobs` 5테이블 + 팀 격리 RLS. SQL Editor 붙여넣기 방식으로 사용자가 직접 적용(CLI 인증 없이). **재실행해도 안전하게(idempotent) 작성** — 첫 시도에서 부분 적용된 상태로 "already exists" 오류가 나서 즉시 고쳤다. Storage 버킷(`blobs/{teamId}/{projectId}/{key}`)도 같은 방식으로 완료 | `4a0c0a2`·`a8b9079`·`896def6` |
| T1-2 | (전날 완료) `Defect` 병합필드 | `64fc3fe` 등 |
| T1-3 | 로컬 삭제 전파 — `Building`~`Memo` 6종 하드삭제 10개 경로 전부에 `meta` KV 삭제기록(`deleted:{projectId}`) 배선, Ctrl+Z 되돌리면 기록에서 제거. **아직 아무도 이 기록을 안 읽는다** — T1-7(동기화 push)이 쓸 재료만 지금 만들어 둔 것 | `af6354e` |

구현 로그: `68_builder_log_Phase5_DeleteLog.md` · 검수: `69_code-reviewer_findings_Phase5_DeleteLog.md`(통과) ·
서버 스키마: `supabase/migrations/*.sql`(저장소에 커밋, 재실행 안전) · 결정: D26.

**통합 판정(리더):** 타입검사 3워크스페이스 0오류 · 단위테스트 **717개 전부 통과**
(canvas-core 392 · project-core 325) · 프로덕션 빌드 통과.

**다음 막힌 지점 — Supabase `service_role` 키가 있어야 T1-4(로그인·팀원 발급 API)에 착수한다.**
사용자에게 요청함(Settings → API → service_role). **절대 소스코드에 넣지 않고 Vercel 서버
환경변수로만 저장한다.** `apps/web/.env.example`에 필요한 변수명 문서화 완료.

### 2026-09-03 — 태블릿 1차 화면 완성 (T2-1~T2-7)

Phase 5 트랙2(§6-2)의 남은 6개(T2-2 정밀표기는 이미 완료)를 전부 끝냈다 — "PC와 다른 현장맞춤
UI"의 1차 완성형이다. `/tablet` 경로로 지금 바로 확인 가능하다.

| # | 내용 | 커밋 |
|---|---|---|
| T2-1 | 태블릿 셸 — 방향 감지(가로=우측패널·세로=바텀시트 3단, D10) · 터치 히트프로파일 주입(코어는 Phase5 트랙A에서 이미 준비돼 있던 것 배선만) · 좌측 세로 툴바 | `6c06141`·`c4ed288` |
| T2-3 | 층 칩 스트립 — 결함수 뱃지, 기존 정렬(`sortByOrder`) 재사용, 사이드바와 상태 항상 일치 | `cc7d267` |
| T2-4 | 미니맵 — 뷰포트 사각형 탭 이동(기존 `CENTER_ON_NORM` 재사용). **검수에서 세로형 도면 좌표 어긋남 발견** → `fitViewport`(canvas-core 기존 함수) 재사용으로 수정 | `cc7d267`(+수정`7efa192`) |
| T2-5 | 마지막 뷰포트(줌·팬) 기억 — `meta` KV `lastView:{projectId}`, 600ms 디바운스 | `9a34101` |
| T2-6 | 바텀시트 높이만큼 도면 안전영역 확보. **검수에서 CSS/JS 높이 계산 소스가 갈라지는 문제 발견**(키보드 뜰 때) → `getBoundingClientRect()` 실측 하나로 통일 | `cc7d267`(+수정`7efa192`) |
| T2-7 | 결함정보 폼 터치 프로파일 — 44pt 버튼, 프리셋 가로 3열. **CSS만 수정, 컴포넌트 구조 0변경** | `b3a21b4` |

**공유 작업트리 동시편집 사고 2건, 둘 다 데이터 손실 없이 해소:**
- T2-1과 T2-5가 `CanvasRoute.tsx`를 동시 편집 — 각 builder가 `git hash-object`/`update-index`로
  hunk 단위 분리 커밋, 리더가 재검증(타입/테스트/빌드) + code-reviewer가 병합 무결성까지 확인
- 이후 배치부터는 **같은 파일을 건드리는 작업은 한 builder에게 순차로 묶어** 재발 방지(T2-3+T2-4+T2-6)

구현 로그: `61·62·64·65·67_builder_log_Phase5_*.md` · 검수: `63·66_code-reviewer_findings_Phase5_*.md`.

**통합 판정(리더):** 타입검사 3워크스페이스 0오류 · 단위테스트 **700개 전부 통과**
(canvas-core 392 · project-core 308) · 프로덕션 빌드 통과. `canvas-core`는 이번 라운드 전체에서
`viewport.ts` 확장(T2-5, 기존 정규화 좌표계 재사용) 외에는 무변경.

### 2026-09-02 — UI 정리 4건 (실사용 확인 중 발견)

사용자가 배포본을 직접 써보며 준 피드백 4건. plan-reviewer 없이 리더가 직접 스펙화
(`_workspace/00_input/scope_UIPolish0902.md`), 명칭·문구가 걸린 2건은 즉석 질문으로 확정.

| # | 내용 | 커밋 | 검수 |
|---|---|---|---|
| U-1 | 편집툴바(`ContextToolbar`)의 표기종류변경·복제·추가 — 처음부터 "준비 중" 비활성 버튼이었다. 제거 확정. 우클릭 메뉴의 동일 버튼도 리더가 같은 결정으로 함께 제거 | `3baadcc`(+`d0bb816`) | 리더 직접 typecheck/build(저위험) |
| U-2 | 캔버스 조작마다 뜨던 하단 토스트 — "삭제+되돌리기"·"경고/안내"는 유지, "성공확인"류(표기추가·이동·스타일변경 등)만 16곳 제거. **커맨드(Undo 스택)는 그대로** — 토스트만 빠졌다 | `58d35fd`(+연장`d411925`) | `59_code-reviewer_findings_*.md` 통과, 경미1(테스트커버리지, 필수아님) |
| U-3 | 결함유형 범례("균열" 등 글자기호 행) 완전 제거 + 상태범례 문구 축약: 신규(현회차)→**신규** · 미보수(전회차)→**결함** · 보수완료→보수완료(그대로). `DB_VERSION` 1 유지(옛 레코드의 `showTypes`는 읽기 시점에 무시) | `cea85b1` | `60_code-reviewer_findings_*.md` 통과, 지적사항 없음 |
| U-4 | **PC/태블릿 구분 테스트** — 별도 배포 대신 `/pc`·`/tablet` 경로(또는 `?ui=` 쿼리)로 같은 배포에서 강제 전환. 실기기 판정(미디어쿼리)이 기본, 이 오버라이드는 속성선택자로 필요할 때만 덮어씀 — 스크립트 실패해도 실기기에서는 안전 | `2c54559` | 리더 직접 처리 |

**리더가 직접 답한 것** — U-3의 "결함" 라벨이 `PREV_PENDING`(보라·전회차 미보수)에 매핑되는 게
맞는지는 사용자가 답한 순서(신규/결함/보수완료)와 일치해 그대로 확정. 도곽 설정창의 체크박스
문구("신규(현회차)" 등)는 범례 자체가 아니라서 안 바꿈 — 원하면 후속으로 통일 가능.

구현 로그: `56·57·58_builder_log_UI*.md` · 검수: `59·60_code-reviewer_findings_UI*.md`.

**통합 판정(UZ, 리더):** 타입검사 3워크스페이스 0오류 · 단위테스트 **691개 전부 통과**
(canvas-core 383 · project-core 308) · 프로덕션 빌드 통과. `main`에 전부 반영, Vercel 자동배포.

### 2026-09-02 — Phase 5 착수: 팀 동기화 서버·로그인 + 오프라인 설치형 태블릿 UI

사용자가 두 방향을 요청했다 — ① 로그인·팀(팀장1+팀원多)·프로젝트별 수동 동기화 ② 태블릿 전용
설치형 웹앱(PWA), PC와 다른 현장맞춤 UI. plan-reviewer가 스펙 검토 후 **차단질문 5건(Q55~Q59)**을
냈고, 그중 4건에 사용자가 즉석 답변 → `DECISIONS.md` **D22~D25**로 확정. 나머지 1건(Q59, 태블릿
다중사용자 로그아웃 처리)은 로그인 화면 착수 시점으로 보류.

**답 없이 바로 시작 가능했던 작업부터 처리:**

| 작업 | 내용 | 커밋 | 검수 |
|---|---|---|---|
| PWA 껍데기(P1~P6) | manifest·서비스워커(손으로 작성, workbox 미사용)·오프라인 저장소 보호(`persist()`를 앱 시작 1회로 이동 — 기존엔 도면업로드에서만 불려 태블릿에서 영원히 안 돌던 버그 발견·수정)·새버전 배너·용량경고·촬영 진입점(`capture=environment`) | `5750688`·`994b376` | 리더 직접 typecheck/test/build만(화면 자체 위험 낮음) |
| T1-2 — `Defect` 병합필드 | **검토 중 발견한 구조적 결함**: 결함 레코드에만 `updatedAt`·`deviceId`·`createdBy`가 없어 나중에 동기화가 붙어도 최신판정이 불가능했다. D23(먼저 올린 쪽이 원본)대로 필드 신설 + 옛 결함은 `updatedAt=null`("미동기화" 신호, `Date.now()`로 채우면 정확히 스펙이 경고한 사고) | `64fc3fe`·`28548ca`(+수정`a382f4d`) | `54_code-reviewer_findings_*.md` 통과, 경미1 → 리더가 즉시 수정 |
| T2-2 — 정밀 표기(조준 크로스헤어) | D22(안A) 그대로 — `⊕조준` 토글(터치 전용 기기만) → 화면 중앙 고정 십자선 → `[여기]`로 확정. **`canvas-core` 변경 0**(화면 중앙 좌표로 기존 POINTER_DOWN+UP 합성) | `d80477a`·`b541cc8` | `55_code-reviewer_findings_*.md` 통과, 지적사항 없음 |

**리더가 직접 처리한 판단** — plan-reviewer가 "매니지드 DB 벤더는 Supabase로"(비차단, Q61)와
"사진은 렌더본만 서버에 올림"(비차단, Q60)을 권장, 반박 근거 없어 그대로 채택.

구현 로그: `51·52·53_builder_log_Phase5_*.md` · 검수: `54·55_code-reviewer_findings_Phase5_*.md` ·
스펙: `50_plan-reviewer_spec_Phase5_TeamSync.md` · 결정: D22~D25 · 가정: `ASSUMPTIONS.md` V9~V16·W1~W6·X1~X6.

**통합 판정(중간, 리더):** 타입검사 3워크스페이스 0오류 · 단위테스트 **686개 전부 통과**
(canvas-core 379 · project-core 307, 이전 667 대비 +19) · 프로덕션 빌드 통과(`dist/sw.js` 산출 확인).

**다음 배치 대기 중** (`TASKS.md` 참고):
1. 태블릿 셸·층칩·미니맵 등 나머지 1차 화면(T2-1·T2-3~T2-7) — 화면이 계속 나오는 트랙이라 우선
2. 삭제 전파(T1-3, D25) — 서버 없이도 착수 가능
3. **Supabase 프로젝트 생성 — 사용자가 계정을 만들어야 서버 트랙(T1-1 이후)에 착수 가능**
   (Vercel 때와 같은 방식으로 안내 예정, 아직 요청 안 함)

### 2026-09-02 — Vercel 실배포 완료

로컬 개발서버로 같은 Wi-Fi에서만 테스트하던 것을 인터넷 어디서나 접속 가능한 정식 URL로 옮겼다.
**동기화 서버(D12, Phase 5)는 아직 미착수·미설계 — 이번엔 지금 앱을 그대로 안정적으로 접속시키는
것만.** 기기(브라우저)마다 데이터가 로컬에만 저장되는 것은 그대로다 — 여러 태블릿 간 결함 데이터
공유는 아직 안 된다.

- URL: **https://on-spect-build-web.vercel.app/** (`main` 자동배포 연결됨 — 앞으로 `main`에
  푸시하면 자동으로 갱신된다)
- `vercel.json`(커밋 `452cb71`까지 2차 수정) — 겪은 문제 2건과 수정:
  1. Vercel 기본 npm이 새 "설치 스크립트 승인" 기능으로 `esbuild`의 postinstall을 막아 빌드가
     죽음 → `installCommand`를 로컬과 같은 `npm@11.9.0`으로 고정
  2. Vercel 프로젝트의 **Root Directory가 `apps/web`로 설정돼 있어**(Import 시 자동 감지된 듯)
     `outputDirectory`가 그 기준으로 재해석돼 어긋남 → `"dist"`(Root 상대경로)로 수정
- 라우팅은 URL 기반이 아니라 앱 내부 상태다(react-router 등 미사용) — 리라이트 규칙 불필요
- PWA manifest·서비스워커는 아직 없다(D11 "PWA 먼저"는 방향만 정해졌고 미구현) — 지금은
  그냥 HTTPS 정적 사이트로 접속하는 수준

### 2026-09-02 완료 — 태블릿 실사용 피드백 7건 (`feat/photo-polish`)

사용자가 태블릿에서 직접 조작하며 찾은 7건. plan-reviewer 단계 없이 **리더가 직접 코드에서 원인을
확인**하고 스펙화했다(`_workspace/00_input/scope_TabletFeedback0901.md`) — 전부 코드에서 원인이 특정됐다.
파일 겹침 기준 3배치, `TASKS.md` T1~T3.

| 배치 | 내용 | 커밋 | 검수 |
|---|---|---|---|
| 1 | 유사결함 검색창 자동포커스 끄기 · 결함선택 편집툴바 자동노출 억제(생성직후는 안 뜨게) · 태블릿 단축키 힌트 숨김 · 모달 배경클릭 닫기 기본 비활성화 | `14302a7`(+로그`4199492`, +수정`28bf30b`) | `43_code-reviewer_findings_Tablet1.md` 보통2 → 해소 |
| 2 | 필기메모 드로잉 중 점선박스 숨김 · 핀치줌 DOM 배선(리듀서는 Phase5에서 이미 구현·테스트됨) | `27c4b83`(+로그`1499d61`, +수정`3cdf81a`) | `44_code-reviewer_findings_Tablet2.md` 보통1 → 해소 |
| 3 | G-8 — 전차(PREV_PENDING) 결함에 이번 회차 사진 추가 시 금차(CURRENT) 전환 + `[전회차로 되돌리기]` 버튼 | `d2f03ec`(+로그`f8f2a71`, +수정`4f953c4`) | `48_code-reviewer_findings_Tablet3.md` 보통1 → 해소 |

**실동작 버그 2건, 구현 중 부수적으로 발견·해소:**
- 배치2: 두 번째 손가락이 핀치를 시작하면 코어가 드래그를 롤백하는데, 그 상태에서 세 번째 손가락이
  닿으면 새 드래그로 오인식돼 결함이 잘못 찍힘 → 핀치 중 포인터 이벤트를 차단
- 배치2(검수 지적): 도구팔레트·사이드바에 엄지를 얹은 채 그리면 화면 전체 터치 수를 세서 핀치로
  오인식 → 캔버스 영역 안의 접점만 걸러서 판정

**리더가 직접 처리한 판단 3건(D21)** — code-reviewer가 배치3 검수에서 도메인 판단이 필요한 확인요청을
3건 냈다. 2건(샘플데이터 `prevDefectId`, 재출력 시 번호 밀림)은 기존 설계 원칙으로 답이 이미 나와 있어
리더가 바로 처리했고, 1건(Undo로 되돌린 뒤 남는 "사진 있는 전회차 결함"이 보고서에 실리는 문제)은
영향이 작아 **비차단으로 가정하고 진행**(현재 동작 유지) — 아래 확인 목록에 포함.

구현 로그: `41·42·45·46·47·49_builder_log_*.md` · 검수: `43·44·48_code-reviewer_findings_*.md` ·
결정: `DECISIONS.md` D21 · 가정: `ASSUMPTIONS.md` U29~U44 · 작업표: `TASKS.md`.

**통합 판정(TZ, 리더):** 타입검사 3워크스페이스 0오류 · 단위테스트 **667개 전부 통과**
(canvas-core 360 · project-core 307, 이전 라운드 644 대비 +23) · 프로덕션 빌드 통과 ·
개발서버 `--host 0.0.0.0`로 기동해 PC·태블릿 동시 접속 확인. `main` 병합 완료(같은 날 진행,
아래 Vercel 절 참고).

### 2026-08-30 완료 — 사용자 수정사항(0828) 4개 배치 (`feat/photo-polish`)

세션이 한 번 만료돼 중단됐다가 이어서 진행했다. 범위는 사용자가 직접 써보고 준 원문 피드백
2건(`Onspect 수정사항1.txt`·`2.txt` → `_workspace/00_input/scope_UserFeedback0828.md`로 정리) —
plan-reviewer가 `30_plan-reviewer_spec_UserFeedback0828.md`로 스펙화하고 위험도 순 4배치로 쪼갰다.
팀 도구(`TeamCreate`) 미가용 → `Agent` + `SendMessage` 폴백, `_workspace/TASKS.md`가 작업표 역할.
각 배치 = builder 구현 → code-reviewer 검수 → builder 수정, 순차 진행(위험도 낮은 것부터).

| 배치 | 내용 | 커밋 | 검수 |
|---|---|---|---|
| 1 | F-1(모달 포커스 버그) · F-2(인쇄뷰 자동인쇄 제거) · R-3(사진 합성렌더러) | `634a6f7`·`82354d6`·`ac9b045`(+수정 `f824bf8`) | `32_code-reviewer_findings_Batch1.md` 보통3·경미4 |
| 2 | R-4(캡션·EXIF촬영정보) · R-5+R-6+R-8+F-4(사진 자르기·주석·사진첩반영·사진번호숨김) · R-9(손상결함표 인쇄뷰) | `ad820c6`·`7f6ef29`·`62cbaff`·`4d44354`(+수정 `194cef8`) | `34_code-reviewer_findings_Batch2.md` 보통3·경미6 |
| 3 | G-6(층 접두번호+사진번호전체연속) · G-3(도곽·범례 프로젝트스코프+버그B2) · G-2(상태범례 신설) | `fc12efa`·`efd9e00`·`8770c42`(+수정 `2574787`) | `36_code-reviewer_findings_Batch3.md` **심각2**·보통3 → 전부 해소 |
| 4 | G-5(D9폐기→유사결함불러오기) · G-1(필기메모 점선상자숨김·획히트·지우개) | `f358ae1`·`8e92157`(+수정 `fd10543`) | `39_code-reviewer_findings_Batch4.md` **심각1**·보통2 → 전부 해소 |

**차단 질문 1건** — Q54(배치3 검수 中): 층 접두어 칸을 비워두면 층 이름에서 자동으로 접두어를
파생해(`지상1층`→`1F`) **손대지 않은 기존 용역의 산출물까지 전부 바뀌는** 문제. 사용자가
**옵트인**(직접 입력한 층만 접두어 적용, 비우면 예전과 완전 동일)으로 답변 → `DECISIONS.md` **D20**.

**심각 버그 2건, 전부 검수→수정 사이클로 해소됨(코드에 안 남아 있음):**
- 배치3: 조사위치도 번호 풍선이 고정 원이라 `1F-01`같은 5글자 접두어가 원 밖으로 넘침 → 텍스트 폭에
  맞춰 늘어나는 스타디움(pill) 도형으로 교체, 렌더·히트·자동배치 3곳 동시 수정
- 배치4: 지우개로 필기메모 획을 지운 뒤 그 메모를 드래그하면 엉뚱한 위치로 튐(앵커`Memo.pos`와
  실제 bbox가 어긋남) → `MOVE_MEMO`를 상자 기준이 아니라 `memo.pos` 기준 순수 델타로 재계산

구현 로그: `31·33·35·37·38·40_builder_log_*.md` · 검수: `32·34·36·39_code-reviewer_findings_*.md` ·
확정 스펙: `30_plan-reviewer_spec_UserFeedback0828.md` · 결정: `DECISIONS.md` D14~D20 ·
가정: `ASSUMPTIONS.md` U17~U28 · 작업표: `TASKS.md`(전 배치 완료 기록).

**통합 판정(Z, 리더):** 타입검사 3워크스페이스 0오류 · 단위테스트 **644개 전부 통과**
(canvas-core 337 · project-core 307, 이전 라운드 597 대비 +47) · 프로덕션 빌드 통과 ·
개발서버 `http://localhost:5173/` 기동 확인. **`main` 병합은 아직 안 함** — 아래 체크리스트를
사용자가 직접 확인한 뒤 병합 여부를 판단한다.

### 2026-08-25 완료 — Phase 5 착수 전 점검 + 태블릿 UI 목업 + 트랙A(코어 터치 지원)

1. **태블릿 UI 목업 13화면** — https://claude.ai/code/artifact/b2a7f632-018a-48d4-8824-358250f7b6f5
   소스: `_workspace/mobile_mockup/*.dc.html`. 과업목록·오프라인준비(G3)·캔버스·정밀표기 두 안 비교(Q6)·
   결함입력(사이드시트/바텀시트3단/면적모드)·사진촬영·전회차대조·결함리스트.
2. **Phase 5 스펙 확정** — `_workspace/26_plan-reviewer_spec_Phase5_Mobile.md`. 사용자 결정 D10~D13
   (`_workspace/DECISIONS.md`): 태블릿 가로=우측 사이드시트 / 런타임 PWA 먼저 / 동기화 자체서버 구축 /
   정밀표기(Q6)는 목업 두 안(조준 크로스헤어 vs 롱프레스 루페) 보고 최종 선택 대기 중.
3. **트랙A 구현 완료** — `packages/canvas-core`에 핀치줌 이벤트 3종(`GESTURE_PINCH_*`)·
   두 번째 포인터 드래그취소 버그 수정(마우스에선 안 드러나던 기존 결함)·`CENTER_ON_NORM`(미니맵 이동)·
   터치용 히트 프로파일(`ReduceContext.hitProfile`, optional) 추가. **PC 웹 동작은 전혀 안 바뀐다**
   (기존 251개 테스트 그대로 통과 + 신규 26개). `apps/web`은 한 줄도 안 건드림 — 화면엔 아직 안 붙어 있다.
   로그: `_workspace/27_builder_log_Phase5_TrackA.md` · 검수: `_workspace/28_code-reviewer_findings_Phase5_TrackA.md`

**다음 트랙(T1 터치 어댑터·T6~T8 화면 배선)은 착수 안 함** — 런타임은 PWA로 정했지만
서버·동기화 프로토콜(D12)이 아직 설계 전이고, 정밀표기(Q6)도 최종 확정 전이라 화면에 실제로
붙이는 작업은 그 다음이다.

### 2026-08-25 완료 — S5(사진) + Phase 4(산출물 4종) · 원격/비대화형 라운드

이번 라운드는 사용자 부재 중 원격으로 진행됐다. `onspect-orchestrator` 스킬로
plan-reviewer → builder(Phase A: 공통기반+S5) → code-reviewer → builder(수정) →
builder(Phase B: 출력 4종) → code-reviewer → builder(수정) → 리더 통합·병합 순으로 진행했고,
**차단 질문은 0건**이었다(전부 안전한 기본값 + 낮은 되돌리기 비용으로 진행).

과정 문서: `_workspace/21_plan-reviewer_spec_S5_Phase4.md`(스펙) ·
`_workspace/22~24_builder_log_*.md`(구현 로그) · `_workspace/23·25_code-reviewer_findings_*.md`(검수).
브랜치 `feat/s5-phase4-a`(커밋 8개) → `main` fast-forward 병합 완료(`a6e5ab6`).

#### ✅ S5 — 사진 추가

- 결함 우측 패널에 사진 섹션 추가. `<input type=file multiple>` 폴더/파일 선택(PC 웹이라 촬영 없음)
- 결함 1건 : 사진 N장, 대표 1장(`isPrimary`) 정확히 1장 — 불변식 #8, 단위테스트로 고정
- Blob 3종 저장(원본·2048px 렌더본·320px 썸네일), EXIF 방향 자동 적용(세로 사진이 안 눕는다)
- 썸네일 드래그 정렬·대표 지정·**회전**·교체·삭제(+되돌리기 토스트)
- **자르기·주석은 이번 범위에서 뺐다** — 필드(`edits.crop`, `annotations`)만 예약, 버튼은 `준비 중`
- **결함 삭제 시 사진도 정리된다** — 단, Ctrl+Z로 결함을 되돌리면 사진도 함께 살아난다
  (연쇄삭제를 즉시가 아니라 "용역을 다시 열 때"로 미뤄서 해결)

#### ✅ Phase 4 — 산출물 출력 4종 + 공통 기반

| 산출물 | 형식 | 상태 |
|---|---|---|
| 손상결함표 | 엑셀 | **표준 13열로 구현.** 실물 서식 오면 `damageTable.ts`의 열 정의만 조정 |
| 결함 리스트 | 엑셀 + 인쇄(PDF) | 9열 축약(현장 대조용, 손상결함표가 정식 제출용) |
| 사진첩 | 인쇄(PDF) | 6장/페이지, 대표사진 기준 |
| 조사위치도 | PNG + 인쇄(PDF) | 도면+번호, 자유그리기·메모·도곽 ON/OFF 반영 |

- **번호는 저장하지 않는다.** 출력 화면(P6, `#/p/:pid/export`)에서 층 칩을 고른 순서가 번호 순서(젠트릭스 방식),
  번호모드(층별리셋/전체연속)·조사구분 필터·보수완료 포함여부로 그 자리에서 계산(`numbering.ts`)
- **4종을 함께 뽑으면 같은 `ExportRun`을 공유** — 손상결함표 사진번호 = 사진첩 사진번호 = 조사위치도 번호가
  항상 같은 결함을 가리킨다 (code-reviewer가 코드에서 직접 확인함)
- **`[같은 번호로 다시 받기]`는 재계산하지 않는다** — 저장된 매핑을 그대로 재사용해 번호 재현성을 보장
- **`ExportRun`은 새 IndexedDB 스토어를 안 만든다** — 기존 `meta` KV를 `exportRun:{id}` 키로 재사용.
  **DB_VERSION은 1 그대로**(마이그레이션 0건)
- **PDF는 라이브러리 없이 브라우저 인쇄**(`window.print()` + `@page`)로 만든다. 버튼은 `[PDF로 인쇄]`
- 엑셀은 `write-excel-file`(동적 import, 71.68KB 별도 청크 — 출력 화면 밖에서는 안 실림)
- 미완성 결함(D3)은 자동 제외되지 않는다 — 포함하고 경고만
- 조사위치도는 선택 하이라이트·hover·가이드선이 전부 빠진 깨끗한 렌더(code-reviewer가 필드 단위로 확인)

---

## ⚠️ 사용자가 직접 확인해주실 것 (아직 아무도 브라우저로 안 눌러봤다)

이번 라운드도 **에이전트가 타입검사·단위테스트·빌드까지만** 검증했다. 태블릿 항목은 실기기 확인이
꼭 필요하다. 배치별 전체 체크리스트는 `41·42·45·46·47·49_builder_log_*.md` 하단에 각각 더 있다.
**브랜치 `feat/photo-polish`에서 확인, 서버는 `npm run dev -- --host 0.0.0.0`로 띄워야 태블릿이 접속된다.**

### 태블릿 실사용 피드백 7건 (이번 라운드)
- [ ] **필기메모** — 여러 획으로 글씨를 이어 쓰는 동안 점선 상자가 안 보이는가. 도구를 바꾸거나 다른 곳을
      탭하면 그 메모가 다시 선택/hover될 때 상자가 정상적으로 보이는가(항상 숨김이면 안 됨)
- [ ] **핀치줌** — 두 손가락으로 확대·축소가 되는가. 팔레트·사이드바에 엄지를 얹은 채 도면에 그려도
      획이 안 지워지고 정상적으로 그려지는가. 핀치 중에 점 도구가 켜져 있어도 결함이 잘못 안 찍히는가
- [ ] **유사결함 불러오기** — 검색창을 눌러도 태블릿 키보드가 안 뜨는가(목록은 바로 탭 가능해야 함)
- [ ] **편집 툴바** — 결함을 연달아 찍을 때마다 툴바가 안 뜨는가. **기존 마커를 다시 탭하면** 뜨는가
- [ ] **단축키 안내** — 태블릿에서 하단의 휠·Space·Alt·Shift·Ctrl+Z 안내줄이 안 보이는가(PC 마우스에서는 그대로 보여야 함)
- [ ] **모달 배경 클릭** — 용역 만들기·도면 올리기 중 배경(스크림)을 눌러도 안 닫히고 입력값이 남아있는가.
      `Esc`나 `✕`/취소 버튼은 정상적으로 닫히는가
- [ ] **전차→금차 전환** — 전차 용역을 "결함까지 가져오기"로 연결해 새 용역을 만들고(보라색 마커 생김),
      그 결함에 사진을 추가하면 **빨간색(금회차)으로 바뀌는가.** Inspector 하단에
      `[전회차로 되돌리기]` 버튼이 뜨는가, 누르면 다시 보라색으로 돌아가는가
- [ ] ⚠️ **기존 샘플 용역은 이 확인에 못 쓴다** — 용역 목록에서 **샘플을 새로 생성**해야 위 전차→금차
      항목이 재현된다(옛 샘플은 `prevDefectId`가 비어 있어 되돌리기 버튼이 안 뜬다)

### 사진 자르기·주석 (배치2 · R-5·R-6)
- [ ] 사진 `[자르기]` — 새 창이 아니라 같은 모달 안에서 바뀌고, 8개 핸들로 크기 조절되는가
- [ ] **세로 사진(EXIF 90° 회전)에서 자르기 사각형이 손가락을 따라오는가** — 어긋나면 좌표 버그
- [ ] `[주석]` — 화살표·자유획, 지우개로 획 하나가 통째로 지워지는가. **`Ctrl+Z`가 주석만 되돌리고 뒤쪽 도면 결함은 안 건드리는가** ⭐
- [ ] **자르기·주석이 사진첩 PDF에 화면과 같은 모양으로 인쇄되는가** ⭐

### 출력 — 층 접두 번호 (배치3 · G-6, D20 옵트인 확정 후)
- [ ] **접두어를 한 곳도 입력하지 않은 기존 용역**을 열어 뽑으면 NO열이 예전 그대로 `1`·`2`·`3`인가
      (번호모드 기본값도 `전체 이어서` 그대로여야 한다 — 바뀌면 안 됨)
- [ ] 층 접두어 칸에 `w` 입력 → 대문자 `W` 저장, `Esc`로 **취소**되는가(저장되면 안 됨)
- [ ] 접두어를 입력한 용역만 NO열이 `1F-01` 형태로 나오고, 조사위치도 번호 풍선도 **원 밖으로 안 넘치는가**(스타디움 도형)
- [ ] `[같은 번호로 다시 받기]` — 접두어 입력 **전**에 낸 옛 산출물을 다시 받아도 그때 그 파일과 완전히 같은가

### 출력 — 도곽·범례 (배치3 · G-3·G-2)
- [ ] **도곽을 한 번도 설정한 적 없는 도면**을 열어도 이제 도곽이 바로 보이는가(예전엔 설정 열고 저장해야 나왔음)
- [ ] 도곽 크기 슬라이더를 끄는 동안 뒤 캔버스에서 실시간으로 커졌다 작아지는가
- [ ] 범례에 상태 체크박스 4개(결함유형/신규/미보수/보수완료) — 기본은 결함유형만 켜져 있어 **기존 출력물이 그대로인가**
- [ ] `보수완료`를 켜도 그 도면에 보수완료 결함이 없으면 행이 안 나오는가

### 유사결함 불러오기 (배치4 · G-5, D9 폐기)
- [ ] 결함을 새로 찍으면 이제 **빈 폼**으로 뜨는가(예전처럼 직전 값이 자동으로 안 채워짐)
- [ ] `[유사결함 불러오기]` → 목록에서 하나 고르면 분류·판정 14필드만 오고, **규모·개소·메모는 안 오는가**
- [ ] 이미 규모(폭×길이)를 입력해둔 결함에 면적모드 결함을 불러오면, 방식이 바뀐다는 **안내 문구**가 뜨는가
- [ ] **잠긴 결함(전회차)**에는 버튼이 비활성인가

### 필기메모 지우개 (배치4 · G-1)
- [ ] 필기메모 점선 상자가 평소엔 안 보이고 **선택·hover할 때만** 보이는가
- [ ] 지우개로 문지르면 **필기메모 획만** 지워지고 점·화살표·영역·자유그리기·번호풍선·리더선은 **절대 안 지워지는가** ⭐
- [ ] **획 3개 이상인 메모에서 왼쪽 끝 획을 지운 뒤 남은 글씨를 드래그** — 끈 만큼만 움직이는가(엉뚱한 곳으로 튀면 버그)
- [ ] 결함 선택 → 지우개 도구로 바꾸고 `Delete` → 결함이 안 지워지는가
- [ ] 한 번의 드래그로 여러 획을 지운 뒤 `Ctrl+Z` → 전부 원래 자리로 돌아오는가

### 회귀 확인 (안 바뀌어야 하는 것)
- [ ] 접두어를 아무 층에도 안 넣고 뽑은 산출물이 예전과 완전히 똑같은가
- [ ] 옛 텍스트 메모(노란 상자)를 끌어보면 예전과 동일하게 동작하는가

---

### 이전 라운드(S5+Phase4, 2026-08-25 이전) 체크리스트 — 참고용, 이미 지나간 라운드

이번 0828 피드백 자체가 사용자가 아래 항목들을 써보고 준 것이므로 **사실상 이미 확인된 것으로 간주한다.**
남은 의문이 있으면 알려주면 된다. 원문은 `21_plan-reviewer_spec_S5_Phase4.md` §8 참고.

---

## 이렇게 가정하고 만들었습니다 (전부 비차단 · 확인 부탁)

되돌리는 비용이 낮은 순. 전문은 `_workspace/ASSUMPTIONS.md` U17~U44.

### 태블릿 라운드 (U29~U44)

| # | 요지 | 지금 구현 | 바꾸려면 |
|---|---|---|---|
| U29 | 텍스트메모 편집기(`MemoEditor`)의 `Ctrl+Enter·Esc` 힌트도 태블릿에서 함께 숨김 | 필기 단축키 힌트와 같은 취급 | 낮음 |
| U31 | "명시적 선택"에 더블클릭·우클릭·좌측 리스트 클릭도 포함 | 화이트리스트 한 곳에 모아둠 | 낮음 |
| U32 | 모달 배경클릭 닫기는 옵트인 prop을 만들되 **지금은 어디서도 켜지 않음** | 도곽 다이얼로그(실시간 미리보기)도 포함해서 전부 비활성 | 낮음 |
| U33 | "드로잉 중" = 필기 세션(도구=MEMO AND 그 메모가 선택됨) | down~up만 보면 "여러 획 이어그리기"를 못 만족해서 | 낮음 |
| U34 | 획을 긋는 도중엔 무관한 다른 메모의 상자까지 숨김 | 스펙 문구("선택 상태와 무관하게")를 문자 그대로 적용 | 낮음 |
| U43 | `[전회차로 되돌리기]` 버튼은 `되돌릴 전회차가 있는 결함`에만 뜬다(사진 유무는 조건 아님) | 없는 전회차로 보내면 출력 필터에서 그 결함이 통째로 빠지는 사고를 막음 | 낮음 |
| D21(N9) | Undo로 되돌린 뒤 남는 "사진 있는 전회차 결함"이 사진첩·손상결함표에 전회차 신분으로 그대로 실림 | 발생 빈도 낮은 조합이라 막지 않고 현재 동작 유지 | **확인 필요** — 실무상 이 조합을 보고서에서 빼야 하면 별도 필터 추가 |

### 0828 피드백 라운드 (U17~U28)

| # | 요지 | 지금 구현 | 바꾸려면 |
|---|---|---|---|
| U19 | 접두어 있는 층은 NO셀이 문자열(`1F-01`) | 접두어 없는 층은 기존처럼 숫자 그대로 — 셀 타입 안 바뀜 | 낮음 |
| U21 | 도곽 다이얼로그 우측 붙임 + 스크림 투명화 둘 다 적용 | 720px 이하는 기존대로 가운데 | 낮음 |
| U23 | `DELETE_MEMO_PATH` 커맨드가 스펙 원문(단수)과 다르게 배치(복수) payload | "한 드래그 = Undo 1스텝" 요구와 단수 payload가 양립 불가해서 — code-reviewer가 타당하다고 승인함 | 낮음(내부 자료구조만, 사용자 동작은 스펙 그대로) |
| U24 | 지우개는 커서에 가장 가까운 획 1개만 지운다 | "다른 게 지워지면 안 된다"는 요구 반영 | 낮음 |
| U26 | 유사결함 후보는 용역 전체·`seq` 내림차순·자기 자신 제외 | — | 낮음 |
| [보통2] 배치4 검수 | `sizeMode` 복사 시 측정값(폭·길이·면적)은 안 와서 규모 입력방식만 바뀜 | **의도된 동작으로 확정**(D9 표 그대로) — 토스트 문구로 "규모 입력 방식도 함께 바뀝니다" 안내만 보강 | 되돌리려면 대상에 측정값 있을 때 `sizeMode` 복사 제외(비쌈) |

이전 라운드(S5+Phase4, Q32~Q37 — PDF 브라우저 인쇄 방식 등)는 이번 0828 피드백이 그 라운드를
써보고 나온 것이므로 **사실상 확인된 것으로 간주**한다. 전문은 `_workspace/QUESTIONS.md`.

---

## 남은 것 / 못 한 것

- **D20(층 접두어 옵트인) 이후 확인 안 됨** — 코드 경로는 검수자가 추적했으나 실제 옛 이력 IndexedDB로 재현은 안 해봄
- 사진첩 캡션에는 결함번호(층 접두어 포함)가 안 들어간다 — 배선 지점 자체가 없음(builder 확인)
- 지우개가 포인터 샘플 지점만 검사 — 매우 빠르게 문지르면 획을 건너뛸 수 있음(경미, 미수정)
- **손상결함표 실물 서식 미확보** — 표준 13열로 구현. 서식 오면 `damageTable.ts`의 `DAMAGE_COLUMNS` 조정만 필요
- EXIF 촬영시각·촬영기기는 배치2에서 파싱 추가됐으나 일부 기기 포맷은 미검증
- 조사구분(`surveyKind`) 필터 UI는 있으나 실제로 걸리는 결함이 여전히 적음
- `apps/web`에는 테스트 러너가 없어 UI·훅은 타입검사로만 검증. 위험한 로직은 전부 `project-core`/`canvas-core`로 빼서 단위테스트 667개로 고정
  (다만 태블릿 라운드의 판정 로직 일부 — `nextToolbarFor`·전차→금차 화이트리스트 — 는 `apps/web`에 있어 이 방식으로 못 덮음, `48_code-reviewer_findings_Tablet3.md` 경미3)
- **핀치줌 DOM 배선은 자동 회귀테스트 없음** — 리듀서(코어)는 테스트로 고정돼 있으나 터치이벤트 배선 자체는 실기기 확인만 가능
- **전차→금차 전환에서 "사진 승계"(전회차 사진을 그대로 복사)는 이번 범위가 아니다** — 의도적으로 안 만듦(K13, Phase 2-D 소관). 이번엔 "이번 회차 새 사진을 추가하면 상태만 전환"까지만
- **D21(N9) 확인 필요** — Undo로 되돌린 뒤 남는 "사진 있는 전회차 결함"이 사진첩·손상결함표에
  전회차 신분 그대로 실리는 것을 실무상 허용할지 — 위 표 참고, 아직 사용자 확답 없음
- **배치2(태블릿) 검수 경미3 미반영** — 필기 세션 중 다른 메모를 더블클릭/선택하는 극단적 케이스를
  세션 종료 이벤트로 승격할지 여부. 화면 동작을 바꾸는 문제라 builder가 임의로 안 정하고 대기 중

---

## 다음 작업 — 이 순서

### 1️⃣ 지금 — 실사용 테스트 (https://on-spect-build-web.vercel.app/)

위 체크리스트를 처음부터 끝까지 직접 눌러보고, 발견한 오류는 하나씩 `onspect-fix`로 좁게 고친다.
`main`에 이미 병합·배포돼 있으니 고치는 즉시 같은 URL에 반영된다(수동 배포 조작 불필요 —
`main`에 푸시만 하면 Vercel이 자동으로 다시 빌드한다).

### 2️⃣ 그 뒤 — Phase 5 오프라인 동기화·병합 → 모바일(React Native)

지금은 PC 웹만 있다. `canvas-core`는 이미 플랫폼 독립으로 짜여 있어(D1) RN이 재사용한다.

---

## 절대 어기면 안 되는 것

`CLAUDE.md`와 `.claude/skills/onspect-build/SKILL.md`에 전문이 있다. 요약:

1. 도면 좌표는 **0~1 정규화** 저장
2. 출력 결함번호·사진번호는 **저장하지 않는다** (출력 시 계산) — 이번 라운드로 실제 구현됨
3. 모든 쓰기는 **로컬 IndexedDB 우선**
4. 면적 = `(폭mm/1000)×(길이mm/1000)`, 소수 4자리 **절사**, 개소는 곱하지 않음
5. 층 `sortOrder` 정수, 지하 음수
6. 항목 계층은 **마스터+연결**
7. 용역 생성 시 항목설정 **스냅샷 복사**
8. 사진 `isPrimary` **정확히 1장** — 이번 라운드로 저장 계층 완성

추가:
- **기하 판정은 스크린 px.** 정규화 공간에서 각도를 재면 종횡비 때문에 틀린다
- 위치·크기는 `geometry`, 색·모양만 `style`
- `canvas-core`는 `window`/`document`/`Image`/`rAF`/React 참조 금지
- `ui/defectForm/*`은 store·repo·캔버스 import 금지 (모바일 재사용 경계)
- **IndexedDB 마이그레이션 금지.** DB 버전 1 유지 — `ExportRun`도 `meta` KV 재사용으로 지켰다
- 색 예약: 빨강=현회차 / 보라=전회차 / 회색=보수완료 / 파랑=UI / 시안=가이드선
- **PDF는 브라우저 인쇄로.** PDF 라이브러리(jspdf 등)를 넣지 않는다 — 한글 폰트 임베딩 비용 회피

---

## 일하는 방식

- **에이전트는 브라우저를 띄우지 않는다.** 코드·타입·테스트·빌드까지가 검증
- **실행 확인은 사용자가 한다.** 클릭해봐야 아는 것은 체크리스트로 넘긴다
- 완료 시 리더가 서버를 띄우고 URL을 준다
- 오류 신고가 오면 `onspect-fix`로 **그 오류 하나만** 좁게 고친다
- 코드 탐색은 **Serena 심볼 도구로.** 전체 파일 읽기가 예산을 가장 빨리 태운다
- 큰 작업은 쪼개고, **항목마다 커밋·푸시**
- **팀 도구(TeamCreate/TaskCreate) 미가용 시 폴백:** `Agent`(백그라운드) + `SendMessage`로 조율,
  작업 목록은 `_workspace/TASKS.md` 파일로 대체 (이번 라운드에 처음 실사용, 잘 작동했다)

---

## 문서 지도

| 문서 | 역할 |
|---|---|
| `CLAUDE.md` | 프로젝트 규칙 · 하네스 트리거 · 변경 이력 |
| `OnSpect_상세기획.md` | **1차 기준.** 데이터 모델 · 번호체계 · 구현 순서(§0-A) |
| `docs/OnSpect_디자인시스템.md` | 색 예약 · 토큰 · 밀도 |
| `docs/benchmark/젠트릭스_분석.md` | 경쟁 제품 실측 |
| `_workspace/21_plan-reviewer_spec_S5_Phase4.md` | S5+Phase4 확정 스펙 — 번호부여·ExportRun·4종 산출물 설계 전문 |
| `_workspace/22~24_builder_log_*.md` | S5+Phase4 구현 로그 |
| `_workspace/23·25_code-reviewer_findings_*.md` | S5+Phase4 검수 결과 |
| `_workspace/00_input/scope_UserFeedback0828.md` | 0828 사용자 피드백 원문 정리 |
| `_workspace/30_plan-reviewer_spec_UserFeedback0828.md` | 0828 피드백 확정 스펙(§5-1~5-5) — 이번 라운드 1차 기준 |
| `_workspace/31·33·35·37·38·40_builder_log_*.md` | 배치1~4(0828) 구현/수정 로그 |
| `_workspace/32·34·36·39_code-reviewer_findings_*.md` | 배치1~4(0828) 검수 결과 |
| `_workspace/00_input/scope_TabletFeedback0901.md` | 태블릿 실사용 피드백 7건 — 리더가 직접 원인조사, 이번 라운드 1차 기준 |
| `_workspace/41·42·45·46·47·49_builder_log_Tablet*.md` | 태블릿 배치1~3 구현/수정 로그 |
| `_workspace/43·44·48_code-reviewer_findings_Tablet*.md` | 태블릿 배치1~3 검수 결과 |
| `_workspace/TASKS.md` | 팀 도구 폴백 작업 목록 (0828 4배치 + 태블릿 3배치 + 통합 전부 완료 기록) |
| `_workspace/DECISIONS.md` | **사용자 확정 사항 D1~D21** — 뒤집지 말 것 |
| `_workspace/QUESTIONS.md` | 질문 이력 (Q1~Q54, 전부 답변됨) |
| `_workspace/ASSUMPTIONS.md` | 에이전트 가정 (접두사별, U17~U28은 0828 라운드 · U29~U44는 태블릿 라운드) |
| `.claude/` | 하네스 — 에이전트 3 + 스킬 5 |

**로컬에만 있는 것** (공개 저장소라 제외):
`샘플도면\` `샘플사진\` `벤치마킹 스크린샷\` `C:\Users\samsung\Desktop\Numdraw\`

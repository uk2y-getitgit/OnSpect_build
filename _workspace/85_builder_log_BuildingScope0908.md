# 구현 로그 — 동(Building) 경계 적용 (D45 · 실사용피드백0907 #4)

작성: builder · 2026-09-08
근거 스펙: `_workspace/83_plan-reviewer_spec_BuildingScope0908.md`
커밋: `6477f7c`(B-1·A-1, 리더가 마무리) · `cb2b8fd`(B-3·B-4·B-2·B-6)

> **범위:** 스펙의 9개 작업 중 **6개**(B-1 · A-1 · B-3 · B-4 · B-2 · B-6).
> B-5 · B-7 · B-8 은 **손대지 않았다** — Q83 · Q85 답변 대기 중.

---

## 완료

| # | 작업 | 파일 | 상태 |
|---|---|---|---|
| **B-1** | 출력 기본 층순서·정렬 버튼을 동 그룹 정렬로 | `routes/Export.tsx`(`defaultFloorOrder`) · `routes/export/FloorChips.tsx`(`downUp`/`upDown`) | ✅ (`6477f7c`) |
| **A-1** | 도면 배율 일괄 적용을 "이 동의 도면만" 으로 좁힘 | `packages/project-core/src/drawingScope.ts`(신설, +테스트 7건) · `data/drawingScale.ts`(재수출) · `routes/CanvasRoute.tsx` · `routes/DrawingScaleDialog.tsx` | ✅ (`6477f7c`) |
| **B-3** | 조사위치도 파일명·경고·`alt` 에 동 표기 | `export/locationMap.ts` · `export/produce.ts` · `export/exportModel.ts` · `export/printView/PrintRoute.tsx` · `export/printView/PrintLocationMap.tsx` | ✅ (`cb2b8fd`) |
| **B-4** | 손상결함표·결함리스트 섹션 제목에 동 표기 | `packages/project-core/src/export/damageTable.ts` (+테스트 4건) | ✅ (`cb2b8fd`) |
| **B-2** | 층칩 동별 그룹 + `[이 동 전체]`·`[이 동만]`·`[이 동 해제]` | `routes/export/FloorChips.tsx` · `styles.css` | ✅ (`cb2b8fd`) |
| **B-6** | 출력 이력 줄에 동 표기 | `routes/export/RunHistory.tsx` · `routes/Export.tsx` · `export/exportModel.ts` | ✅ (`cb2b8fd`) |

### 공통 원칙 준수 (스펙 §2)

| # | 원칙 | 지킨 방법 |
|---|---|---|
| P1 | 동이 1개면 문자 그대로 지금과 같다 | 모든 분기가 `buildings.length >= 2`(또는 동 그룹 수 ≥ 2). 동 이름이 없으면 `null`/`''` 로 떨어져 문구가 예전 그대로 조립된다 |
| P2 | 저장 스키마 무변경 | 새 필드·새 스토어 0개. `DB_VERSION` 1 유지. `ExportRun` 스키마 무변경. `DamageSection.buildingId/buildingName`·`LocationMapPage.buildingName` 은 **런타임 모델** |
| P3 | 번호 계산 무수정 | `assignNumbers`·`numbering.ts` 를 한 줄도 안 건드렸다. 동은 `floorIds` 부분집합으로만 작용 |
| P4 | 동 이름은 `trim()`, 빈 이름은 없는 것처럼 | `locationMapFloors` · `buildDamageTable` · `runBuildingLabel` 모두 `trim()` 후 `''` 검사 |

### 세부 — 무엇을 왜 바꿨나

**B-3 조사위치도**
- 진짜 버그: `produce.ts` 가 `suffix: page.floorName` 이라 **두 동의 `1층` 이 같은 파일명**으로
  내려가 브라우저가 `(1)` 을 붙였다 → 어느 동인지 식별 불가. 동 2개 이상이면 `A동_1층`.
- 경고 문구(`NO_DRAWING`·`IMAGE_FAILED`·`CLIPPED`) 셋 다 `A동 1층 — …` 로.
- 문자열 조립은 `locationMapLabel`(공백 구분, 화면용) · `locationMapFileSuffix`(`_` 구분, 파일명)
  **두 함수 한 곳**뿐. 동 판정은 `exportModel.locationMapFloors` **한 곳**뿐 —
  호출부가 다운로드(`produce.ts`)·인쇄 뷰(`PrintRoute.tsx`) 둘이라 각자 판정하면
  파일명엔 동이 붙고 `alt` 엔 안 붙는 상태가 생긴다.
- **도면 렌더 경로는 무수정** — 좌표·번호 주입에 손대지 않았다(불변식 #1·#2).

**B-4 손상결함표·결함리스트**
- 섹션 제목이 `■ {층이름}` 뿐이라 두 동에 `1층` 이 있으면 **제목이 같은 섹션이 둘** 생겼다.
- 판정 기준을 `위치` 열(`buildLocations`)과 **완전히 동일**하게 맞췄다
  (`input.buildings.length >= 2` + 이름 `trim()`). 테스트로 두 함수의 판정 일치를 고정했다.
- 엑셀(`damageTableFile.ts`)·인쇄 뷰(`PrintDamageTable.tsx`)는 `section.title` 을 그대로
  쓰므로 **수정 불필요** — 코어 한 곳만 바꿔 둘 다 따라온다.
- `위치` 열은 스펙대로 **건드리지 않았다**(U84 · Q86 = 허용).
- 사진첩 캡션의 `위치` 도 무변경(`buildings: []` 를 넘기는 2026-09-04 결정 유지).

**B-2 층칩 동별 그룹**
- 그룹 순서·그룹 안 층 순서는 **`floors`(=`exportFloors()`) 등장 순서를 그대로** 쓴다.
  이 파일은 새 정렬 규칙을 만들지 않는다 (B-1 에서 넣은 `orderedIds()` 정렬 로직은 그대로 뒀다).
- `[이 동 전체]` = 현재 선택 **뒤에 이어 붙임**(이미 선택된 층은 자리를 안 바꾼다 —
  "누른 순서 = 출력 순서" 규칙이 흔들리지 않게).
  `[이 동만]` = 선택을 비우고 그 동만. `[이 동 해제]` = 그 동만 제거.
- 그룹 헤더에 `선택수/전체수` 배지. `[이 동 전체]` 는 이미 전부 선택이면, `[이 동 해제]` 는
  하나도 선택 안 됐으면 비활성.
- 칩 이름의 동 접두는 **뺐다** — 그룹 헤더와 중복(스펙 B-a 5).
- 하단 `[전체 선택]`·`[지하→지상]`·`[지상→지하]`·`[해제]` 는 그대로 두었다(동 전체 범위).
- CSS 는 `styles.css` **끝에 덧붙이지 않고** `.xp-chip__flag` 바로 뒤에 새 클래스
  (`.xp-bgroups`·`.xp-bgroup*`)만 추가했다. 기존 규칙은 한 줄도 안 고쳤다
  (다른 builder 의 `--defect-prev` 규칙과 겹치지 않는다).

**B-6 출력 이력**
- `12건 · 전체 이어서 · 3개 층 · A동` / `… · A동·B동`.
- **`ExportRun` 스키마 무변경** — 저장된 `run.floorRanges` 의 `floorId` 를 지금 `bundle` 로
  환산한다. 동 이름을 나중에 고치면 이 요약도 따라 바뀌지만, 그건 화면 요약이지
  재현되는 산출물 내용이 아니다(층 이름과 같은 성질 · D19 와 일관).
- 나열 순서는 `sortByOrder(buildings)` — 층칩·출력 순서와 같은 기준.

---

## 미완료 / 막힌 것

| 작업 | 이유 | 필요한 것 |
|---|---|---|
| **B-5** 사진첩 동별 머리말 + 동 경계 페이지 분리 | 지시대로 착수 안 함 | **Q83 답** (한 파일 안 동별 섹션인가 / 동마다 따로 파일인가). 답에 따라 페이지 분할 규칙이 정반대 |
| **B-7** 층 접두어 중복 경고 (`1F-01` 이 두 동에) | 지시대로 착수 안 함 | **Q85 답** (경고만 / 자동 접두). 자동 변경을 나중에 택하면 **이미 뽑은 보고서와 번호가 달라진다** |
| **B-8** `[동마다 따로 뽑기]` 일괄 버튼 | Q83 = C 일 때만 존재하는 작업 | Q83 답 |

⚠️ **`exportModel.ts::photoBookHeaderText` 의 비차단 가정 주석은 아직 옛 내용 그대로다.**
스펙 §4 "모순" 항목이 *"코드와 함께 이 주석도 반드시 갱신할 것"* 이라고 지적했지만,
그 갱신은 **B-5 의 일부**다(D45 가 뒤집는 가정 = 사진첩 머리말 규칙). B-5 를 할 때 함께 고쳐야 한다.
지금 주석만 먼저 고치면 코드와 주석이 어긋나므로 그대로 두었다.

---

## 검증한 것

| 항목 | 결과 |
|---|---|
| 타입 검사 (3 workspace) | ✅ `npm run typecheck` — canvas-core · project-core · web 전부 통과 |
| 단위 테스트 | ✅ `npm test` — canvas-core **30파일/477건** · project-core **22파일/399건**, 실패 0 (이번 라운드에 11건 추가: `drawingScope` 7 · `damageTable` 4) |
| 프로덕션 빌드 | ✅ `npm run build` — 308 모듈, 4.16s. 새 경고 없음(기존 chunk 500kB 경고만) |
| 스펙 대조 | ✅ 6개 작업 각각을 스펙 §2 A/B 절 · §3 표와 한 줄씩 대조 |

### 이번에 추가한 단위 테스트

**`packages/project-core/test/drawingScope.test.ts` (7건)** — A-1 대상 선정
- 같은 동의 도면만 고른다 / 입력 순서 보존 / 동 1개면 전체와 같다(P1) /
  층을 모르는 고아 도면 제외 / 없는 동 id → 빈 배열 / 빈 동 id → 빈 배열(전체로 새지 않음) /
  입력 무변형(순수 함수)

**`packages/project-core/test/damageTable.test.ts` (4건 추가)** — B-4 섹션 제목
- 동 2개면 `■ 본관 1층`·`■ 별관 1층` (두 동에 모두 `1층` 이 있어도 구분) /
  동 1개면 `■ 지하1층` 그대로(P1) / 이름이 빈 동은 없는 것처럼(P4) /
  **섹션 제목 판정 == `buildLocations` 판정** (두 곳이 갈리지 않음)

### 미검증 (코드로 확인 불가 — 아래 체크리스트로 넘김)

- 화면 렌더 결과(동 그룹 헤더 배치·줄바꿈·다크 테마 대비)
- 실제 다운로드 파일명 · 실제 PDF 섹션 머리 행
- 캔버스 배율 미리보기·되돌리기의 실제 동작

---

## 직접 확인해주실 것

동이 **2개 이상**이고 두 동에 **같은 이름의 층**(예: 둘 다 `1층`)이 있는 용역으로 봐주세요.
**동이 1개인 용역도 하나 열어 아무것도 안 바뀌었는지** 꼭 함께 봐주세요 — 이번 변경의
가장 중요한 안전장치가 "동 1개면 무변화" 입니다.

### A-1 · 도면 배율

- [ ] **A동 도면 → [도면 크기] → 체크 → 125% → [적용]** →
      **B동 도면을 열었을 때 100% 그대로**인가. A동의 다른 층 도면은 125%인가
- [ ] 체크를 **켰다가 다시 끄고** [적용] → 그 도면 하나만 바뀌는가
      (다른 도면이 미리보기 상태로 안 남는가)
- [ ] 적용 토스트의 장수가 **그 동의 도면 수**인가 (용역 전체 수가 아니라)
- [ ] **동이 1개인 용역**에서 체크박스 문구가 **예전 그대로**(`모든 도면에 같은 배율 적용`)인가

### B-1 · 기본 층 순서

- [ ] 출력 화면을 **처음 열었을 때** 층칩의 순번 뱃지가 **왼→오 순서와 일치**하는가
      (`A동 지하1층 ①, A동 1층 ②, … B동 지하1층 ⑤ …` — 동을 오가며 지그재그가 아닌가)
- [ ] `[지하→지상]`·`[지상→지하]` 를 눌러도 **동이 섞이지 않는가**
      (`[지상→지하]` 는 동 순서는 그대로, 각 동 안에서만 위→아래로 뒤집힌다)

### B-2 · 층칩 동 그룹

- [ ] 동 그룹 헤더(동 이름 + `선택수/전체수`)와 세 버튼이 보이는가
- [ ] **`[이 동만]`** 을 누르면 **그 동의 층만** 선택되고 다른 동 선택이 사라지는가
- [ ] `[이 동 전체]` 를 누르면 그 동의 층이 **현재 선택 뒤에** 붙는가
      (앞서 고른 층들의 순번이 그대로인가)
- [ ] `[이 동 해제]` 가 그 동의 층만 빼는가
- [ ] **동이 1개인 용역**에서 그룹 헤더가 **안 보이고** 화면이 예전과 같은가
- [ ] 칩 이름에 동 이름이 **중복해서** 안 붙는가 (그룹 헤더가 이미 말하므로 뺐습니다)

### B-3 · 조사위치도

- [ ] 조사위치도를 받으면 파일 2개가 **이름으로 구분**되는가
      (`…_조사위치도_A동_1층_….png` / `…_조사위치도_B동_1층_….png`)
- [ ] 도면 없는 층 경고가 `A동 1층 — 도면이 없어…` 처럼 **어느 동인지** 보이는가
- [ ] **동이 1개인 용역**의 파일명이 예전과 **한 글자도 같은가**(`…_조사위치도_1층_….png`)

### B-4 · 손상결함표 · 결함리스트

- [ ] 엑셀·PDF 모두 섹션 머리 행이 **`■ A동 1층`** 인가 (두 동의 `1층` 섹션이 구분되는가)
- [ ] `위치` 열은 **예전 그대로** `A동 1층 …` 인가 (제목과 중복되지만 이번엔 의도된 것 · Q86)
- [ ] **동이 1개인 용역**의 섹션 제목이 `■ 1층` 그대로인가

### B-6 · 출력 이력

- [ ] 이력 줄에 `12건 · 전체 이어서 · 3개 층 · A동` 처럼 **어느 동인지** 보이는가
- [ ] 두 동을 함께 뽑은 이력은 `· A동·B동` 인가
- [ ] **동이 1개인 용역**의 이력 줄이 예전과 같은가 (동 표기 없음)

### 전체

- [ ] **동이 1개인 용역**에서 뽑은 산출물 4종이 **이전에 뽑은 것과 글자 단위로 똑같은가**
- [ ] 옛 출력 이력에서 `[같은 번호로 다시 받기]` 를 눌렀을 때 **번호가 그때 그대로**인가
      (동 변경은 번호에 관여하지 않도록 만들었습니다 — P3)

---

## 알려진 한계 · 판단이 필요했던 곳

1. **`drawingsInBuilding` 을 `data/drawingScale.ts` 가 아니라 `project-core` 에 두었다.**
   스펙 A-c 는 `data/drawingScale.ts` 에 두라고 했지만 **`apps/web` 에는 테스트 러너가 없고**,
   그 파일은 브라우저 API(`composeA4`)를 import 해서 테스트에서 불러올 수 없다.
   기존 관례(`photoTransform.ts` — *"apps/web 에는 테스트 러너가 없으므로 여기 순수 함수로 뺀다"*)를
   따라 `project-core/drawingScope.ts` 에 구현하고, **`data/drawingScale.ts` 에서 재수출**해
   호출부의 import 경로는 스펙대로 유지했다. 대상 선정이 틀리면 다른 동 좌표가 조용히 밀리는
   되돌리기 어려운 사고라 테스트를 포기할 수 없었다. (폴더 구조 결정이라 질문하지 않았다)

2. **이름이 빈 동의 그룹 헤더는 `(이름 없는 동)` 으로 쓴다.**
   P4 는 "빈 이름이면 없는 것처럼 취급" 인데, 그룹 헤더는 그룹 자체가 존재하므로 라벨이 필요하다.
   파일명·섹션 제목·이력 요약에서는 P4 그대로 **아예 안 붙인다**(빈 이름 = 표기 없음).
   화면 헤더에만 자리표시자를 쓴다. 문구가 마음에 안 드시면 알려주세요.

3. **`[이 동 전체]` 는 이미 선택된 층의 자리를 바꾸지 않는다.**
   "현재 선택 뒤에 이어 붙인다" 를 그대로 읽어 **아직 안 고른 층만** 뒤에 붙였다.
   이미 고른 층까지 끝으로 옮기면 앞서 정해둔 출력 순서가 흔들린다.

4. **`photoBookHeaderText` 의 옛 비차단 가정 주석이 아직 남아 있다** (위 "미완료" 참조).
   B-5 와 함께 고쳐야 한다 — 지금 주석만 고치면 코드와 어긋난다.

5. **다른 동의 도면 배율을 한 번에 바꾸는 방법은 이제 없다.** U82/Q84 의 "교체" 가정대로
   용역 전체 일괄 선택지를 두지 않았다. 동이 3개면 세 번 해야 한다.

6. **`_workspace/ASSUMPTIONS.md`·`QUESTIONS.md`·`TASKS.md` 의 미커밋 수정은 건드리지 않았다**
   (리더의 작업 중 파일로 보임). `83_…spec…md` 도 커밋하지 않았다.

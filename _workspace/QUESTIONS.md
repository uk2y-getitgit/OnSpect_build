# QUESTIONS

에이전트 → 사용자 질문 적재. 리더가 모아서 사용자에게 전달한다.

---

## Q1 · plan-reviewer · 2026-08-22 · Phase 3

**막힌 지점:**
도면에서 [점] 도구로 위치를 찍는 **그 순간** 결함(Defect) 레코드가 만들어져 저장되는가?
만들어진다면 부재·결함유형이 아직 비어 있는 **미완성 결함**이 존재하게 된다. 이걸 허용하는가?

상세기획 §2.5 **G7** 이 이 항목을 `⬜ 확인 필요` 로 남겨두었는데,
Phase 3에서 "점을 찍는다"를 구현하는 순간 반드시 걸린다.

**왜 임의로 정하면 안 되는가:**
- 좌측 조사항목 리스트에 **찍자마자 1건이 늘어나는지** 여부가 달라진다 (사용자가 즉시 보는 동작)
- 허용하면 `Defect.status` 또는 별도 플래그에 `DRAFT` 개념이 **데이터 모델에 굳는다**
- 불허하면 마커 생성이 "클릭 → 필수 항목 입력 완료 전까지 임시" 라는 완전히 다른 흐름이 된다
- 출력 시 미완성 결함을 어떻게 다룰지(경고·제외)가 Phase 4에 연쇄된다

**선택지:**
- **A. 허용 — 찍는 즉시 저장, 미완성 상태 허용**
  → 리스트에 즉시 뜬다. `미완성 N건` 배지 + 출력 시 경고가 필요하다.
  → 근거: 젠트릭스 실측 화면(`스크린샷 _01`)에서 마커 37번이 이미 도면에 있는데
     우측 패널의 결함유형은 `선택해주세요.` 상태다 — **경쟁 제품은 허용한다.**
  → 근거: 상세기획 §2-B 입력 3원칙 #3 "직전 입력을 기억한다 — 위치만 새로 찍으면 됨"은
     찍는 순간 직전 값이 채워진 결함이 생긴다는 뜻이다. A안과 정합한다.
- **B. 불허 — 필수 항목을 채워야 저장**
  → 찍으면 임시 마커만 뜨고, 부재·결함유형을 고른 뒤에 확정된다.
  → 현장에서 손이 바쁠 때(§2.5 G7 "사진 먼저 찍는 경우") 흐름이 끊긴다.
- **C. 허용하되 출력에서 자동 제외** (A + 안전장치)
  → 미완성은 저장되지만 손상결함표·리스트 출력에서 빠지고, 출력 모달에 `미완성 N건 제외됨` 표시.

**추천:** **A안**, 여유가 되면 **C안**.
경쟁 제품이 A로 동작하고, 현장 입력 속도가 이 제품의 핵심 가치이기 때문이다.

**차단 여부:** **차단** — 단 **T11(점 마커 생성) 한정.**
T1~T10(코어 기하·뷰포트·렌더·히트테스트·드래그·Undo·스냅 2종·병합)은 답 없이 착수해도
구조가 굳지 않는다. **T11만 세워두고 나머지를 진행하면 된다.**

---

## Q2 · plan-reviewer · 2026-08-22 · Phase 3

**막힌 지점:**
상세기획 §3-3은 "하나의 Defect에 Mark N개(긴 균열 = 표시 여러 개, 번호 1개)",
"Defect당 Label 1개"라고 한다. 그러면 **마크가 3개일 때 리더선은 몇 개이고 어디에 붙는가?**

**왜 임의로 정하면 안 되는가:**
도면에 그려지는 그림 자체가 달라지고, **리더선 각도 스냅이 무엇을 기준으로 각도를 재는지**가
여기서 정해진다. 젠트릭스 스크린샷 14장에는 마크 N개 사례가 잡히지 않아 근거가 없다.

**선택지:**
- **A. 리더선 1개 — 대표 마크에 붙는다** (`Label.anchorMarkId` 에 마크 id 저장)
  → 선이 깔끔하다. 대표 마크는 첫 마크가 기본이고 사용자가 바꿀 수 있다.
- **B. 리더선 N개 — 모든 마크에 부챗살처럼 붙는다**
  → "이 번호가 이 세 군데를 가리킨다"가 명확하다.
  → **각도 스냅이 어느 선 기준인지 모호해진다.** 이번 범위의 핵심 기능과 충돌한다.
- **C. 리더선 1개 — 마크 집합의 중심(centroid)에 붙는다** (`anchorMarkId = null`)
  → 필드 없이 자동. 다만 마크가 흩어져 있으면 선 끝이 **아무것도 없는 빈 공간**을 가리킨다.

**추천:** **A안.** 각도 스냅이 단일 앵커를 요구하고, B의 부챗살은 도면이 지저분해진다.

**차단 여부:** **비차단.**
`Label.anchorMarkId: uuid | null` 필드를 **지금 미리 정의**하면 어느 답이 와도 마이그레이션이 없다
(A → 마크 id 저장 / C → null / B → 필드 무시하고 전체 순회).
1차 구현은 **A안 + `anchorMarkId = marks[0].id`** 로 진행한다.

---

## Q3 · plan-reviewer · 2026-08-22 · Phase 3

**막힌 지점:** 마크(점)를 드래그해 옮기면 **번호 풍선도 따라 움직이는가, 제자리에 남는가?**

**왜 임의로 정하면 안 되는가:** PC 정리 작업의 결과가 유지되느냐 날아가느냐가 갈린다.

**선택지:**
- **A. 따라온다 (같은 델타만큼 이동)** → 리더선 길이·각도가 보존된다. 정리해둔 줄맞춤이 유지된다.
- **B. 제자리에 남는다** → 리더선이 늘어나거나 각도가 틀어진다. 마크만 미세 조정했는데 정리가 깨진다.

**추천:** **A안.** PC 정리 단계에서 라벨 위치는 공들여 맞춘 결과물이다.

**차단 여부:** **비차단.** A안으로 구현. 코드 한 줄 차이라 뒤집기 쉽다.

---

## Q4 · plan-reviewer · 2026-08-22 · Phase 3

**막힌 지점:** **번호 풍선의 크기가 줌에 따라 함께 커지는가, 화면상 항상 같은 크기인가?**

**왜 임의로 정하면 안 되는가:**
- **화면과 출력물이 같아 보이는지**(WYSIWYG)가 여기서 결정된다
- 줌아웃 상태에서 풍선이 너무 작아져 **누를 수 없는** 문제가 생길 수 있다
- 젠트릭스 스크린샷 근거가 **엇갈린다** — 줌아웃 화면(`_00`,`_05`)과 줌인 화면(`_02`)의
  풍선 크기 비율이 배율 차이만큼 벌어지지 않는다. 경쟁 제품이 어느 쪽인지 단정할 수 없다

**선택지:**
- **A. 도면 좌표 고정 (줌하면 함께 커짐, WYSIWYG)**
  → 출력물과 화면이 정확히 같다. 스타일 슬라이더(번호풍선 크기)의 효과가 화면에 그대로 보인다.
  → 줌아웃하면 아주 작아진다. (히트 영역만 스크린 기준 최소 12px 보장으로 보완)
- **B. 화면 고정 (항상 같은 크기)**
  → 어느 배율에서도 누르기 쉽다.
  → **화면과 출력물이 다르게 보인다.** 슬라이더로 크기를 맞춰도 화면에서 확인이 안 된다.

**추천:** **A안 + 히트 영역 최소값 보장.** 이 제품의 PC 화면은 "출력물을 정리하는 화면"이므로
화면에서 본 것이 그대로 출력되어야 한다.

**차단 여부:** **비차단.** A안으로 구현.

---

## Q5 · plan-reviewer · 2026-08-22 · Phase 3

**막힌 지점:** **리더선 각도 스냅에 45도 배수를 포함하는가?**

**왜 임의로 정하면 안 되는가:**
기획서 원문은 "각도가 **90도**(수직/수평) 근처에 오면 자동 직교화"만 말한다.
45도를 넣는 것은 기획서를 넘어서는 판단이고, **사용자가 원하는 사선 배치를 방해할 수 있다.**

**선택지:**
- **A. 90도 배수 4방향만** (`0/90/180/270`)
  → 기획서 문구 그대로.
  → 근거: 젠트릭스 실측 도면의 리더선 다수가 **의도적인 임의 사선**이다.
     45도를 켜면 그 사선들이 전부 45도로 끌려가 오히려 방해가 된다.
- **B. 45도 배수 8방향** (`0/45/90/…/315`)
  → 대각선 배치가 깔끔해진다. 사선 리더가 많은 도면에서는 이쪽이 나을 수 있다.

**함께 확인 부탁드리는 기본 수치** (감이 안 맞으면 숫자만 바꾸면 됩니다)
| 항목 | 제안값 |
|---|---|
| 각도 스냅 진입 | **5도** 이내 |
| 각도 스냅 이탈 | 8도 (떨림 방지) |
| 각도 스냅 최대 이동 | **12px** — 리더선이 길면 5도라도 라벨이 크게 튀므로 상한을 둠 |
| 최소 리더 길이 | 24px 미만이면 각도 스냅 끔 |
| 정렬 스냅 진입 / 이탈 | **6px / 10px** |

**추천:** **A안.** 위 수치대로 구현하고 전부 상수 한 파일(`constants.ts`)에 모은다.

**차단 여부:** **비차단.** A안 + 위 수치로 구현. `ANGLE_SNAP_45` 플래그만 켜면 B가 된다.

---

## Q6 · plan-reviewer · 2026-08-22 · Phase 3

**막힌 지점:**
번호 풍선을 끄는 동안 **스마트 정렬 가이드**와 **리더선 각도 스냅**이 동시에 걸릴 수 있다.
같은 축을 서로 다른 값으로 당길 때 **어느 쪽이 이기는가?**

*(참고: 대부분은 싸우지 않는다. 각도 스냅이 수직이면 x만 고정하므로 y축 줄맞춤은 동시에 적용된다.
문제가 되는 건 각도 스냅이 고정한 축을 정렬 스냅도 잡으려는 경우뿐이다.)*

**왜 임의로 정하면 안 되는가:** 사용자가 손끝으로 매번 느끼는 동작이다.

**선택지:**
- **A. 각도 스냅 우선** → 리더선 직교가 지켜지고, 그 축의 줄맞춤은 포기한다.
  → 근거: 리더선이 89도인 건 도면에서 눈에 확 띄지만, 번호가 2px 어긋난 건 티가 덜 난다.
- **B. 정렬 스냅 우선** → 번호 줄맞춤이 지켜지고, 리더선 직교는 포기한다.
  → 번호를 여러 개 나란히 정리하는 작업 중이라면 이쪽이 자연스럽다.

**추천:** **A안.** 어긋남이 더 크게 보이는 쪽을 지킨다.

**차단 여부:** **비차단.** `SNAP_PRIORITY = 'ANGLE_FIRST'` 상수로 구현. 한 줄로 뒤집힌다.

> 참고로 **적용되지 않은 스냅의 가이드선은 그리지 않는다.** 선은 보이는데 라벨이 안 붙어 있으면
> 사용자는 기능이 고장났다고 느낀다.

---

## Q7 · plan-reviewer · 2026-08-22 · Phase 3

**막힌 지점:**
상세기획 §3-3은 `marks: Mark[]` 를 "**1개 이상**"으로 정의한다.
그러면 **마지막 남은 마크를 지우면** 어떻게 되는가?

**왜 임의로 정하면 안 되는가:** 사진과 입력값까지 딸린 결함 레코드가 통째로 사라질 수 있다.

**선택지:**
- **A. 확인 다이얼로그 → 결함 전체 삭제** (`이 표기를 지우면 결함 1건이 함께 삭제됩니다`)
  → Undo로 되살릴 수 있다.
- **B. 삭제 거부** (`마지막 표기는 지울 수 없습니다`) → 결함 삭제는 리스트에서만.
- **C. 마크만 지우고 결함은 "위치 미지정" 상태로 남긴다**
  → §2.5 G1 엑셀 임포트가 만드는 `위치 미지정 N건` 상태와 같은 통을 쓴다. 재활용성이 있다.

**추천:** **A안.** Undo가 있으므로 위험이 낮고 조작이 가장 직관적이다.
(C안은 §2.5 G1 엑셀 임포트를 구현할 때 자연스럽게 다시 검토하게 된다)

**차단 여부:** **비차단.** A안으로 구현.

---

## Q8 · plan-reviewer · 2026-08-22 · Phase 3

**막힌 지점:**
PC 정리 화면에서 **전회차 표기(`PREV_PENDING` 보라 / `REPAIRED` 회색)** 를
이동·삭제할 수 있는가, 보기만 하는가?

**왜 임의로 정하면 안 되는가:**
전회차 데이터는 **이전 회차의 기록**이다. 실수로 옮기거나 지우면 대조의 기준이 사라진다.
반대로 완전히 잠그면 전회차 번호가 현회차 번호와 겹쳐 보일 때 정리할 방법이 없다.

**선택지:**
- **A. 선택만 가능 (이동·삭제 불가)** → 기준 데이터가 안전하다. 겹침은 손댈 수 없다.
- **B. 라벨(번호 풍선) 이동만 허용, 마크 이동·삭제는 불가**
  → 마크는 실제 위치라 잠기고, 번호 위치만 겹침 해소용으로 옮길 수 있다.
- **C. 현회차와 동일하게 전부 허용**

**추천:** **B안.** 실제 위치는 잠그고 표기 정리만 허용하는 것이 "PC는 정리"라는 이 화면의 목적과 맞다.

**차단 여부:** **비차단.**
1차는 가장 안전한 **A안**으로 구현하고(전회차는 정렬 가이드 후보에는 포함),
답을 받으면 B/C로 확장한다. 렌더·히트테스트 구조는 세 안 모두 동일하다.

---

## Q9 · builder · 2026-08-22 · Phase 3 구현

**막힌 지점:**
스펙 §2-5 는 표기를 선택하면 **"우측 결함정보 패널"** 이 뜬다고만 쓰여 있고
그 패널이 무엇을 보여주는지 정의가 없다. Phase 3 은 도면 캔버스 범위이고
결함 속성 입력(부재·결함유형 선택, 폭·길이 입력)은 Phase 2 조사입력의 영역이다.

**왜 임의로 정하면 안 되는가:**
여기에 입력 폼을 넣으면 Phase 2 화면과 **두 벌**이 된다. 반대로 아무것도 안 넣으면
D3(미완성 결함 허용)에서 "미완성을 채우는 경로"가 이 화면에 없어진다.

**선택지:**
- **A. 읽기 전용 표시 + 캔버스 조작만** (번호 위치 초기화 / 삭제) → Phase 2 폼과 중복 없음
- **B. 최소 입력 폼(부재·결함유형 2개)까지 이 화면에 둔다** → 미완성 결함을 여기서 바로 채운다
- **C. Phase 2 조사입력 화면을 이 패널에 그대로 재사용** → Phase 2 완성 후에 가능

**추천:** **A안 → 나중에 C안.** Phase 2 폼이 나오면 그것을 이 자리에 끼운다.

**차단 여부:** **비차단.** 1차는 A안으로 구현했다 (ASSUMPTIONS C1).

---

## Q10 · builder · 2026-08-22 · Phase 3 구현

**막힌 지점:**
`POINT` 도구가 켜진 상태에서 **이미 있는 표기 위를** 클릭하면?
스펙 §2-5 는 "좌클릭 / 도면, POINT 도구 → 그 지점에 마크 생성" 이라고만 쓰여 있다.

**왜 임의로 정하면 안 되는가:** 사용자가 즉시 보는 동작이다.
"생성" 으로 잡으면 표기가 겹쳐 쌓이고, "선택" 으로 잡으면 겹친 자리에 결함을 못 찍는다.

**선택지:**
- **A. 히트되면 선택, 빈 도면이면 생성** → 실수로 겹쳐 쌓이지 않는다. 겹친 자리는 살짝 옆에 찍고 옮긴다
- **B. 도구가 켜져 있으면 무조건 생성** → 겹친 자리에도 찍을 수 있지만 오조작이 잦다

**추천:** **A안.** 실수의 비용이 낮은 쪽.

**차단 여부:** **비차단.** A안으로 구현했다 (ASSUMPTIONS C2).

---

## Q11 · builder · 2026-08-22 · Phase 3 구현

**막힌 지점:**
전회차 표기(A8: 선택만 가능) 위에서 **커서를 무엇으로 할 것인가.**
`ui-quality.md` §3 표는 "마커·라벨 위 = `move`" 라고만 되어 있다.

**왜 임의로 정하면 안 되는가:** `move` 를 띄우면 "옮길 수 있다" 고 거짓말하는 것이 되고,
사용자는 끌어보고 나서야 안 된다는 걸 안다 (ui-quality 가 금지하는 상황).

**선택지:**
- **A. `pointer`** — 선택은 되지만 이동은 안 된다는 신호
- **B. `not-allowed`** — 아무것도 안 된다는 신호. 선택도 되는데 과한 표현
- **C. `move` 로 통일하고 끌면 토스트로 거절** — 조작 후에야 알게 된다

**추천:** **A안.**

**차단 여부:** **비차단.** A안으로 구현했다 (ASSUMPTIONS C3).

---

## Q12 · builder · 2026-08-22 · Phase 3 구현

**막힌 지점:**
창 크기를 바꾸면(`RESIZE`) 뷰포트를 **다시 Fit 할 것인가, 보던 자리를 유지할 것인가.**
스펙 §2-3 은 "도면(층)마다 뷰포트를 기억한다" 만 정하고 리사이즈는 언급하지 않는다.

**왜 임의로 정하면 안 되는가:** 확대해서 세밀하게 정리하던 중에 창을 넓혔더니
화면이 전체보기로 튀면 작업 위치를 잃는다. 반대로 유지하면 창을 크게 넓혔을 때
도면이 화면 한쪽에 치우쳐 보인다.

**선택지:**
- **A. 유지 (팬 클램프만)** — Figma·PowerPoint 방식. 작업 위치를 잃지 않는다
- **B. 항상 재-Fit** — 항상 전체가 보이지만 확대 작업 중에 튄다

**추천:** **A안.** `전체 맞춤` 버튼 / `0` 키 / 빈 곳 더블클릭으로 언제든 되돌릴 수 있다.

**차단 여부:** **비차단.** A안으로 구현했다 (ASSUMPTIONS C4).

---

## Q13 · plan-reviewer · 2026-08-22 · S1 용역·도면 업로드

**막힌 지점:**
**도면 업로드에 PDF 를 지원할 것인가, 1차는 이미지(PNG/JPG/WEBP/SVG)만 할 것인가.**
상세기획 §G2 는 `PDF 다중페이지 → 페이지별 분할` 을 S1 흐름에 넣어 두었지만
**PDF 렌더링 수단이 정해져 있지 않다.**

**왜 임의로 정하면 안 되는가:**
`pdf.js` 도입은 번들(워커 포함 1MB+)·라이선스(MPL-2.0)·오프라인 번들링을 건드린다.
반대로 미루면, **사용자가 가진 실제 도면이 대부분 PDF 일 경우 S1 의 목적(실데이터로 테스트)이 반쯤만 달성된다.**
"도면업로드가 빠져서 테스트가 쉽지 않다"는 지적이 S1 을 만든 이유이므로, 이 판단이 S1 의 가치를 좌우한다.

**선택지:**
- **A. 1차는 이미지만. PDF 는 후속(S1-T13)**
  → 지금 착수 가능. PDF 파일을 드롭하면 `PNG/JPG로 변환해 올려주세요` 안내가 뜬다.
     사용자가 도면을 미리 변환해야 하는 수고가 생긴다.
- **B. 지금 PDF 를 넣는다 (`pdf.js`)**
  → 실무 도면을 그대로 올릴 수 있고 페이지=층 자동 배정까지 가능. S1 완료가 늦어지고 번들이 커진다.

**추천:** **A안.** 다만 **구조적으로는 이미 B를 대비해 두었다** —
인입 파이프라인을 `파일 → PageCandidate[] → 층 배정 → Drawing[]` 로 잘라
이미지 경로와 PDF 경로가 **뒤쪽 화면·모델·저장을 공유**하게 설계했고(스펙 §1),
`Drawing.source` 에 `{kind:'PDF_PAGE', pageIndex, pageCount}` 를 **지금 미리 정의**했다.
그래서 나중에 켜도 **마이그레이션이 없다.**

**차단 여부:** **비차단.** A안으로 진행하며 S1-T13 을 조건부 작업으로 세워 둔다.
답이 "B" 면 T8 직후에 T13 을 끼운다. 앞선 작업을 버리지 않는다.

**사용자께 드리는 되물음 한 줄:** 지금 손에 있는 도면 원본이 **PDF 인가 이미지인가?**
PDF 뿐이라면 A안은 매번 변환 작업을 강요한다.

---

## Q14 · plan-reviewer · 2026-08-22 · S1 용역 등록 폼

**막힌 지점:** 두 가지가 상세기획 §3-1 과 어긋난다.
1. **점검구분** — 사용자 지정은 3종(정기안전점검·정밀안전점검·정밀안전진단)인데
   상세기획 §3-1 은 **`기타` 를 포함한 4종**이다.
2. **§3-1 의 `발주처` · `점검기간` · `구조유형 기본값`** 을 S1 등록 폼에서 **빼도 되는가.**

**왜 임의로 정하면 안 되는가:**
`기타` 를 살리면 자유 텍스트 필드가 하나 더 붙고 **표시명 생성 규칙이 갈라진다**
(`2026 상반기 ○○아파트 기타` 는 말이 되지 않으므로 별도 처리가 필요하다).
`발주처`·`점검기간` 은 **보고서 머리말에 쓰인다**(§3-1). 지금 안 받으면 Phase 4 에서
기존 용역 전부를 다시 편집해야 한다.

**선택지:**
- **A. 3종 + 나머지 3필드는 모델에만 예약(`null`), 폼에는 넣지 않는다**
  → 등록이 4필드로 끝나 빠르다. Phase 4 전에 편집 화면에서 채우면 된다.
- **B. 4종(기타 포함) + 3필드를 접이식 `선택 입력` 으로 폼에 둔다**
  → 한 번에 다 받는다. 등록 폼이 길어지고 대부분 비워질 것이다.

**추천:** **A안.** 사용자가 이번에 명시한 필드가 정확히 4개이고,
예약 필드는 `Project` 타입에 이미 넣어 두었으므로 나중에 폼만 추가하면 된다(마이그레이션 없음).

**차단 여부:** **비차단.** A안으로 확정해 스펙 §2-4·§2-5 에 반영했다.

---

## Q15 · plan-reviewer · 2026-08-22 · S1 도면 업로드

**막힌 지점:**
**한 층에 도면이 여러 장 필요한가.**
상세기획 §3-2 는 `Drawing.floorId` 라서 **1층 : N도면**이 가능한데,
Phase 3 캔버스는 `SET_DRAWING` 이 **층당 도면 1장**을 전제로 이미 만들어져 있다.

**왜 임의로 정하면 안 되는가:**
1:N 을 허용하면 좌측 트리가 `동 ▸ 층 ▸ 도면` **3단**이 되고,
층 전환 UX·뷰포트 기억(층별 → 도면별)·번호 부여 범위(층 내 → ?)가 전부 한 겹씩 늘어난다.
반대로 1:1 로 굳히면, 대형 건물에서 한 층을 구역별로 나눈 도면(A구역/B구역)을 받을 수 없다.

**선택지:**
- **A. S1 은 층당 도면 1장. 모델은 1:N 을 유지(`Drawing.floorId` + `sortOrder`)**
  → 화면이 단순하고 Phase 3 을 그대로 쓴다. 나중에 켜면 트리에 한 단이 늘어난다.
- **B. 지금부터 층당 N장**
  → 구역 분할 도면을 바로 받는다. 트리·층 전환·뷰포트 기억을 지금 다시 설계해야 한다.

**추천:** **A안.** 모델이 이미 1:N 이므로 데이터를 버리지 않는다. 제약은 UI 에만 건다.

**차단 여부:** **비차단.** A안으로 확정해 스펙 §2-4-b·§2-8 에 반영했다.

**사용자께 드리는 되물음 한 줄:** 실무에서 **한 층 평면도를 2장 이상으로 나눠 쓰는 경우**가 있는가?

---

## Q16 · builder · 2026-08-22 · S2a 자유그리기

**막힌 지점:**
**자유그리기를 그렸을 때 어느 결함에 붙는가.**
`07_spec_S2a_and_responsive.md` §S2a-1 은 자유그리기를 "마크가 아니라 **결함에 종속된 스케치**"
라고 하고 "**결함번호와 연동된다**" 고 한다. 그런데 §S2a-2 의 생성 조작 표에는
`그리기 | 드래그로 자유 곡선` 뿐이고, **어느 결함에 붙는지가 없다.**

**왜 임의로 정하면 안 되는가:**
화면에 보이는 동작이 갈린다.
상세기획 §3-3 은 `marks: Mark[] — 1개 이상`이라서 **스케치만으로는 유효한 결함을 만들 수 없다.**
그러면 아무것도 선택하지 않은 채 그렸을 때 셋 중 하나여야 한다:
① 만들지 않는다 ② 마크 없는 결함을 만든다(상세기획 위반) ③ 스케치 중심에 점 마크를 몰래 만든다.
③ 은 사용자가 만들지 않은 표기가 생기고, ② 는 번호 풍선이 없는 유령 결함이 생긴다.

**선택지:**
- **A. 선택된 결함에 붙인다. 선택이 없으면 만들지 않고 안내 토스트**
  → 상세기획 `marks ≥ 1` 을 지킨다. 대신 "결함 먼저 선택" 한 단계가 늘어난다
- **B. 선택이 없으면 스케치 중심에 점 마크를 함께 만들어 새 결함을 만든다**
  → 한 번에 끝난다. 대신 사용자가 찍지 않은 점 표기가 도면에 생긴다

**추천:** **A안.** 스케치는 "이 결함이 이렇게 생겼다" 는 부연이지 결함 그 자체가 아니다.
결함을 먼저 만들고 덧그리는 것이 실제 작업 순서와도 맞는다.

**차단 여부:** **비차단.** A안으로 구현했다 (`commitCreateSketch`).
바꾸려면 `interaction.ts` 의 그 함수 하나만 고치면 된다.

**사용자께 드리는 되물음 한 줄:**
현장에서 **결함을 찍기 전에 먼저 형상을 스케치**하는 순서가 흔한가?

---

## Q17 · plan-reviewer · 2026-08-23 · S3 설정 화면

**막힌 지점:**
**설정 화면이 편집하는 대상이 "전역 마스터"인가 "지금 열어 둔 용역의 설정"인가.**

상세기획 §2-A 는 **서로 배타적인 두 가지를 동시에 약속한다.**
- 효과 목록: *"이름을 고치면 전 프로젝트에 한 번에 반영"*
- 바로 아래 인용 블록: *"Project 생성 시 항목설정을 **스냅샷**으로 복사해 붙인다.
  조직 설정을 바꿔도 이미 만든 과업의 보고서 용어는 변하지 않는다"*

스냅샷이 있으면 일괄 반영은 **불가능하다.** 문서가 이 충돌을 해소하지 않았다.

**왜 임의로 정하면 안 되는가:**
화면 수와 진입 경로가 갈린다.
- 전역만 편집하게 하면 → **현장에서 없는 부재를 즉시 추가할 수 없다.** 그러면 앱을 못 쓴다
- 용역만 편집하게 하면 → 용역 A 에서 다듬은 목록이 **용역 B 로 영영 넘어가지 않는다**
- 둘 다 만들면 → 사용자가 지금 어느 쪽을 고치고 있는지 헷갈리고, "왜 저쪽엔 안 생기지" 가 반복된다

그리고 이건 **보고서 용어가 회차마다 바뀌느냐**의 문제라, 잘못 정하면 작년 보고서와 올해 보고서의
부재명이 서로 달라진다.

**선택지:**
- **A. 설정 화면은 언제나 "이 용역의 설정"만 편집한다. 전역(ORG)은 새 용역의 초기값 역할만 한다.
  `[이 설정을 기본값으로 저장]` 버튼 하나로 현재 용역 설정을 전역에 승격시킨다**
  → 불변식 #7(과업 스냅샷)을 그대로 지킨다. 현장 즉시 추가가 된다.
     개선이 다음 용역으로 넘어가는 길도 있다. 다만 **이미 만든 다른 용역에는 반영되지 않는다**
- **B. 전역 설정 화면과 용역 설정 화면을 따로 둔다 (2개)**
  → 조직 표준과 현장 예외를 분리해 관리할 수 있다.
     화면·라우트·"어느 쪽을 보고 있나" 표시가 전부 두 벌이 된다
- **C. 스냅샷을 만들지 않는다. 모든 용역이 전역 설정 하나를 본다**
  → 가장 단순하고 "전 프로젝트 일괄 반영"이 그대로 성립한다.
     대신 **작년 용역의 부재명이 오늘 바뀐다.** 불변식 #7 을 버리는 것이다

**추천:** **A안.** 스냅샷(불변식 #7)은 보고서 산출물의 신뢰성 문제라 포기할 수 없고,
현장 즉시 추가도 포기할 수 없다. 둘을 동시에 만족하는 최소 형태가 A 다.
B 는 필요해지면 라우트 하나를 더 여는 것으로 언제든 올라갈 수 있다.

**차단 여부:** **비차단.** A안으로 스펙 §2-2·§2-4·§2-5-a 에 확정했다.
데이터 모델은 A·B·C 가 전부 같고(문서 2종: `ORG` / `{projectId}`),
화면은 `scope` 프롭 하나라 뒤집는 비용이 낮다.

**사용자께 드리는 되물음 한 줄:**
**작년에 쓴 부재명을 올해 고쳤을 때, 작년 보고서를 다시 뽑으면 옛 이름이 나와야 하는가 새 이름이 나와야 하는가?**

---

## Q18 · plan-reviewer · 2026-08-23 · S4 규모 입력

**막힌 지점:**
**폭 구간 버튼이 손상결함표에 넣는 숫자가 정의돼 있지 않다.**

젠트릭스 실측 사이드바의 폭 프리셋은 **구간**이다 —
`~0.1mm미만` `0.1mm이상~0.2mm미만` `0.2mm이상~0.3mm미만` `0.3mm이상~0.5mm미만` `0.5mm이상` `직접입력`.
그런데 손상결함표에 인쇄되는 폭은 **단일 숫자 `0.2`** 이고,
면적도 그 숫자로 계산된다 (`0.2 ÷ 1000 × 2000 ÷ 1000 = 0.0004`).

즉 구간 버튼 하나를 누르면 **어떤 대표 숫자 하나가 확정돼 보고서에 찍힌다.** 그 값이 무엇인지가 없다.

**왜 임의로 정하면 안 되는가:**
이 숫자가 **보고서에 그대로 인쇄되고 면적까지 결정한다.**
`0.1~0.2` 를 0.1 로 잡느냐 0.2 로 잡느냐에 따라 같은 결함의 면적이 **2배 차이**난다.
그리고 이것은 발주처가 보는 수치라 실무 관행이 이미 정해져 있을 가능성이 높다.

**선택지:**
- **A. 구간 상한** — `~0.1미만`→0.1, `0.1~0.2`→0.2, `0.2~0.3`→0.3, `0.3~0.5`→0.5
  → 실측표의 `0.2` 와 맞는다(0.1~0.2 버튼에서 나왔다고 보면). 보수적으로 크게 잡는다
- **B. 구간 하한** — `0.1~0.2`→0.1, `0.2~0.3`→0.2
  → "이상" 값을 적는 해석. `~0.1미만` 은 0 이 되어 무의미해진다
- **C. 구간 중앙** — `0.1~0.2`→0.15
  → 소수 둘째 자리가 생겨 표가 지저분해진다

**추천:** **A안(상한).** `~0.1mm미만` 이 0.1 로 읽히는 것이 가장 자연스럽고,
실측 손상결함표의 `0.2` 와도 모순되지 않는다.
`0.5mm이상` 만 예외로 두어(상한이 없으므로) **직접입력으로 전환하고 0.5 를 초기값**으로 넣는다.

**차단 여부:** **비차단.** A안으로 확정하고 매핑을 `items/presets.ts` **상수표 한 곳**에 모았다.
답이 오면 그 표의 숫자만 바꾸면 된다.

**사용자께 드리는 되물음 한 줄:**
**균열폭을 구간으로 관찰했을 때, 보고서 폭 열에는 실제로 어떤 숫자를 적으시는가?**

---

## Q19 · plan-reviewer · 2026-08-23 · S4 규모 입력

**막힌 지점:**
**"넓이 × 높이"로 입력한 결함의 손상결함표 `폭`·`길이` 열에 무엇을 쓰는가.**

사용자 표현은 *"균열 폭×길이 또는 **넓이×높이**"* 인데,
상세기획 `AREA` 모드는 **면적 직접입력**이고(누수흔적 실측 `0 / 0 / 0.5`),
가로×세로는 §2-B 에서 `[가로×세로로 입력] ← 누르면 가로·세로 입력 후 자동계산` 이라는
**면적을 구하는 보조 수단**으로만 나온다.

그래서 가로 1000 · 세로 500 으로 입력해 면적 0.5㎡ 를 얻었을 때,
표의 `폭(mm)` `길이(mm)` 두 열에 **0 을 쓰는지 1000·500 을 쓰는지**가 정해져 있지 않다.

**왜 임의로 정하면 안 되는가:**
발주처가 받는 표의 내용이 달라진다.
- `0 / 0` 이면 → 면적만 있는 결함으로 보인다 (누수흔적 실측과 일관)
- `1000 / 500` 이면 → 폭이 1000mm 인 균열처럼 읽힌다. **폭 열은 균열폭(mm 단위 소수)을 적는 열**이라
  1000 이 들어가면 열의 의미가 무너진다

**선택지:**
- **A. `0 · 0` 을 쓴다. 가로·세로는 앱 안에만 남긴다(재편집용)**
  → 실측 누수흔적 행과 일관된다. 폭 열의 의미가 지켜진다
- **B. `가로 · 세로` 를 그대로 쓴다**
  → 표에 원본 치수가 남는다. 대신 폭 열이 균열폭과 면적치수를 섞어 담는다
- **C. 열을 하나 더 만든다 (`가로` `세로`)**
  → 실측 표 양식을 벗어난다. 발주처 양식과 어긋날 위험

**추천:** **A안.** 실측 손상결함표 양식이 13열로 고정돼 있고 누수흔적 행이 `0 / 0 / 0.5` 라는
전례를 이미 보여준다.

**차단 여부:** **비차단.** A안으로 확정했다(스펙 §3-5-c `outputSize`).
가로·세로는 `areaWMm`/`areaHMm` 로 **저장은 해 두었으므로**, B 로 바꿔도 데이터 손실이 없다.
실제 표기는 Phase 4 출력에서 드러난다.

**사용자께 드리는 되물음 한 줄:**
**면적으로 기재하는 결함(망상균열·박리·누수흔적)의 폭·길이 열은 실제 보고서에서 비어 있는가, 0 인가, 치수가 들어가는가?**

---

## Q20 · plan-reviewer · 2026-08-23 · S3 씨앗 데이터

**막힌 지점:**
**기본 항목 세트(씨앗)의 도메인 정확성.** 설정이 비면 결함을 한 건도 입력할 수 없어
씨앗을 넣기로 확정했는데(스펙 §2-7), 내용의 상당 부분이 **안전진단 실무 지식**이다.

확인이 필요한 것:
1. **부재 17종의 구조체 / 비구조체 구분** — 특히 `파라펫`(비구조체로 둠), `외부벽체`(비구조체로 둠)
2. **결함유형 13종 중 실측 근거가 없는 2종** — `파손` · `백태`
3. **결함유형별 규모모드 기본값** — `접합부 이격`(폭×길이), `철근노출`(면적), `마감균열`(폭×길이)
4. **보수방안 5종 중 실측 근거가 없는 2종** — `단면복구공법` · `경과관찰`
5. **발생원인 3번 이름** — 실측 화면에 `재료분리, 시공불량` 으로 한 항목처럼 보인다. 맞는가

**왜 임의로 정하면 안 되는가:**
구조체/비구조체는 **손상결함표의 `구조체 유형` 열에 그대로 인쇄**되고,
안전진단에서 구조체 결함과 비구조체 결함은 **평가 등급에 미치는 영향이 다르다.**
잘못된 기본값을 깔면 사용자가 매번 고쳐야 하고, 놓치면 보고서가 틀린다.

**선택지:**
- **A. 스펙 §2-7 표 그대로 깐다**
  → 즉시 쓸 수 있다. 틀린 항목은 사용자가 설정 화면에서 고친다
- **B. 부재·결함유형만 깔고 구조체 구분은 전부 `비구조체` 로 통일**
  → 안전한 척하지만 실제로는 **모두 틀릴 확률이 더 높다**
- **C. 씨앗을 넣지 않는다**
  → 사용자가 17 + 13 + 4 + 5 개를 손으로 입력해야 한다. S4 테스트 자체가 불가능해진다

**추천:** **A안.** 씨앗은 **되돌릴 수 없는 결정이 아니다** —
전부 이름 변경·삭제·정렬·구조체 구분 변경이 가능하고, 기존 결함에는 영향이 없다(스펙 §2-5-e).

**차단 여부:** **비차단.** A안으로 `items/seed.ts` 에 넣는다. 표 한 곳을 고치면 된다.

**사용자께 드리는 되물음 한 줄:**
**스펙 §2-7 의 부재 17 · 결함유형 13 · 원인 4 · 보수방안 5 표를 한 번 훑어봐 주시고,
틀린 것과 반드시 있어야 하는데 빠진 것만 알려주세요.**

---

## Q21 · plan-reviewer · 2026-08-23 · S3 발생원인 코드번호

**막힌 지점:**
**발생원인 코드번호가 마스터 고정값인가, 보고서마다 다시 매기는 값인가.**

상세기획 §2-A 는 `CauseMaster | id, orgId, 이름, **코드번호**(손상결함표용)` 으로
**마스터 필드**라고 명시하고, 효과로 *"건조수축 1번은 어느 결함유형에서 골라도 항상 코드 1번"* 이라고 쓴다.

그런데 실측 손상결함표의 발생원인 열은 `2` `3` 처럼 **작은 숫자**이고,
젠트릭스 설정 화면(`결함정보 기본설정.jpg`)에는 **코드번호가 아예 보이지 않는다.**
실무 보고서 범례는 *"그 보고서에 등장한 원인만 1..n 으로 매기고 표 아래에 범례를 단다"* 인 경우가 흔하다.

**왜 임의로 정하면 안 되는가:**
**출력 재현성**과 직결된다.
- 마스터 고정이면 → 같은 데이터에서 항상 같은 숫자. 안전하다. 대신 원인이 30종이 되면 표에 `27` 이 찍힌다
- 보고서마다 재부여면 → **계산값**이므로 `ExportRun` 에 매핑을 스냅샷해야 한다(상세기획 §4-3).
  그러지 않으면 재다운로드가 다른 번호의 보고서를 만든다

지금 마스터에 고정 코드를 넣어 두었다가 나중에 재부여로 바꾸면 **두 체계가 공존**하게 된다.

**선택지:**
- **A. 마스터 고정 코드. 출력은 그대로 인쇄**
  → 재현성이 공짜로 확보된다. 원인이 많아지면 숫자가 커진다
- **B. 출력 시 재부여(그 보고서에 등장한 순서로 1..n) + `ExportRun` 스냅샷**
  → 실무 범례와 맞는다. Phase 4 에 스냅샷 저장이 반드시 따라와야 한다
- **C. 둘 다 — 마스터 코드를 두되 출력 옵션으로 재부여를 켤 수 있게**
  → 가장 유연하다. Phase 4 에서 옵션 하나가 늘어난다

**추천:** **A안으로 지금 확정하고, C 를 Phase 4 의 확장 경로로 남긴다.**
마스터 코드가 있으면 B·C 어느 쪽으로도 갈 수 있지만, 없으면 재부여밖에 못 한다.

**차단 여부:** **비차단.** A안으로 `CauseMaster.code`(정수, 문서 내 유일, `max+1` 자동 부여)를 확정했고,
설정 화면 컬럼3 에 배지로 노출하며 **사용자가 직접 고칠 수 있게** 했다.

**사용자께 드리는 되물음 한 줄:**
**손상결함표 아래 발생원인 범례는 회사 표준 번호를 쓰시는가, 그 보고서에 나온 것만 1번부터 다시 매기시는가?**

---

## Q22 · plan-reviewer · 2026-08-23 · S4 필드 범위

**막힌 지점:**
**사용자가 지정한 결함정보 6종에 `누수여부` · `위치보조` · `개소` 가 없다.**

이번 S4 범위로 주신 목록은
`구조유형 / 부재 / 진행여부 / 규모 / 발생원인 / 보수보강방안` 6종이다.

그런데 **실측 손상결함표의 열 구성**에는 셋 다 있다:
`NO · 위치 · 부재명 · 구조체 유형 · 결함의 유형 및 형상 · 폭 · 길이 · 면적 · **개소(EA)** ·
진행상황 · **누수여부** · 발생원인 추정 · 사진번호`

상세기획 §3-3 · §2-B 도 `누수여부 bool` · `위치보조 text` · `규모.개소 int` 를 모두 포함한다.

**왜 임의로 정하면 안 되는가:**
**나중에 붙이면 그 전에 입력한 결함이 전부 `null` 이 된다.**
현장에서 200건을 입력한 뒤 "누수여부 열이 비었다"를 발견하면 200건을 다시 열어야 한다.
반대로 지금 넣으면 폼이 그만큼 길어지고, 요구하지 않으신 항목으로 화면을 늘리는 것이 된다.

**선택지:**
- **A. 셋 다 넣는다** (`누수여부` O/X 2택 · `개소` −/+ 프리셋 1~10 · `위치보조` 자유입력)
  → 손상결함표가 바로 완성된다. 폼이 3섹션 길어진다
- **B. 필드만 만들고 폼에는 노출하지 않는다**
  → 화면이 짧다. 대신 **입력할 방법이 없어 항상 비어 있다.** 실질적으로 안 넣은 것과 같다
- **C. 이번엔 빼고 Phase 4 출력 만들 때 넣는다**
  → 그 전에 입력한 결함이 전부 null 이 된다

**추천:** **A안.** 셋 다 실측 표의 열이고, `개소` 는 이미 `Defect.countEa` 로 저장 중이며
`위치보조` 는 층 이름만으로 부족한 `위치` 열을 채우는 유일한 수단이다.

**차단 여부:** **비차단.** A안으로 스펙 §3-1·§3-2 에 확정했다. 폼에서 빼는 것은 언제든 쉽다.

**사용자께 드리는 되물음 한 줄:**
**현장에서 누수여부와 위치(거실·복도·계단실)를 그 자리에서 함께 찍으시는가, 사무실에서 정리하시는가?**

---

## Q23 · plan-reviewer · 2026-08-23 · S3 과업 설정 스냅샷

**막힌 지점:**
**S1·S2a 로 이미 만들어 둔 용역들에 항목설정 스냅샷을 언제 붙이는가.**

상세기획 §2-A 는 *"`Project` 생성 시 항목설정을 스냅샷으로 복사해 붙인다"* 고 한다.
그런데 **S1 이 이미 용역을 만들 수 있고**, 그렇게 만든 용역들은 생성 시점에 복사할 설정이 없었다.

**왜 임의로 정하면 안 되는가:**
- 일괄 마이그레이션을 돌리면 → IndexedDB 버전을 올려야 하고, 마이그레이션 경로가 하나 늘어난다.
  D5 가 정한 `migrations[]` 체계를 처음으로 실제 사용하는 사건이 되며, 실패 시 사용자 데이터가 걸린다
- 아무것도 안 하면 → 기존 용역에서 결함을 입력할 수 없다

**선택지:**
- **A. 지연 스냅샷 — 그 용역을 처음 열 때 그 시점의 전역 설정을 복사한다**
  → DB 버전 그대로. 코드 한 함수(`ensureProjectSettings`).
     단 "생성 시점"이 아니라 "여는 시점"의 설정을 받는다
- **B. 일괄 마이그레이션 — DB 를 열 때 모든 용역에 스냅샷을 만든다**
  → 시점이 명확하다. DB 버전 업 + 마이그레이션 함수 + 실패 처리가 필요하다
- **C. 기존 용역(샘플·테스트용)은 버리고 새로 만들게 안내한다**
  → 가장 깨끗하다. 사용자가 이미 넣어 둔 도면·결함이 있으면 손실

**추천:** **A안.** 지금은 전역 설정이 씨앗 한 벌뿐이라 "여는 시점"과 "생성 시점"의
실질 차이가 없다. 마이그레이션을 한 번 아끼는 값이 더 크다.

**차단 여부:** **비차단.** A안으로 스펙 §2-4 에 확정했다.

**사용자께 드리는 되물음 한 줄:**
**지금 브라우저에 남아 있는 테스트 용역을 계속 쓰실 생각인가, 아니면 초기화하고 새로 만드셔도 되는가?**

---

## Q24 · builder · 2026-08-23 · 새로 추가한 부재의 구조체 구분 기본값

**막힌 지점:**
설정 화면에서 `[새 부재]` 로 부재를 추가할 때 `구조체 / 비구조체` 중 무엇으로 시작할지가
스펙에 없다. 이 값은 손상결함표 **`구조체 유형` 열에 그대로 인쇄된다.**

**왜 임의로 정하면 안 되는가:**
잘못 지정된 채로 결함을 입력하면 그 값이 결함마다 스냅샷돼 보고서에 남는다.
설정을 나중에 고쳐도 **이미 입력한 결함은 바뀌지 않는다**(§2-5-e) — 조용히 틀린 값이 남는다.

**선택지:**
- **A. 구조체로 시작** → 현장에서 대부분의 신규 부재가 구조 부재라는 가정
- **B. 비구조체로 시작** → 씨앗 17종 중 10종이 비구조체다(다수결)
- **C. 추가할 때 라디오로 반드시 고르게 한다** → 입력 한 단계가 늘지만 틀릴 일이 없다

**추천:** 지금은 **A안**으로 넣었고 행 `⋯` 메뉴에서 한 번에 바꿀 수 있으며 배지로 항상 보인다.
현장 실무에서 신규 추가가 잦다면 **C안**이 안전하다.

**차단 여부:** **비차단.** A안으로 구현 완료 (ASSUMPTIONS G2).

---

## Q25 · builder · 2026-08-23 · `[기본 항목 세트 다시 불러오기]` 가 되살리는 범위

**막힌 지점:**
스펙 §2-7-e 는 *"병합(없는 것만 추가)"* 이라고만 한다.
**"없는 것"이 마스터만인지, 사용자가 정리해 뺀 링크(부재↔결함유형 조합)까지인지**가 갈린다.

**왜 임의로 정하면 안 되는가:**
링크까지 되살리면, `기둥` 에서 안 쓰는 결함유형 8개를 골라 뺀 사용자의 작업이 버튼 한 번에
전부 되돌아온다. 반대로 아예 안 깔면, 되살아난 부재가 **결함유형이 하나도 없는 빈 항목**이 된다.

**선택지:**
- **A. 새로 추가된 마스터가 낀 링크만 깐다** (구현한 방식)
  → 되살아난 부재는 결함유형 13종을 전부 받고, 기존 부재의 정리 결과는 유지된다
- **B. 씨앗 전조합을 다시 깐다** → 사용자의 링크 정리가 전부 무효
- **C. 마스터만 추가하고 링크는 손대지 않는다** → 되살아난 항목이 빈 채로 남아 쓸 수 없다

**추천:** **A안.**

**차단 여부:** **비차단.** A안으로 구현 완료 (ASSUMPTIONS G3). 단위테스트로 고정했다.

---

## Q26 · builder · 2026-08-23 · 캔버스에서 설정으로 들어가는 버튼의 자리

**막힌 지점:**
스펙 §2-5-a 는 진입점을 *"P5 캔버스 **하단 툴바** [설정]"* 이라고 적었는데,
현재 캔버스 화면에는 **하단 툴바가 없다.** 좌측 도구 팔레트와 상단바뿐이다.

**왜 임의로 정하면 안 되는가:**
새 툴바를 만들면 캔버스 작업 영역이 그만큼 줄고, S2b 가 붙일 결함정보 패널 레이아웃과 충돌한다.

**선택지:**
- **A. 상단바 왼쪽, `← 용역 구성` 옆에 `설정` 버튼** (구현한 방식) → 새 영역을 만들지 않는다
- **B. 하단 툴바를 새로 만든다** → 스펙 문구에 맞지만 캔버스 높이를 잃는다
- **C. 좌측 도구 팔레트 맨 아래** → 도구가 아닌 것이 도구 목록에 섞인다

**추천:** **A안.** 되돌아오는 경로(`?from=f/{floorId}`)는 스펙대로 동작한다.

**차단 여부:** **비차단.** A안으로 구현 완료 (ASSUMPTIONS G5).

---

## Q27 · builder · 2026-08-23 · 같은 이름의 항목을 두 개 두는 경우가 있는가

**막힌 지점:**
`[추가]` 는 스펙 §2-5-c 대로 같은 이름의 마스터를 두 번 만들지 않는다(함정 #4).
그런데 **이름 변경**에 대해서는 스펙에 규정이 없다.
지금은 `벽(비구조체)` 를 `기둥` 으로 고치는 것처럼 **다른 마스터와 같은 이름이 되는 변경을 거부**한다.

**왜 임의로 정하면 안 되는가:**
허용하면 같은 이름 마스터가 둘이 되어 함정 #4 가 뒷문으로 들어온다 —
발생원인이라면 **같은 이름에 코드번호가 2개**가 되어 보고서 범례가 깨진다.
반대로 거부가 과하면, 실무에 같은 이름을 의도적으로 쓰는 사례가 있을 때 막힌다.

**선택지:**
- **A. 거부한다** (구현한 방식) — `이미 같은 이름의 발생원인이 있습니다`
- **B. 경고만 하고 허용한다**

**추천:** **A안.** 특히 발생원인은 코드번호 유일성이 출력 재현성의 근거다.

**차단 여부:** **비차단.** A안으로 구현 완료 (ASSUMPTIONS G4).

**되물음:** 부재·결함유형·보수방안 중에 **같은 이름을 두 벌 두어야 하는 실무 사례**가 있습니까?

---

## Q28 · builder(AUTORUN) · 2026-08-23 · F5 범례 — 결함유형별 색을 지정할 것인가

**막힌 지점:**
F5-2(범례) 스펙은 범례 행을 `{ color: 결함유형 색, sym: '', desc: 결함유형 이름 }` 으로
요구한다 — 결함유형(13종)마다 고유 색이 있다고 전제한다. 그런데 이 프로젝트의 색 체계
(`docs/OnSpect_디자인시스템.md` §3)는 지금까지 **상태색**(빨강=현회차 · 보라=전회차 ·
회색=보수완료) 하나뿐이고, "결함유형별 색"이라는 두 번째 색 축은 데이터 모델
(`DefectTypeMaster`)에도, 디자인시스템 문서에도 정의돼 있지 않다.

**왜 임의로 정하면 안 되는가:**
13종에 지금 색을 배정하면(예: 균열=파랑, 박리=주황…) 상태색과 안 겹치는 팔레트를
새로 만들어야 하는데, 이건 스펙을 그대로 옮기는 일이 아니라 **새 디자인 결정**이다.
나중에 실제 팔레트가 정해지면 이미 저장·출력된 범례 색을 전부 다시 칠해야 한다.

**선택지:**
- **A. 결함유형별 색을 지금 배정한다** — 팔레트를 새로 만들어야 하고, 상태색과
  혼동되지 않는 13색을 골라야 한다 (예: 결함 표기 자체는 여전히 상태색이므로,
  범례의 색은 "이 도면에 어떤 유형이 있는지"만 보여주는 보조 정보가 된다)
- **B. 색을 쓰지 않고 문자 기호로만 구분한다** — 발생원인 코드번호 배지
  (`①②③…`, `circledNumber()`, S3 에서 이미 만들어 둔 함수)와 같은 방식을 결함유형에도
  적용해 `① 수직균열`, `② 망상균열` 처럼 번호+이름으로 범례를 채운다. 색 배정 문제
  자체가 사라진다

**추천:** **B안.** 이미 만들어 둔 `circledNumber()` 를 재사용할 수 있고, 상태색
체계를 어지럽히지 않는다. 다만 실측 젠트릭스 화면(원본 Numdraw)이 색 샘플 방식을
썼다면 사용자 기대와 다를 수 있다.

**차단 여부:** **F5-2 만 차단.** F5-1(도곽)·F5-3(도면 크기)·F5-4(도면 영역)는 이
질문과 무관하게 착수 가능하다(F5-4 는 이미 완료됨 — F1 작업 중 `Drawing.imgLayout` 로
해결).

---

## Q29 · builder · 2026-08-23 · F5-3 도면 크기 조절 — 좌표를 정말 그대로 둬도 되는가

**막힌 지점 (지시를 따랐지만 근거가 흔들리는 곳):**
지시서는 못박았다 — *"Numdraw 가 배율 변경 시 넘버링 좌표를 같이 옮기는 코드는 이식하지 마라.
우리는 좌표가 0~1 정규화라 배율이 바뀌어도 상대 위치가 유지된다."*
**그대로 구현했다.** 다만 그 근거가 F1 이후에는 절반만 맞다:

- F1 **전** 도면 → 정규화의 분모가 **도면 그림 자체**였다. 배율이 바뀌어도 그림과
  표기가 같이 늘어난다. 지시의 설명이 정확히 맞는다.
- F1 **후** 도면 → 정규화의 분모가 **A4 지면**이다. 배율을 키우면 **그림만 커지고
  표기는 지면 기준 제자리**에 남는다. 결과적으로 표기가 도면의 다른 부분을 가리킨다.

**왜 임의로 정하면 안 되는가:**
"표기를 함께 옮긴다"로 바꾸면 그것이야말로 실사용자 좌표를 건드리는 조작이고,
지시서가 명시적으로 금지한 코드다. 반대로 지금처럼 두면 결함이 이미 찍힌 도면의
배율을 바꿨을 때 표기가 그림과 어긋나 보인다.

**지금 구현한 것 (안전 쪽):**
좌표를 손대지 않았다. 대신 결함이 있는 도면에서 배율을 바꾸려 하면 다이얼로그가
*"결함 N건이 있습니다. 표기 위치는 지면 기준으로 유지되고 그림만 움직입니다.
어긋나 보이면 배율을 되돌리세요"* 라고 알린다. 배율 되돌리기는 완전 무손실이다
(좌표를 안 건드렸으므로).

**선택지:**
- **A. 지금 그대로 (권장)** — 배율은 "지면 안에서 그림 크기를 정하는" 설정으로 쓰고,
  결함을 찍기 **전**에 정하는 것을 전제한다. 좌표는 절대 안전하다
- **B. F1 정규화된 도면에 한해 배율 변경 시 좌표도 같은 비율로 이동** — 그림과 표기가
  항상 붙어 다닌다. 대신 배율을 여러 번 왕복하면 반올림(6자리)이 누적된다
- **C. 결함이 1건이라도 있으면 배율 변경을 아예 막는다** — 가장 단순하고 사고가 없다

**추천:** A. 실제로 배율을 조절할 시점은 도면을 등록한 직후이지 조사 중이 아니다.
다만 **사용자가 조사 중에도 배율을 바꾸고 싶다면 B 또는 C 로 바꿔야 한다.**

**차단 여부:** **비차단.** A 로 구현·커밋했다 (`ASSUMPTIONS.md` H1).

---

## Q30 · builder · 2026-08-23 · 필기 메모로 바꾼 뒤 옛 텍스트 메모를 계속 지원할 것인가

**지금 구현한 것:**
`Memo.paths` 를 추가하고 메모 도구를 손글씨로 바꿨다. **새 메모는 전부 손글씨**다.
이미 저장된 텍스트 메모(`paths === null`)는 **읽는 시점에 갈라** 예전 그대로
노란 상자 + 더블클릭 글 편집을 유지한다. DB 버전은 1 그대로다.

**결정이 필요한 지점:**
텍스트 메모를 **새로 만들 방법이 이제 없다.** 지금은 그게 사용자 요구
(*"메모→자유그리기처럼 동작(필기메모)"*)에 정확히 맞지만, 실무에서 긴 문장 메모
(예: "3층 좌측 계단실 누수 — 관리사무소 확인 요청")를 손글씨로 쓰기는 불편하다.

**선택지:**
- **A. 지금 그대로 — 메모는 손글씨 전용** (요구 그대로). 옛 텍스트 메모는 읽기·편집만
- **B. 도구를 둘로 쪼갠다** — `필기메모` + `글자메모`. 팔레트 버튼이 하나 늘어난다
- **C. 손글씨 메모에 선택적 텍스트 캡션**을 붙인다 (획 + 짧은 글)

**추천:** A 로 두고, 실제로 써 본 뒤 불편하면 B. 데이터 구조는 이미 B·C 를 받는다
(`text` 와 `paths` 가 한 레코드에 공존할 수 있게 만들어 뒀다).

**차단 여부:** **비차단.** A 로 구현·커밋했다 (`ASSUMPTIONS.md` H4).

---

## Q31 · builder · 2026-08-23 · [A4로 맞추기] 이후 썸네일이 옛 비율로 남는다

**지금 구현한 것:**
`[A4로 맞추기]` 는 **Blob 을 하나도 건드리지 않는다.** 저장된 렌더 래스터·썸네일은
옛 비율 그대로 두고, 화면에는 원본을 A4 로 다시 합성한 결과(런타임 캐시)를 보여준다.
덕분에 되돌리기가 레코드 복원만으로 끝나고 저장 용량도 늘지 않는다.

**남는 것:** 용역 구성 화면의 **썸네일**은 옛 비율(A4 여백 없음)로 계속 보인다.
그림 자체는 맞고 비율만 다르다.

**선택지:**
- **A. 그대로 둔다** — 썸네일은 "어떤 도면인지" 알아보는 용도라 비율 차이가 문제되지 않는다
- **B. 재정규화 시 썸네일만 새로 만든다** — 되돌리기 때 옛 썸네일을 되살려야 하므로
  Blob 두 개의 수명 관리가 생긴다(고아 Blob 위험)
- **C. 썸네일도 런타임 합성** — 목록 화면에서 도면 수만큼 합성이 돌아 느려진다

**추천:** A. B 는 "되돌리기 가능"이라는 이번 요구와 상충하는 복잡도를 만든다.

**차단 여부:** **비차단.** A 로 구현·커밋했다 (`ASSUMPTIONS.md` H5).

---

## Q32 · plan-reviewer · 2026-08-25 · Phase 4 — PDF 를 어떻게 만들 것인가

**막힌 지점:** 기획서 §6 은 산출물을 `PDF/엑셀` 로 적었다. 브라우저에서 PDF 를 만드는 길은 둘뿐이다.

**왜 임의로 정하면 안 되는가:** 사용자가 버튼을 누른 뒤 보게 되는 동작이 완전히 달라진다.
한쪽은 파일이 바로 떨어지고, 한쪽은 인쇄 대화상자가 뜬다.

**선택지:**
  A. **브라우저 인쇄**(`window.print()` + CSS `@page`) → 의존성 0, **한글이 시스템 폰트로 완벽히 나옴**,
     페이지네이션을 CSS 가 처리. 대신 사용자가 인쇄 대화상자에서 `PDF로 저장` 을 한 번 더 골라야 한다
  B. **PDF 라이브러리**(`jspdf` · `pdf-lib`) → 버튼 한 번에 파일이 떨어진다. 대신 한글을 찍으려면
     TTF 를 임베드해야 하고, 서브셋 없이 Noto Sans KR 을 넣으면 **번들에 4~8MB** 가 붙는다.
     서브셋 툴체인을 빌드에 넣는 것은 이번 범위를 크게 넘는다
  C. 서버 렌더 → 이 제품은 지금 서버가 없다(Phase 5)

**추천:** **A.** 한글 폰트 임베딩은 이 프로젝트에서 가장 비싸고 가장 조용히 깨지는 작업이다
(폰트가 빠진 자리에 네모가 찍히는데 개발자 화면에서는 캐시 때문에 잘 보인다).
A 는 그 위험을 통째로 없앤다. 버튼 라벨을 `[PDF로 인쇄]` 로 하고 안내 문구를 상시 노출한다.

**차단 여부:** **비차단.** A 로 진행한다 (`ASSUMPTIONS.md` K1).

---

## Q33 · plan-reviewer · 2026-08-25 · S5 — 사진 자르기·주석을 1차에 넣을 것인가

**막힌 지점:** 상세기획 §2-C 플로우에 `[보정] 자르기·회전  [주석] 그리기·화살표` 가 있다.
범위 문서가 "난이도가 높으면 1차 범위를 좁힐 수 있는지 판단하라"고 명시적으로 위임했다.

**왜 임의로 정하면 안 되는가:** 기획서에 적힌 기능을 빼는 판단이다. "만들어졌다"의 기준이 달라진다.

**선택지:**
  A. **회전만 1차** → `edits.rotate` 필드 하나. 세로 사진이 눕는 문제(출력물 품질에 직결)는 해결된다.
     자르기·주석은 필드만 예약하고 버튼은 `준비 중`
  B. 회전 + 자르기 → 드래그 사각형 UI + 확대 + 비율고정 + 되돌리기가 있어야 쓸 만하다.
     반쪽으로 내면 잘못 자른 사진을 사용자가 복구하지 못한다
  C. 전부 → 주석은 사실상 두 번째 캔버스 앱이다(벡터 저장 · 편집 · 출력 렌더 · Undo).
     이번 라운드가 5개 모듈짜리라 Phase 4 를 잡아먹는다

**추천:** **A.** `edits.crop` · `annotations` 필드를 지금 확정해 두므로 나중에 UI 만 붙이면 되고,
데이터 마이그레이션이 0 이다. 사진이 없으면 사진첩이 아예 안 만들어지므로 **등록·대표지정이 우선**이다.

**차단 여부:** **비차단.** A 로 진행한다 (`ASSUMPTIONS.md` K3).

---

## Q34 · plan-reviewer · 2026-08-25 · 번호모드가 `층별리셋`일 때 사진번호도 리셋되는가

**막힌 지점:** 상세기획 §4-3 은 `2. 번호모드에 따라 NO 부여` / `3. 사진번호는 별도 카운터` 로만 적혀
있고, 사진번호에 번호모드가 적용되는지가 없다.

**왜 임의로 정하면 안 되는가:** 손상결함표의 `사진번호` 열과 사진첩의 `사진 N` 이 어긋나면
보고서를 보는 사람이 사진을 못 찾는다. 조용히 틀리는 종류의 오류다.

**선택지:**
  A. **사진번호도 번호모드를 따른다** (층별리셋이면 층마다 1부터) → 층별로 구분된 보고서에서
     결함번호와 사진번호가 같은 구조를 갖는다. 대신 `사진 1` 이 층 수만큼 생긴다
  B. 사진번호는 항상 전체 연속 → 사진첩이 통짜라 사진번호가 유일해진다.
     대신 층별리셋 보고서에서 `NO 1 / 사진 47` 같은 조합이 나온다

**추천:** **A.** 층별리셋을 고른다는 것은 보고서를 층 단위로 나눠 편철한다는 뜻이고,
그 안에서 두 번호가 다른 규칙을 따르면 대조가 불가능해진다.

**차단 여부:** **비차단.** A 로 진행한다 (`ASSUMPTIONS.md` K6).

---

## Q35 · plan-reviewer · 2026-08-25 · 결함 리스트(A4 세로)는 손상결함표와 무엇이 다른가

**막힌 지점:** 기획서 §6 은 `리스트 = 전체 결함 표 형태 나열, A4 세로` 뿐이고,
상세기획 §6 표에도 형식만 있다. **손상결함표(13열)는 A4 세로 폭에 물리적으로 안 들어간다.**

**왜 임의로 정하면 안 되는가:** 어떤 열을 뺄지가 이 산출물의 용도를 결정한다.

**선택지:**
  A. **9열 축약** — `NO · 위치 · 부재 · 결함유형 · 폭 · 길이 · 면적 · 개소 · 사진번호`.
     뺀 것: 구조체유형 · 진행상황 · 누수여부 · 발생원인.
     성격 = **현장 대조·검수용 빠른 목록**(손상결함표가 제출용 정식 서식)
  B. 13열 전부 + 글자를 6pt 로 축소 → 읽을 수 없다
  C. 13열 전부 + A4 가로 → 기획서가 "A4 세로"로 못박았다

**추천:** **A.** 뺀 4열은 전부 손상결함표에서 확인하는 항목이라 중복이다.
`DEFECT_LIST_COLUMNS` 배열 한 줄이라 실물 서식이 오면 즉시 조정된다.

**차단 여부:** **비차단.** A 로 진행한다 (`ASSUMPTIONS.md` K9).

---

## Q36 · builder · 2026-08-25 · 엑셀 인쇄에서 머리말 5행이 페이지마다 반복되지 않는다

**막힌 지점:** 스펙 §3-4 는 *"엑셀 인쇄 반복 행 = 1~5행 (`<계 속>` 이 페이지마다 반복된다)"* 를
요구한다. 채택한 `write-excel-file@4` 는 **`printTitles`(반복 행) 설정을 노출하지 않는다.**
`sticky rows` 는 화면 고정일 뿐 인쇄 반복이 아니다.

**왜 임의로 정하면 안 되는가:** 반복 행은 **제출 서식의 요구사항**일 수 있다.
발주처가 "페이지마다 `<계 속>` 이 있어야 한다"고 하면 라이브러리를 바꿔야 한다.

**선택지:**
  A. **지금대로 둔다** — 머리말 5행은 **첫 페이지에만** 나온다.
     사용자가 엑셀에서 `페이지 레이아웃 → 인쇄 제목 → 반복할 행 = $1:$5` 를 한 번 지정하면 된다
     (파일당 1회, 30초). 출력 화면에 이 안내를 적어 둔다
  B. `exceljs` 로 갈아탄다 — `worksheet.pageSetup.printTitlesRow` 를 지원한다.
     대신 Vite 에서 `buffer`/`stream` 폴리필이 필요할 수 있고 번들이 훨씬 크다
  C. 손상결함표를 인쇄 뷰(HTML)로도 낸다 — `thead` 가 페이지마다 자동 반복된다.
     대신 엑셀과 PDF 두 벌을 유지해야 한다

**추천:** **A.** 라이브러리 경계가 `xlsx.ts` 한 파일이라 B 로 갈아타는 비용이 파일 1개다.
실물 서식을 받기 전에 번들을 키울 이유가 없다. `DAMAGE_REPEAT_ROWS = 5` 상수를
`damageTableFile.ts` 에 남겨 두어 B 로 갈 때 쓸 자리를 표시했다.

**차단 여부:** **비차단.** A 로 진행했다 (`ASSUMPTIONS.md` M2).

---

## Q37 · builder · 2026-08-25 · 사진첩은 파일로 내려받을 수 없다

**막힌 지점:** 스펙 §3-6 은 사진첩의 **배치**만 정의하고 **파일 형식**을 정하지 않았다.
§4-9 로 보면 PDF = 브라우저 인쇄다. 그런데 §4-1 의 `[생성]` 은 산출물 4종을
체크박스로 함께 고르게 돼 있어, 사진첩만 골랐을 때 **아무 파일도 안 나온다.**

**왜 임의로 정하면 안 되는가:** `[생성]` 을 눌렀는데 아무 일도 안 일어나는 것처럼 보이면
사용자는 고장으로 읽는다.

**선택지:**
  A. **`[생성]` 은 파일로 낼 수 있는 것만 내려받고**(손상결함표·결함리스트 엑셀, 조사위치도 PNG),
     사진첩은 이력 줄의 `[사진첩 PDF]` 버튼으로 인쇄 뷰를 연다.
     `[생성]` 이 끝나면 토스트로 그 사실을 알린다
  B. `[생성]` 이 사진첩 인쇄 탭을 자동으로 연다 → **팝업 차단에 걸린다**
     (사용자 제스처와 `await` 사이에 시간이 벌어진다)
  C. 사진첩을 여러 장의 PNG 로 내려받는다 → 6장 그리드를 캔버스로 다시 그려야 하고,
     인쇄 뷰와 두 벌이 된다

**추천:** **A.** B 는 브라우저가 막고, C 는 같은 화면을 두 번 만든다.

**차단 여부:** **비차단.** A 로 진행했다 (`ASSUMPTIONS.md` M3).

---

## Q38 · plan-reviewer · 2026-08-25 · Phase 5 — 도면 확대 상태에서 정밀 표기 방식 (상세기획 §7 Q6 확정) ⭐

**막힌 지점:** §7 미결 Q6 *"도면 확대 상태에서 정밀 표기 방식 (핀치줌 + 크로스헤어? 롱프레스?)"* 이
Phase 3부터 계속 미결로 남아 있다. 태블릿 현장 입력의 **가장 자주 쓰는 동작**이라 목업 전에 확정해야 한다.

**왜 임의로 정하면 안 되는가:** 사용자가 현장에서 하루에 수백 번 반복할 동작이다.
잘못 정하면 "쓸 수 없는 앱"이 되고, 나중에 바꾸면 터치 어댑터·조준 오버레이·툴바를 전부 다시 만든다.

**선택지:**
  A. **탭 즉시 찍기(기본) + `⊕조준` 토글 시 화면 중앙 고정 크로스헤어 + `[여기]` 확정**
     → 손가락이 목표를 안 가린다. **코어 변경 0**(화면 중앙 좌표로 POINTER_DOWN/UP 합성).
       젠트릭스도 "터치 중 가이드라인 표시 → 떼면 생성" 계열. 조작이 2단계(맞추기→확정)로 늘어난다
  B. 롱프레스 → 손가락 위 **루페(확대경)** → 드래그 미세조정 → 뗌 확정
     → iOS 텍스트 커서처럼 익숙하다. **롱프레스가 컨텍스트 메뉴와 충돌**하고, 루페를 추가 렌더해야 하며,
       확대율이 올라갈수록 손 떨림이 그대로 확대된다
  C. 누르는 동안 손가락 **위쪽 40pt 오프셋**에 십자선
     → 한 동작으로 끝난다. **화면 상단 가장자리 결함을 못 찍고**, 왼손/오른손잡이별로 방향을 바꿔야 한다
  D. 별도 UI 없이 확대 배율로 해결
     → 가장 단순하지만, 이것으로 해결이 안 돼서 Q6가 미결로 남았다

**추천:** **A.** 코어를 안 건드리고, 손가락이 목표를 안 가리며, 롱프레스를 컨텍스트 메뉴에 남겨둘 수 있다.
방향·영역·자유그리기는 드래그 생성이라 조준을 쓰지 않고, 대신 **선택 후 `↑↓←→` 넛지 패드(1탭=1px)** 로 미세조정한다.

**차단 여부:** **비차단.** A 를 기본값으로 목업을 그린다 (`ASSUMPTIONS.md` N5).
다만 오래 미결이던 항목이라 **최종 확인을 부탁드린다.**

---

## Q39 · plan-reviewer · 2026-08-25 · Phase 5 — 태블릿 **가로** 화면은 사이드시트인가 바텀시트인가

**막힌 지점:** 상세기획 §2-B 는 `PC = 우측 사이드시트 / 모바일 = 바텀시트` 두 가지만 정의한다.
태블릿 가로(예: 1194×834)가 어디에 속하는지 두 가지로 읽힌다.

**왜 임의로 정하면 안 되는가:** 결함 입력 시트는 현장에서 가장 오래 보는 화면이다.
잘못 고르면 도면이 절반 가려지거나 캔버스가 너무 좁아진다. 목업 12장 중 6장이 여기 걸린다.

**선택지:**
  A. **가로 = 우측 사이드시트(400pt) / 세로 = 바텀시트 3단**
     → 가로는 세로 높이가 짧아(834pt) 바텀시트 55%면 도면 하단이 통째로 가려진다.
       사이드시트를 붙여도 캔버스가 794×834 로 넓다. 대신 **구현이 두 벌**이 된다
  B. 방향과 무관하게 항상 바텀시트
     → 구현 한 벌. 가로에서 도면 하단이 가려진다
  C. 방향과 무관하게 항상 사이드시트
     → 구현 한 벌. 세로(834pt 폭)에서 캔버스가 434pt 로 쪼그라들어 도면을 못 본다

**추천:** **A.** `ui/defectForm/*` 내용물은 어차피 같고 **껍데기(셸)만 두 벌**이라 추가 비용이 크지 않다.

**차단 여부:** **비차단.** A 로 목업을 그린다 (`ASSUMPTIONS.md` N6).

---

## Q40 · plan-reviewer · 2026-08-25 · Phase 5 — **서버·동기화 프로토콜이 설계돼 있지 않다** 🔴

**막힌 지점:** G3(오프라인 준비)는 *"사무실 PC에서 만든 과업이 현장 태블릿에 있어야 한다"* 가 전제인데,
**그 사이를 잇는 서버와 동기화 규칙이 어느 문서에도 없다.**
지금 있는 것은 `RecordBase { updatedAt, deviceId, createdBy }` 필드 예약("Phase 5 LWW 병합의 기준")뿐이다.

정해지지 않은 것: 서버 존재 여부 · 인증/조직 · 업로드 큐 · 충돌 해결(같은 결함을 PC와 태블릿이 동시 수정) ·
사진 대용량 업로드 · 삭제 전파(tombstone) · `revision` 발급 주체.

**왜 임의로 정하면 안 되는가:** 동기화 규칙을 잘못 잡으면 **현장 데이터가 조용히 사라진다.**
되돌리기 비용이 이 프로젝트에서 가장 큰 축이고, 데이터 모델(`Defect`·`Photo`) 자체가 바뀔 수 있다.

**선택지:**
  A. **서버 없이 간다 — PC ↔ 태블릿 파일 내보내기/가져오기**(`.onspect` 번들 zip)
     → 구현이 작고 오늘 시작할 수 있다. 팀 동시작업 불가, USB/클라우드 드라이브 수동 왕복
  B. **최소 서버 — 과업 단위 스냅샷 push/pull**(충돌 시 "누구 것을 쓸지" 사용자가 통째로 선택)
     → 중간 비용. 결함 단위 병합을 안 해도 되어 위험이 크게 준다. 두 사람이 같은 층을 동시에 하면 한쪽이 진다
  C. **레코드 단위 LWW 동기화**(`updatedAt` 기준) + 사진 별도 큐
     → 기획 의도에 가장 가깝다(필드가 이미 예약돼 있다). 설계·구현·테스트 비용이 가장 크다

**추천:** **판단 근거 없음** — 이건 제품 판매 형태(요금제 Q9·조직 개념)와 직결된 사업 결정이다.
다만 **G3 화면(M-01~M-03)은 세 안 모두에서 같은 모습**이라 목업은 지금 그려도 안전하다.

**차단 여부:** 🔴 **차단** — 트랙 B(오프라인 준비 구현)·트랙 F(사진 동기화)를 시작할 수 없다.
트랙 A(코어 터치 지원)·C(위치 찾기)·목업은 차단되지 않는다.

---

## Q41 · plan-reviewer · 2026-08-25 · Phase 5 — 태블릿 런타임이 React Native 인가 태블릿 브라우저(PWA) 인가 🔴

**막힌 지점:** `NEXT.md` 는 "모바일(React Native)", D1은 "canvas-core는 플랫폼 독립으로 작성하여 이후 RN이 재사용"
이라고 적혀 있으나, **RN에서 `renderModel` 이 뱉는 `DrawOp[]` 를 그릴 렌더러가 없다.**
지금 렌더러는 웹 Canvas2D 전용이다.

**왜 임의로 정하면 안 되는가:** 카메라 API · 저장소 · 배포 방식 · UI 컴포넌트가 전부 갈린다.
잘못 고르면 렌더러를 한 벌 더 만들거나(RN+Skia), 현장 사진이 브라우저에 의해 삭제된다(PWA).

**선택지:**
  A. **React Native + Skia**
     → 카메라·파일시스템·백그라운드 동기화가 안전하다. 스토어 배포. **`DrawOp` 렌더러를 새로 작성**해야 하고
       `ui/defectForm/*`(현재 `.tsx` 웹)도 다시 만든다. 앱 심사·배포 파이프라인이 새로 생긴다
  B. **태블릿 브라우저 반응형 웹 + PWA 설치**
     → **렌더러·폼·저장 계층 재작성 0.** 오늘 있는 코드가 그대로 돈다. 촬영은 `<input capture>`/getUserMedia 로 된다.
       ⚠️ **iPadOS Safari의 IndexedDB는 축출 가능한 저장소**라 현장 사진 수 GB가 사라질 수 있다
       (`navigator.storage.persist()` 요청이 필수이고 보장되지 않는다). 백그라운드 동기화도 제한적
  C. **B 먼저 실측 → 한계가 확인되면 A**
     → 재사용률이 가장 높고 위험이 늦게 온다. 나중에 A로 갈 때 UI 한 벌을 버린다

**추천:** **C.** D1(캔버스 코어 플랫폼 독립)을 **뒤집지 않는다** — D1은 "RN이 재사용할 수 있게 짜라"이지
"모바일은 RN이어야 한다"가 아니다. 다만 **Android 태블릿 위주면 B의 저장소 위험이 거의 없고,
iPad 위주면 위험이 크다** → **현장에서 실제로 쓸 기기가 무엇인지**를 함께 알려주시면 판단이 확정된다.

**차단 여부:** 🔴 **차단(구현)** / **비차단(목업)** — 목업은 화면 구성이 같고 컴포넌트 질감만 달라진다.

---

## Q42 · plan-reviewer · 2026-08-25 · Phase 5 — 전회차 결함을 촬영한 뒤 그 사진을 지우면 상태가 되돌아가는가

**막힌 지점:** 상세기획 §2-D 는 *"촬영하는 순간 status = CURRENT, 보라 → 빨강"* 만 정하고,
**그 사진을 지웠을 때**를 정하지 않았다. 현장에서 흔들린 사진을 지우는 일은 자주 일어난다.

**왜 임의로 정하면 안 되는가:** `전회차 3건 / 현회차 14건` 카운트와 **출력 포함 여부**가 여기서 갈린다
(PREV_PENDING = 출력 제외 / CURRENT = 포함). 사진 없는 결함이 현회차로 남아 보고서에 빈 사진번호로 나갈 수 있다.

**선택지:**
  A. **마지막 사진을 지우면 `PREV_PENDING` 으로 되돌린다**
     → 카운트가 항상 정확하다. 사용자가 값을 손으로 고쳤는데도 보라로 돌아가면 그 수정이 묻힌 것처럼 보인다
  B. **한 번 CURRENT 가 되면 되돌아가지 않는다.** 되돌리려면 명시적으로 `[전회차로 되돌리기]`
     → 사용자 의도가 분명하다. 사진 없는 CURRENT 가 생겨 출력 경고(D3 미완성 경고)에 걸린다
  C. **사진이 지워지고 값 수정도 없었을 때만** PREV_PENDING 으로 되돌린다 (조건부)
     → 가장 "맞는" 동작이지만, 사용자가 왜 되돌아갔는지 예측하기 어렵다

**추천:** **B.** 이미 D3에서 *"자동 제외하지 않고 경고만"* 이라는 방침을 세웠다.
자동으로 상태를 되돌리는 것보다 **사용자가 보고 판단하게** 하는 쪽이 이 프로젝트의 일관된 원칙이다.

**차단 여부:** **비차단.** B 로 가정한다 (`ASSUMPTIONS.md` N8).

---

## Q43 · plan-reviewer · 2026-08-25 · 사진 마무리 — `대표 외 사진 포함` 의 번호를 어떻게 붙이는가

**막힌 지점:** K7 이 이 옵션을 비활성으로 둔 이유는 *"부번이 생겨 번호체계가 바뀝니다"* 뿐이고,
**부번 규칙 자체는 어디에도 정해져 있지 않다.** 결함 12번에 사진이 3장 있을 때
사진첩에 `12 · 12-1 · 12-2` 로 나갈지, `12 · 13 · 14` 로 나갈지가 갈린다.

**왜 임의로 정하면 안 되는가:** 후자를 고르면 `assignNumbers()` 가 사진마다 번호를 세야 하고,
`ExportRun.mapping` 이 `Record<defectId, {no, photoNo}>` — **결함 1건 : 번호 1개** 구조라
표현할 수 없게 된다. 재현성 스냅샷이 통째로 깨지고 `[같은 번호로 다시 받기]` 가 무너진다.
발주처에 이미 낸 보고서와 번호가 달라지는 사고다.

**선택지:**
  A. **부번 `12-1` · `12-2`** — 대표만 정수 번호를 갖고 나머지는 파생 부번.
     `numbering.ts` · `ExportRun` **무변경**. 손상결함표 `사진번호` 열은 `12` 하나만 유지
     → 재현성 무손상. 단 **부번 순서는 스냅샷에 없어서**, 출력 뒤 사진 순서를 바꾸고
       재출력하면 `-1`/`-2` 가 서로 바뀔 수 있다(정수 번호는 그대로)
  B. **사진마다 정수 번호** `12 · 13 · 14` — 표는 깔끔하지만 `ExportRun` 스키마 변경 +
     기존 이력 전부 무효. 손상결함표 한 칸에 `12, 13, 14` 를 넣어야 해 열 폭이 무너진다
  C. **대표 외 사진은 사진첩 맨 뒤에 부록으로 몰아서** 번호 없이 싣는다
     → 어떤 결함의 사진인지 표에서 찾아갈 길이 없어진다

**추천:** **A.** 유일하게 `assignNumbers()` 와 `ExportRun` 을 안 건드리는 안이고,
"사진번호 = 대표사진 기준"(상세기획 §2-C)이라는 원문 규칙도 그대로 산다.
부번 순서가 흔들릴 수 있는 점은 **이미 존재하는 성질과 같은 급**이다 —
지금도 대표를 바꾸면 사진첩에 실리는 사진 자체가 바뀐다(`buildPhotoBook` 이 인쇄 시점에
`primaryOf()` 를 다시 부른다).

**차단 여부:** **비차단.** A 로 확정하고 진행한다 (`ASSUMPTIONS.md` R9 ·
`29_plan-reviewer_spec_PhotoPolish.md` §2-8). 되돌리는 비용: A→C 는 낮음, A→B 는 높음.

---

## Q44 · plan-reviewer · 2026-08-25 · 조사구분을 **직전 입력에서 이어받을 것인가** (D9 표 보강)

**막힌 지점:** `surveyKind`(외관조사/상세조사)를 결함정보 폼에 노출하는 순간,
"다음 결함이 이 값을 이어받는가"를 정해야 한다.
D9(사용자 확정) 표에는 **`surveyKind` 가 아예 없다.** 지금 코드가 `false`(이어받지 않음)인 것은
builder 가정 J4 이고, 그 근거가 *"폼에 노출되지 않고 어느 화면도 이 값을 바꾸지 않는다"* 라
**이번 작업으로 근거가 소멸한다.**

**왜 임의로 정하면 안 되는가:** 사용자가 보는 동작이고, **틀리면 조용히 틀린다.**
이어받지 않으면 상세조사 중인 조사자가 결함을 찍을 때마다 `외관조사` 로 되돌아가고,
한 번 잊으면 그 결함이 **외관조사로 저장돼 상세조사 출력에서 빠진다.**
반대로 이어받으면, 한 번 `상세조사` 로 바꾼 뒤 잊은 사용자의 이후 결함이 전부 상세조사가 된다.

**선택지:**
  A. **이어받는다(`true`)** → 조사 세션이 통째로 한 구분에 머무는 실제 작업 방식과 맞는다.
     D9 원칙("분류·판정은 이어받고 측정값·개별정보는 새로 받는다")에 대입하면 명백히 분류 쪽.
     표가 `이어받음 14 · 새로 받음 8` 이 된다
  B. **이어받지 않는다(`false`, 현행 유지)** → 매번 `외관조사` 로 시작. 상세조사 작업이 고통스럽고,
     잊었을 때 **기본값 쪽으로** 조용히 틀린다

**추천:** **A.** 두 실패 모드를 비교하면 A 쪽이 낫다 —
A 에서 잘못되면 **사용자가 직접 고른 값**이 유지된 것이고 폼에 그 값이 보인다.
B 에서 잘못되면 **아무도 고르지 않은 기본값**이 들어가고 화면상 정상으로 보인다.

**차단 여부:** **비차단.** A 로 확정하고 진행한다 (`ASSUMPTIONS.md` R10 ·
`29_plan-reviewer_spec_PhotoPolish.md` §2-7). 되돌리는 비용: `defectAttrs.ts` 1줄 + 테스트 1곳.
**D9 를 뒤집는 것이 아니다** — D9 표에 없던 필드를 같은 원칙으로 채우는 것이다.

---

# ── 사용자 수정사항 2파일 검토 (2026-08-28) · Q45~Q52 ──

> 근거 문서: `_workspace/30_plan-reviewer_spec_UserFeedback0828.md`
> **재상신: Q42(전회차 → 현회차 전환 시 사진 삭제 처리)** — 사용자 파일2-④가 이 항목을 요청했다.
> Q42 는 아직 답이 없다. 파일2-④ 착수 전에 반드시 답이 필요하다.

## Q45 · plan-reviewer · 2026-08-28 · 파일1-①⑦ — "필기"가 어느 도구이며, 점선상자를 없애면 무엇으로 선택·이동하는가 🔴

**막힌 지점:** 원문 *"필기기능은 필기+지우개 기능 필요 / 필기시 영역표시 x / 필요시 선택도구로
필기된 구간 영역 선택하여 메모 이동"* 이 세 가지를 동시에 못 정한다.

1. **"필기"가 어느 도구인가.** 툴 팔레트에는 비슷한 것이 **두 개** 있다(`canvas/ToolPalette.tsx:77-98`):
   - `그리기(SKETCH)` — 결함에 붙는 자유그리기. **결함 상태색**으로 그려진다
   - `필기메모(MEMO)` — 결함과 무관한 독립 레코드. **중립 앰버**로 그려진다
   지우개가 무엇을 지워야 하는지가 여기서 갈린다.
2. **"영역표시 x" 가 무엇인가.** 필기메모는 획을 감싸는 **점선 상자**를 그린다
   (`packages/canvas-core/src/memoGeom.ts:105 · 136-156`). 이것을 없애라는 뜻으로 읽힌다.
3. **그런데 그 점선 상자가 히트 영역이자 이동 손잡이다** (`interaction.ts:874-887` `MOVE_MEMO`).
   없애면 **지금 방식으로는 메모를 잡을 수도 옮길 수도 없게 된다.** 그래서 원문이
   "선택도구로 구간 영역 선택"을 함께 요구한 것으로 보이는데, **마퀴(드래그 사각형) 선택은
   코드에 존재하지 않는다** (`packages/` 전체에 `marquee`·다중선택 0건).

**왜 임의로 정하면 안 되는가:** 이 항목은 `canvas-core` 의 **툴 집합 · 히트테스트 · 선택 모델**을
동시에 바꾼다. 277개 캔버스 단위테스트가 걸려 있고, Phase 5 태블릿 터치(트랙 A)가 같은
히트 프로파일을 공유한다. 방향을 잘못 잡으면 그 전부를 다시 짠다.
게다가 지우개가 `SKETCH` 를 대상으로 하면 **결함에 붙은 표기**를 지우게 되어
"다른 점·화살표·번호가 지워지면 안 된다"는 원문 요구와 정면으로 충돌한다.

**선택지:**
  A. **필기 = 필기메모(MEMO)만.** 점선 상자를 **선택/hover 시에만** 표시하고 평소엔 숨긴다.
     히트는 **획 근처 12px** 로 바꾼다(상자 아님). 이동은 획을 잡고 끈다. 지우개는 **메모 획만** 지운다.
     마퀴 선택은 만들지 않는다.
     → 원문 3요구를 전부 만족하면서 **신규 개념이 0개**다. 다만 "선택도구로 구간 영역 선택"이라는
       표현과는 다르다(획을 직접 잡는다)
  B. **A + 마퀴 선택 신설.** 선택도구로 빈 곳을 드래그하면 사각형이 나오고, 그 안의 메모 획을 잡는다.
     → 원문 그대로다. 다만 다중선택 모델·`MOVE_MANY` 커맨드·Undo 병합이 새로 필요하다 (작업량 2~3배)
  C. **필기 = 자유그리기(SKETCH)도 포함.** 지우개가 두 종류를 다 지운다.
     → 결함에 붙은 표기를 지우개로 지우게 되어 "번호가 지워지면 안 된다"와 위험하게 가깝다. **비추천**

**추천:** **A 로 시작하고, 써 보신 뒤 부족하면 B.**
A 는 되돌리는 비용이 낮고(렌더 조건 + 히트 함수), B 는 A 위에 얹는 구조라 버리는 작업이 없다.

**차단 여부:** **차단.** 세 요구가 서로를 정의하므로 하나만 골라 착수할 수 없다.

> ✅ **확정: D14 (2026-08-28)** — **추천안 A 그대로.** 필기 대상은 `MEMO` 만(`SKETCH` 제외),
> 점선상자는 선택/hover 시에만 표시, 히트는 획 근처 12px, 지우개는 메모 획만,
> **마퀴 다중선택은 이번 범위에서 뺀다.** 구현 명세는
> `_workspace/30_plan-reviewer_spec_UserFeedback0828.md` §5-1. 작업 **G-1**.

---

## Q46 · plan-reviewer · 2026-08-28 · 파일1-⑤ — 범례 `결함 · 보수 · 신규` 가 무엇인가 🔴

**막힌 지점:** 원문 *"범례 On/off -> 결함, 보수, 신규"* 의 세 단어가 현재 범례 모델과 안 맞는다.

지금 범례는 **한 종류뿐**이다 — 그 도면에 쓰인 **결함유형 이름 목록**을 중립 회색 + 문자 기호로
그린다(`canvas/pageDecor.ts:49-78`, D8 확정). On/off 도 하나뿐이다(`DrawingLegend.enabled`).

`결함 / 보수 / 신규` 는 **결함 상태**(현회차 빨강 · 전회차 보라 · 보수완료 회색)를 가리키는 것으로
읽히는데, **상태 범례는 코드에 없다.**

**왜 임의로 정하면 안 되는가:** D8(사용자 확정)이 *"결함유형별 고유 색을 만들지 않는다.
범례는 회색 한 색 + 문자 기호"* 라고 못 박았다. 상태 범례를 만들면 **범례에 색이 들어간다** —
D8 이 막으려던 바로 그 상황("이 색이 유형인가 상태인가")이 될 수 있다. 다만 상태 범례는
색이 **상태 하나만** 뜻하므로 D8 의 취지와 어긋나지 않을 수도 있다. 판단이 갈린다.

**선택지:**
  A. **상태 범례를 새로 만든다.** 기존 결함유형 범례와 **별개 블록**으로, 3행 각각 On/off:
     `● 신규(현회차)` `● 미보수(전회차)` `● 보수완료`. 색은 예약색 그대로.
     → 도면 위 색이 무슨 뜻인지 보고서만 봐도 안다. D8 과 충돌하지 않는다(유형색이 아니라 상태색)
  B. **기존 결함유형 범례를 3그룹으로 나눈다** — 결함유형을 결함/보수/신규로 분류해 접었다 폈다.
     → 결함유형에 "이건 보수 항목"이라는 새 속성이 필요하다. **데이터 모델이 늘어난다**
  C. **출력 옵션의 범례 체크박스를 3개로 쪼갠다** (`params.render.legend` → 3플래그).
     → 지금 있는 것만 켜고 끄므로 가장 싸지만, 끌 대상이 하나뿐이라 의미가 없다

**추천:** **A.** 세 단어가 상태색과 정확히 대응하고, 도면에 이미 그 세 색이 쓰이고 있다.
다만 **"결함"이 전회차 미보수를 뜻하는 것이 맞는지** 확인이 필요하다 — 그것이 A의 유일한 전제다.

**차단 여부:** **차단.** 무엇을 켜고 끄는지 모르면 UI 도 데이터도 못 정한다.

> ✅ **확정: D15 (2026-08-28)** — **추천안 A 그대로.** 기존 결함유형 범례(D8)와 **별개 블록**으로
> 상태 범례 3행(`신규(현회차)` 빨강 · `미보수(전회차)` 보라 · `보수완료` 회색), 각각 On/off.
> **D8 을 뒤집지 않는다** — D8 은 *결함유형별* 색을 막은 것이고 이건 *상태* 색이다.
> 명세 §5-2(기본값 전부 꺼짐 · 그 도면에 없는 상태는 켜져 있어도 안 그린다). 작업 **G-2**.

---

## Q47 · plan-reviewer · 2026-08-28 · 파일1-④ — 도곽을 프로젝트 단위로 올리는가 🔴

**막힌 지점:** 원문 *"도곽은 모든도면에 크기가 연동되어야 함"*.
지금 도곽 설정은 **도면 1장 단위**다 (`Drawing.titleBlock`, `project-core/src/types.ts:186`).
도면이 10장이면 10번 설정한다.

**함께 물어야 하는 것 — 이번 검토에서 찾은 버그가 여기 붙어 있다.**
새 도면은 `titleBlock: null` 로 만들어지는데(`apps/web/src/data/factory.ts:124`),
`pageDecor.ts:28` 이 `null` 을 "도곽 없음"으로 읽는다. 그런데 설정 다이얼로그는 `null` 을
`DEFAULT_DRAWING_TITLE_BLOCK`(`enabled: true`)로 폴백해 **체크박스가 켜진 것처럼 보여준다**
(`TitleBlockDialog.tsx:43-45`). 결과적으로 **`[저장]` 을 한 번 누르기 전까지 도곽이 화면에도
출력물에도 안 나온다.** → 파일1-③ "도곽 출력안됨"의 근본 원인이다.

**왜 임의로 정하면 안 되는가:** 스코프를 올리면 **층마다 다른 축척(SCALE)을 쓸 수 없게 된다.**
지하주차장과 기준층의 축척이 다른 것은 실무에서 흔하다. 또 D6(용역별 설정 스냅샷)이
*"작년 보고서를 다시 뽑으면 옛 이름이 나와야 한다"* 를 이유로 세워져 있어, 프로젝트 단위 설정을
새로 만들 때 그 원칙과 어떻게 맞물릴지 정해야 한다.

**선택지:**
  A. **프로젝트 기본값 + 도면 오버라이드.** `Project` 에 도곽 기본값을 두고 `Drawing.titleBlock` 은
     남긴다. 도면 설정에 `[전역 설정으로 초기화]` 버튼. **상세기획의 스타일 상속 규칙과 같은 패턴**이다
     → 신규 개념 0. 축척만 도면별로 다르게 둘 수 있다. 구현 중간
  B. **완전히 프로젝트 단위로 통일.** `Drawing.titleBlock` 을 안 읽고 프로젝트 값만 쓴다
     → 가장 단순하지만 **도면별 축척을 포기**한다
  C. **도면 단위 유지 + `[이 설정을 모든 도면에 적용]` 버튼 하나만 추가**
     → 가장 싸다(버튼 1개 + 일괄 저장). 도면을 나중에 추가하면 다시 눌러야 한다

**추가 확인:** 위 버그 수정(`pageDecor.ts` 가 `null` 을 기본값으로 폴백)을 함께 해도 되는가?
**해도 된다면 지금까지 도곽이 없던 도면에 도곽이 갑자기 나타난다** — 사용자가 보는 화면이 바뀐다.

**추천:** **A + 버그 수정 승인.** A 는 이 프로젝트가 이미 쓰는 패턴이라 학습 비용이 0이고,
"모든 도면 연동"과 "층별 축척"을 둘 다 만족하는 유일한 안이다.

**차단 여부:** **차단.** 저장 데이터 위치가 바뀌는 결정이다.

> ✅ **확정: D16 (2026-08-28)** — 추천안 A(프로젝트 기본값 + 도면 오버라이드)가 아니라
> **필드별로 스코프를 지정하는 더 정밀한 안**으로 확정됐다:
> **프로젝트 공유 = 용역명(`projectTitle`) · 스케일(`scale`) · 도곽크기(`tbScale`)** /
> **도면별 유지 = 도면명(`drawingName`)**.
> plan-reviewer 가 타입을 다시 확인해 미언급 5필드(`enabled`·`col0`·`col1`·`labelFontSz`·`valueFontSz`)도
> **프로젝트 스코프로 확정**했다(근거: 명세 §5-3-a). **버그 B2 수정도 함께 승인됨.**
> 명세 §5-3(승격 규칙·UI 포함). 작업 **G-3**.

---

## Q48 · plan-reviewer · 2026-08-28 · 파일2-② — 발생원인↔보수방안이 **새 연결**인가 **씨앗 데이터**인가 🔴

**막힌 지점:** 원문 *"누수, 마감파손 등에 대한 발생원은 보수.보강방안 설정 추가"* 가 두 가지로 읽힌다.

현재 항목 계층은 **4단**이고 연결은 두 개뿐이다 (`packages/project-core/src/items/types.ts:52-64`):
```
linkDefectTypeCause   결함유형 → 발생원인
linkDefectTypeRepair  결함유형 → 보수방안
```
`발생원인 → 보수방안` 연결은 **없다.**

그리고 씨앗 데이터(`items/seed.ts:68-82`)는 이렇다:
```
발생원인 4종: 건조수축 · 하중변화 · 재료분리,시공불량 · 이질재 접합     ← 전부 균열 계열
보수방안 5종: 표면처리 · 에폭시주입 · V커팅충진 · 단면복구 · 경과관찰    ← 전부 균열 계열
```
결함유형 13종에 이 5개가 **전조합으로** 붙는다(`seed.ts:175-182`). 즉 **누수흔적·마감박리·백태에도
"에폭시 주입공법"만 뜬다** — 실무에서 못 쓴다. 사용자가 지적한 것이 이것일 가능성이 높다.

**왜 임의로 정하면 안 되는가:** 두 해석의 비용이 10배 차이난다.
- 씨앗 확장이면 **코드 변경 0**, 데이터 표만 늘린다
- 새 연결이면 계층이 5단이 되고, 설정 화면에 열이 하나 더 생기고, `resolve.ts` 의 조회 규칙과
  결함 입력 폼의 연동(발생원인을 바꾸면 보수방안 목록이 바뀐다)이 전부 바뀐다.
  **불변식 #6(마스터+연결)을 지키기만 하면 구조상 안전하지만, 되돌리기가 비싸다.**

**선택지:**
  A. **씨앗 데이터만 확장.** 누수·마감·박리·백태 계열 발생원인과 보수·보강방안을 표에 추가하고,
     결함유형별 링크를 실무에 맞게 정리한다 (예: 누수흔적 → 방수공법·실링재 교체)
     → 코드 0줄. **다만 "어느 결함유형에 어느 보수방안"의 실무 표를 사용자가 주셔야 한다**
  B. **`linkCauseRepair` 신설.** 발생원인을 고르면 그에 맞는 보수방안만 뜬다
     → 원문에 더 가깝다. 계층 5단. 설정 화면 재설계 필요
  C. **A 먼저, B 는 나중에.** 씨앗을 채워 쓰다가 정말 필요하면 연결을 추가한다
     → A 를 해도 B 로 가는 길이 막히지 않는다(마스터는 그대로, 링크만 추가)

**추천:** **C.** 지금 아픈 것은 "누수에 맞는 보수방안이 목록에 아예 없다"는 쪽이고,
그건 A 로 즉시 해결된다. 연결까지 필요한지는 실제로 써 보면 드러난다.

**차단 여부:** **차단.** 데이터 구조 결정이고, A 를 고르면 **실무 표(어느 결함유형에 어느
발생원인·보수방안)를 사용자가 주셔야 한다.**

---

## Q49 · plan-reviewer · 2026-08-28 · 파일2-③ — D9(직전 입력 이어받기)를 유지하는가 끄는가 🔴

**막힌 지점:** 원문 *"비슷한 유형 결함 불러오기 기능 개선 -> 별도버튼 구현 (결함리스트 노출->선택)
(지금은 직전 입력된 결함정보가 자동선택)"*.

괄호 안의 서술은 **D9 그대로의 정상 동작**이다 (`apps/web/src/store.ts:386-389`).
`_workspace/DECISIONS.md` **D9 (2026-08-24, 사용자가 직접 확정)**:
*"분류·판정은 이어받고(부재·결함유형·발생원인·보수방안 등) 측정값·개별정보는 매번 새로 받는다.
갱신 시점 — 필드가 바뀔 때마다 즉시."*

괄호가 **불만**인지 **현황 설명**인지에 따라 결과가 정반대다.

**왜 임의로 정하면 안 되는가:** D9 는 사용자가 직접 답한 결정이고, 그 근거로 상세기획 §2-B
"입력 3원칙 #3 — 직전 입력을 기억한다. 위치만 새로 찍으면 됨"이 인용돼 있다.
**에이전트가 추측으로 뒤집으면 안 되는 정확히 그 유형이다.**
그리고 D9를 끄면 "위치만 찍으면 되는" 빠른 입력이 사라져 현장 입력 속도가 크게 느려진다.

**선택지:**
  A. **D9 유지 + `[불러오기]` 버튼 추가.** 기본은 지금처럼 직전 값이 채워지고,
     다른 결함을 참고하고 싶을 때만 버튼을 눌러 결함 리스트에서 고른다 (고르면 그 결함의
     분류·판정 필드만 복사. 측정값·메모·위치보조는 복사하지 않는다 — D9 표 그대로)
     → 빠른 입력을 잃지 않고 원문 요구도 만족. **D9 위반 아님**
  B. **D9 를 끈다.** 새 결함은 빈 폼으로 시작하고, 반드시 버튼으로 불러온다
     → 원문의 괄호를 불만으로 읽은 해석. **매번 버튼을 눌러야 해서 입력이 느려진다**
  C. **D9 를 끄되 마지막 값은 버튼의 맨 위에 "직전 입력" 항목으로 둔다**
     → 절충. 클릭 1회 추가

**추천:** **A.** 원문이 "개선"이라 했지 "제거"라 하지 않았고, D9 는 사용자 본인의 결정이다.
A 는 D9 를 건드리지 않으므로 **틀렸을 때 되돌리는 비용이 가장 낮다**(버튼만 지우면 된다).

**차단 여부:** **차단.** D9(사용자 기결정)를 뒤집을 수 있는 요청이다.

> ✅ **확정: D18 (2026-08-28)** — **선택지 B. D9 를 폐기한다.**
> 새 결함은 빈 폼에서 시작하고, `[유사결함 불러오기]` 버튼 → 결함번호(`seq`) 선택 → 로드.
> **불러오는 필드 범위는 plan-reviewer 가 확정했다 — D9 표 그대로(복사 14 · 새로 받음 8).**
> 전체 복사가 아니다: 폭·길이·개소·메모까지 오면 지우는 손이 더 든다(명세 §5-4-a).
> D9 폐기에 따른 코드·문서 갱신 대상 10파일 51곳은 §5-4-c 에 전부 나열했다.
> ⚠️ **프로젝트 기본 구조유형(`seedAttrs`)은 D9 가 아니므로 함께 지우지 않는다**(§5-4-d). 작업 **G-5**.

---

## Q50 · plan-reviewer · 2026-08-28 · 파일2-⑤ — 층 접두어의 출처 · 자릿수 · 사진번호 전체연속 🔴

**막힌 지점:** 원문 *"층별선택시 No-> 해당층 구분 (예:1F-01, RF-01, W-01, B1F-01 등) /
사진번호는 연속되어 구분되게 해야함 (1F-01 = 1번사진, 2F-01 = 13번사진 등)"* 에 결정이 세 개 들어 있다.

**(가) 층 접두어를 어디서 얻는가.**
`Floor` 레코드에는 **약어 필드가 없다** (`project-core/src/types.ts:85-93` — `name`·`sortOrder` 뿐).
`floorOrder.ts::parseFloorName` 이 `지상1층→ABOVE(1)` · `지하1층→BELOW(1)` · `옥탑→ROOFTOP` ·
`옥상→ROOF` · `PIT` 를 이미 구분하므로 **`1F` `B1F` `RF` `PIT` 는 자동 파생 가능하다.**
**그러나 예시의 `W`(외벽?)는 `UNKNOWN` 이라 파생할 수 없다.**

**(나) 자릿수.** `01` 은 2자리 0채움으로 읽힌다. 결함이 100건을 넘으면 `1F-100` 인가 `1F-0100` 인가.

**(다) 사진번호 전체연속 — 이것이 기존 규칙을 뒤집는다.**
지금 `numbering.ts:176-181` 은 **층별리셋 모드에서 사진번호도 함께 리셋한다**:
```ts
if (params.mode === 'PER_FLOOR') { no = 0; photoNo = 0; }   // K6 · Q34
```
사용자 예시(`1F-01 = 1번사진`, `2F-01 = 13번사진`)는 **사진번호를 리셋하지 말라**는 뜻이다.
K6/Q34 는 **에이전트 가정이었고 사용자 답변이 없었다** → 뒤집어도 D 계열 결정 위반이 아니다.

**왜 임의로 정하면 안 되는가:** (가)는 저장 스키마가 걸리고, (다)는 **모든 산출물 4종의 사진번호가
동시에 바뀐다.** 손상결함표의 `사진번호` 열 · 사진첩 캡션 1행 · 조사위치도가 전부 같은 값을 쓴다
(불변식 #2 · 재현성). 잘못 정하면 발주처 제출본의 번호가 어긋난다.
**다행히 `ExportRun.mapping` 은 정수를 그대로 담으므로 이미 뽑은 보고서의 재출력은 영향이 없다.**

**선택지 (가):**
  A. **자동 파생만.** `1F · B1F · RF · PH · PIT`. 파싱 실패 층은 접두어 없이 `01` 로 나간다
     → 코드만. 저장 변경 0. **`W` 는 못 만든다**
  B. **`Floor.code` 신설**(선택 입력, 비우면 A로 자동 파생). 용역 구성 화면에서 층마다 입력
     → `W` 도 되고 발주처 표기 관행도 맞출 수 있다. 필드 1개 추가(선택 필드라 마이그레이션 불필요)
  C. **B + 프로젝트 단위 일괄 편집 표**

**선택지 (나):** A. 2자리 고정(`1F-100` 은 3자리로 자연 확장) / B. 자릿수 설정값 / C. 0채움 없음(`1F-1`)

**선택지 (다):** A. **사진번호는 항상 전체 연속**(원문 그대로) / B. 지금처럼 번호모드를 따라간다
/ C. **출력 옵션으로 고를 수 있게 한다**

**추천:** (가) **B** — `W` 는 자동으로 못 만들고, 발주처마다 표기가 다르다는 것이 이 프로젝트의
반복된 교훈(D6·K18)이다. (나) **A**. (다) **A** — 사용자가 실제 예시까지 들어 명확히 요구했다.

**차단 여부:** **차단.** (가)는 저장 스키마, (다)는 산출물 4종의 번호를 동시에 바꾼다.

> ✅ **확정: D19 (2026-08-28)**
> - **(가) 선택지 B** — `Floor.code` 신설(선택 입력, 비우면 이름에서 자동 파생).
>   **"외부"는 수동 입력 없이 자동으로 `W`** → `parseFloorName` 에 `EXTERIOR` 유형 신설(명세 §5-5-b).
> - **(나) 선택지 A** — 2자리 0채움(`1F-01`), 100 이상은 자연 확장. 이견 없음으로 확정.
> - **(다) 선택지 A** — **사진번호는 항상 전체 연속. `numbering.ts:180` 의 `photoNo = 0` 을 삭제한다.**
>   ⚠️ **이로써 Q34 / 가정 K6 은 폐기된다**(사진번호가 번호모드를 따라가지 않는다).
> - ⭐ plan-reviewer 추가 확정: **`ExportParams.floorCodes` 스냅샷을 넣는다** — 접두어가 파생값이라
>   층 이름·code 를 고치면 재출력 결과가 달라진다. 재현성을 접두어까지 확장한다(§5-5-f).
> - ⚠️ **미해결 세부는 Q53 으로 분리**(접두어가 일부 층에만 있을 때). 비차단.
>
> 명세 §5-5. 작업 **G-6**.

---

## Q51 · plan-reviewer · 2026-08-28 · 파일1-②③ — "출력안됨" 2건의 실제 증상 확인

**막힌 지점:** 사용자가 "출력안됨"으로 신고한 두 건 중 **한 건은 원인을 찾았고 한 건은 못 찾았다.**
진단이 맞는지 확인이 필요하다.

**① 사진첩 "출력안됨" — 설계된 동작으로 보인다.**
```
apps/web/src/export/produce.ts:27-32   FILE_ARTIFACTS = [손상결함표, 결함리스트, 조사위치도]   ← 사진첩 없음
apps/web/src/routes/Export.tsx:71      '파일이 아니라 인쇄 뷰로 냅니다 — 생성 후 [사진첩 PDF] 를 누르세요'
```
사진첩을 체크하고 `[생성]` 을 눌러도 **파일이 안 내려온다.** 아래 **출력 이력** 칸의
`[사진첩 PDF]` 를 눌러야 열린다(M3 · Q37 로 기록된 알려진 성질).
그리고 원문의 나머지 두 요구는 **이미 되어 있다** — `print.css:150-174` 가 2열×3행 = **6장/페이지**,
`.pv-cell__frame { border: 0.3mm solid #666; }` = **셀 테두리**.
→ **사진첩 인쇄 뷰를 한 번도 보지 못하신 것으로 추정된다.**

**② 조사위치도 결함번호 "출력안됨" — 코드에서 누락 경로를 못 찾았다.**
번호 주입은 3곳 모두 정상이다 (`produce.ts:113` · `PrintRoute.tsx:117` · `locationMap.ts:196-197`).
다만 **번호 풍선이 도면 이미지 px 기준 34로 고정**이라(`constants.ts:159` + `renderModel.ts:736`)
장변 6000px 스캔 도면이면 A4 인쇄 시 번호 지름이 **약 1.7mm** — 사실상 안 보인다.
보정 수단은 도면별 `번호 크기`(F6 `labelScale`)로 이미 있다.

**질문:**
  1. 출력 화면에서 사진첩을 체크하고 `[생성]` 을 누르신 뒤, **아래 출력 이력의 `[사진첩 PDF]`**
     를 눌러 보신 적이 있습니까? (없다면 한 번 눌러 보시고 결과를 알려주세요)
  2. 조사위치도의 번호는 (a) **아예 안 보인다** (b) **너무 작아서 안 보인다**
     (c) **도면 밖으로 잘렸다**(생성 시 `번호 N개가 도면 밖에 있습니다` 경고가 떴는지) 중 어느 쪽입니까?
  3. 도면 한 장의 대략적인 픽셀 크기(또는 원본 파일 크기)를 알려주실 수 있습니까?

**왜 임의로 정하면 안 되는가:** 원인이 다르면 고칠 곳이 완전히 다르다.
(a)면 렌더 경로 버그, (b)면 기본 `labelScale` 정책, (c)면 여백 정책(K11)이다.
아무 데나 손대면 안 아픈 곳을 고치고 아픈 곳은 그대로 남는다.

**추천:** 판단 근거 없음 — 실제 화면을 봐야 안다.

**차단 여부:** **비차단.** ①은 파일1-⑥(Q52)로 어차피 개선되고, ②는 다른 작업을 막지 않는다.

---

## Q52 · plan-reviewer · 2026-08-28 · 파일1-⑥ — Excel/PDF 버튼 매트릭스 🔴

**막힌 지점:** 원문 *"손상결함표, 결함리스트, 사진첩, 조사위치도 각각 Excel생성버튼, PDF생성버튼 구현
-> 미리보기 화면 띄워주고 출력버튼 한번더 눌러서 파일저장"*.

**뒷부분(미리보기 후 별도 출력)은 주장이 사실이고 바로 고칠 수 있다** —
`PrintRoute.tsx:151-168` 이 데이터 준비 즉시 `window.print()` 를 자동 실행한다.
이건 질문 없이 F-2 로 고친다.

**앞부분(4종 × 2형식 = 8버튼)이 문제다.** 지금 존재하는 조합은 이렇다:

| 산출물 | Excel | PDF(인쇄 뷰) |
|---|---|---|
| 손상결함표 | ✅ | ❌ **없다** (R-9 로 만들 예정, 미착수) |
| 결함 리스트 | ✅ | ✅ |
| 사진첩 | ❌ **불가능** (사진 격자를 엑셀 셀에 앉히는 것은 별개 작업) | ✅ |
| 조사위치도 | ❌ (PNG 로 나온다) | ✅ |

**왜 임의로 정하면 안 되는가:** 8칸을 전부 버튼으로 만들면 **4칸이 눌러도 안 되는 버튼**이 된다.
"버튼이 있는데 안 눌린다"는 지금 사진첩에서 겪고 계신 문제(Q51 ①)와 **똑같은 종류의 실패**다.
그리고 "사진첩 Excel" 을 진짜로 만들지 여부는 작업량이 전혀 다르다.

**선택지:**
  A. **있는 것만 버튼으로 낸다.** 산출물 행마다 실제로 가능한 형식만 버튼이 뜬다
     (손상결함표 `[Excel] [PDF]` / 결함리스트 `[Excel] [PDF]` / 사진첩 `[PDF]` / 조사위치도 `[PNG] [PDF]`).
     **R-9(손상결함표 PDF)를 먼저 끝내야 한다.**
     → 못 하는 것을 못 한다고 보여준다. 사용자가 헤매지 않는다
  B. **8칸 전부 만든다.** 사진첩 Excel · 조사위치도 Excel 을 새로 구현
     → 원문 그대로. 사진첩 Excel(이미지 6장 격자 삽입)은 `write-excel-file` 로 **불가능에 가깝다**.
       라이브러리 교체가 필요하고, 그건 이 프로젝트가 의존성 최소 원칙으로 피해 온 방향이다
  C. **A + 불가능한 칸은 회색으로 보이되 이유를 툴팁으로 설명**
     → 사용자가 "왜 없지?"를 묻지 않아도 된다

**추가 질문:** **조사위치도는 지금 PNG 로 나온다.** 이것을 Excel 로도 원하십니까,
아니면 PNG 로 충분합니까? (PNG 는 워드·한글에 그대로 붙습니다)

**추천:** **C.** A 의 정직함에 설명을 더한 것이고, 되돌리는 비용이 낮다.
**단 R-9(손상결함표 인쇄 뷰)가 선행되어야 한다** — 이미 스펙이 나와 있고 미착수 상태다.

**차단 여부:** **차단.** 사용자가 보게 될 산출물 형태를 정하는 결정이다.
(다만 **F-2(자동 인쇄 제거)는 이 질문과 무관하게 먼저 진행한다.**)

---

## Q53 · plan-reviewer · 2026-08-28 · D19 후속 — 접두어가 **일부 층에만** 있을 때 나머지 층의 번호는?

**막힌 지점:** D19 가 *"접두어를 입력하면 **그 층은** 층별 번호부여가 활성화된다 — 접두어 유무가
**그 층의** 넘버링 표기 방식을 결정하는 스위치"* 라고 했다. 이것을 **층마다 따로**로 읽으면 모순이 난다.

```
층A (접두어 1F)   → 1F-01, 1F-02, 1F-03
층B (접두어 없음) → ??
```
- 층B가 **이어서 `04, 05`** 라면: 앞 층이 `1F-01~03` 으로 표기됐으니 사용자는 `04` 를 보고
  **앞에 3건이 있었다는 것을 알 수 없다.** 번호가 자기 설명적이지 않게 된다.
- 층B가 **`01, 02`** 라면: 접두어 없는 층도 결국 리셋되는 것이므로, 사실상 **전체가 층별리셋**이다.

즉 "층마다 독립적으로 리셋/연속을 섞는다"는 해석은 **번호 체계로서 성립하지 않는다.**

**왜 임의로 정하면 안 되는가:** 결함번호는 손상결함표·결함리스트·사진첩·조사위치도 **4종이 공유**하고
발주처 제출본에 그대로 인쇄된다. 잘못 정하면 보고서 전체의 번호가 어긋난다.
다만 `numbering.ts` 자체는 어느 해석에서도 안 바뀌고(포매터와 모드 결정만 바깥에서 한다)
되돌리는 비용이 낮아 **차단은 아니다.**

**선택지:**
  A. **접두어가 하나라도 있으면 출력 번호모드를 `PER_FLOOR`(층별리셋)로 자동 전환한다.**
     접두어 있는 층 → `1F-01`, 접두어 없는 층 → `01`(그 층 안에서 리셋).
     출력 화면에서 사용자가 다시 바꿀 수 있다(자동 전환은 **기본값 제안**이지 강제가 아니다).
     → D19 문장을 만족하면서 번호가 항상 자기 설명적이다. `assignNumbers` 무변경
  B. **접두어는 표시만 담당하고 리셋 여부는 기존 전역 모드를 그대로 따른다.**
     `CONTINUOUS` 면 `1F-04, 1F-05` 처럼 접두어는 붙되 번호는 이어진다
     → "접두어 = 층별 넘버링 활성화"라는 D19 문장과 어긋난다
  C. **접두어 없는 층은 출력에서 제외한다**
     → 조용히 결함이 빠진다. **채택하면 안 된다**(D3 "자동 제외 금지"와 같은 종류의 사고)

**추천:** **A.** D19 문장에 가장 가깝고, 모순이 없고, `numbering.ts` 를 한 줄도 안 바꾼다.
`ExportRun.params.mode` 에 실제로 쓰인 모드가 저장되므로 재출력도 같은 결과가 나온다.

**차단 여부:** **비차단.** **A 로 진행한다**(`ASSUMPTIONS.md` U13).
되돌리는 비용: `Export.tsx` 의 모드 초기값 계산 1곳 + 포매터 호출부.

---

## Q54 · code-reviewer · 2026-08-30 · D19 후속 — 접두어를 **비워 두면** 접두어가 없는 것인가, 이름에서 자동으로 만드는 것인가 🔴

**막힌 지점:** 배치3 구현(`floorOrder.ts::floorCodeOf`)은 층 접두어 칸이 **비어 있으면 층 이름에서
접두어를 파생**한다 — `지상1층` → `1F`. 층 이름은 거의 항상 파싱되므로 결과적으로
**접두어가 없는 층이 사실상 존재하지 않고, 사용자가 접두어를 끌 방법도 없다.**

그래서 **접두어를 한 번도 입력한 적 없는 기존 용역까지** 다음이 전부 바뀐다:
- 손상결함표·결함리스트 NO 열: `1` → `1F-01`
- 출력 번호모드 기본값: `전체 이어서` → `층별 1번부터`(자동 제안이 항상 발동)
- 조사위치도 번호 풍선: `1` → `1F-01`
- **이력에서 `[같은 번호로 다시 받기]`** 한 옛 출력물도 접두어가 붙는다(예전 파일과 다른 결과)

**왜 임의로 정하면 안 되는가:** 결함번호는 발주처 제출본에 그대로 인쇄되고 4종 산출물이 공유한다.
"기존 보고서와 같은 번호가 다시 나온다"는 재현성 원칙(§10-1)에 직접 걸린다.
또 D19 원문은 *"접두어를 **입력하면** … 접두어 **유무**가 스위치가 된다"* 인데, 자동 파생이면
그 스위치가 항상 ON 이고 끌 수 없다 — **사용자 문장과 구현이 어긋난다.**
반면 스펙 §5-5(a)(c)는 *"null/'' = 이름에서 자동 파생"* 을 명시했다. **문서 둘이 갈린다.**

**선택지:**
  A. **옵트인 — 사용자가 직접 넣은 접두어만 출력에 쓴다.**
     비어 있으면 접두어 없이 `1` · `2` · `3`(예전 그대로). 자동 파생값은 입력칸의
     **회색 placeholder(제안)** 로만 보여 주고, 사용자가 원하면 한 번 눌러 채운다.
     → 손대지 않은 용역·옛 이력의 산출물이 **한 글자도 안 바뀐다.** D19 문장과도 일치.
     비용: `floorCodesOf` 한 함수(3줄) + 테스트.
  B. **현행 유지 — 이름에서 자동 파생.** 아무것도 안 해도 `1F-01` 이 나오는 것이 편하다는 판단.
     → 기존 용역의 출력물이 전부 바뀌고 되돌릴 방법이 없다. 옛 이력 재다운로드도 달라진다.
  C. **자동 파생하되 층마다 끌 수 있게 한다** — `code` 에 `-` 같은 "없음" 표식을 허용.
     → 표식 규칙을 사용자가 외워야 한다. 입력칸 하나로는 설명이 안 된다.

**추천:** **A.** 새 기능이 기존 산출물을 조용히 바꾸지 않는 것이 이 프로젝트의 일관된 원칙이었다
(D15 상태 범례를 "기본 전부 false — 기존 출력물이 한 글자도 안 바뀌게" 로 정한 것과 같은 판단).
접두어를 쓰고 싶은 사용자는 칸이 눈앞에 있고 제안값까지 보이므로 한 번만 누르면 된다.

**차단 여부:** **차단.** 어느 쪽이냐에 따라 배치3 산출물의 기본 동작이 정반대가 되고,
사용자 확인 체크리스트(D-22 "예전과 똑같아야 한다")를 지금 상태로는 통과시킬 수 없다.

---

## Q55 · plan-reviewer · 2026-09-02 · ⭐ D13(Q6) 재상정 — 정밀 표기 방식 최종 선택 🔴

**막힌 지점:** 2026-08-25 에 D13 으로 *"확정하지 않고 목업 두 안을 함께 그려서 비교한다"* 고 정했고,
목업은 나왔다(`_workspace/mobile_mockup/M05a_AimCrosshair.dc.html` · `M05b_LongpressLoupe.dc.html`).
**그런데 그 뒤로 선택이 이뤄지지 않았다.** 이제 태블릿 실화면을 만들 차례인데, 도면에 결함을
**어떻게 찍는가**가 정해지지 않아 캔버스 화면 작업(T2-2)을 시작할 수 없다.

**왜 임의로 정하면 안 되는가:**
1. **매일 수백 번 반복할 손동작**이다. 현장 입력의 품질(좌표 정확도)이 전부 여기서 갈린다.
2. **두 안의 작업량이 다르다.** 안 A 는 코어 변경 0 · 오버레이 1파일. 안 B 는 **손가락 아래를
   실시간으로 확대해 그리는 루페 렌더가 새로 필요**하다(캔버스를 한 번 더 그린다).
3. 한 번 손에 익으면 바꾸기 어렵다 — 나중에 뒤집으면 사용자가 다시 배워야 한다.
4. D13 이 *"실제 구현 착수 전에 반드시 확정"* 이라고 이미 못 박아 두었다.

**선택지:**
  A. **조준 크로스헤어 (M-05a, plan-reviewer 원 추천)**
     `⊕조준` 토글을 켜면 **화면 정중앙에 십자선이 고정**된다. 한 손가락으로 도면을 밀고
     두 손가락으로 확대해 십자선 아래에 목표를 맞춘 뒤, 오른손 엄지 자리의 `[여기]` 를 누른다.
     → 손가락이 목표를 절대 가리지 않는다 / **코어 변경 0**(화면 중앙 좌표로 POINTER_DOWN+UP 합성) /
       확대·이동과 무관하게 십자선이 늘 같은 자리 / 연속 표기가 빠르다
     → 대신 "밀어서 맞춘다"는 동작이 한 단계 더 있다. 조준 토글을 켜고 끄는 것을 기억해야 한다
  B. **롱프레스 루페 (M-05b)**
     도면을 꾹 누르면 손가락 위에 **돋보기(루페)** 가 뜨고, 그 안을 보며 드래그로 미세조정한 뒤
     손을 떼면 그 자리에 찍힌다. iOS 텍스트 커서와 같은 방식이라 익숙하다.
     → 한 동작으로 끝난다 / 별도 토글이 없다
     → 대신 **루페 렌더를 새로 만들어야 한다**(작업 상) / 롱프레스가 이미 컨텍스트 메뉴
       (`CONTEXT_MENU`, 코어에 있음)에 배정돼 있어 **충돌한다**(둘 중 하나를 옮겨야 함) /
       확대율이 올라갈수록 손가락 1px 떨림이 확대돼 오히려 흔들린다
  C. **둘 다 넣고 설정에서 고른다**
     → 두 경로를 다 유지·검수해야 하고, 롱프레스 충돌 문제는 그대로 남는다. 1차에는 과하다

**추천:** **A.** 코어 변경이 0 이고 손가락 가림이 구조적으로 해결되며 롱프레스 충돌이 없다.
다만 이건 **손맛의 문제**라 목업 두 화면을 태블릿에서 직접 보고 고르시는 것이 맞다.
(`_workspace/mobile_mockup/M05a_AimCrosshair.dc.html` · `M05b_LongpressLoupe.dc.html`)

**차단 여부:** 🔴 **차단.** 태블릿 캔버스 실화면(T2-2)을 시작할 수 없다.
다른 태블릿 작업(PWA 껍데기 P1~P6, 층칩·미니맵)은 이 답 없이 진행 가능하다.

---

## Q56 · plan-reviewer · 2026-09-02 · 🔴 `Defect` 에 병합 재료(`updatedAt`·`deviceId`·`createdBy`)가 없다

**막힌 지점:** `packages/project-core/src/types.ts` 의 `RecordBase` 주석은
*"모든 레코드가 갖는 필드. **Phase 5 병합·감사의 재료다.** 나중에 붙이면 기존 레코드가 전부
`null` 이라 병합이 그 구간을 판정할 수 없다"* 라고 못 박고 있다.
그런데 실제로 확인해 보니 **`Defect` 만 이 네 필드를 하나도 갖고 있지 않다.**

- `Project`·`Building`·`Floor`·`Drawing`·`Photo`·`ItemSettings` → `RecordBase &` ✅
- `Memo`(`canvas-core/types.ts` 104~107행) → 네 필드를 직접 나열 ✅
- **`Defect`(`canvas-core/types.ts` 259~286행 + `DefectAttrs` 208~252행) → 없음** ❌
- `apps/web/src/data/factory.ts` 에도 `makeDefect` 가 없다 — 결함은 `interaction.ts` 안에서
  만들어지고 그 자리에 시각·기기 스탬프가 없다
- 검증: `canvas-core/src` 전체에서 `updatedAt` 은 2곳뿐이고 **둘 다 메모 경로**다

**이 상태로는 못 하는 것:** LWW 병합 판정 / "미전송 변경 N건" 집계 / `[기기에서 비우기]` 안전장치 /
"이 결함은 누가 찍었나" 감사. **팀 동기화의 뼈대가 전부 여기 걸린다.**

**왜 임의로 정하면 안 되는가:** 필드를 더하는 것 자체는 자명하다. 진짜 결정은
**"이미 저장된 옛 결함(사용자 IndexedDB 에 이미 있는 것)의 `updatedAt` 을 무엇으로 읽는가"** 이고,
그 값이 **첫 동기화에서 누구의 데이터가 살아남는지**를 결정한다. 잘못 고르면 사무실 PC 의
기존 결함 수백 건이 태블릿의 빈 상태에 덮인다 — 되돌릴 수 없다.

**선택지:** (셋 다 `DB_VERSION` 은 1 유지 — optional 필드 추가 + 읽기 정규화)
  A. **옛 결함은 `updatedAt = 0` 으로 읽는다 (항상 지는 쪽).**
     → 어느 기기에서든 **한 번이라도 만진 결함이 이긴다.** 안 만진 옛 결함끼리 부딪히면
       `deviceId` 사전순으로 결판난다.
     → 위험: 태블릿에서 실수로 결함을 살짝 건드리면 PC 의 정성껏 입력한 값을 덮는다.
  B. **첫 동기화 때 서버가 받은 시각을 부여한다 (먼저 올린 쪽이 원본).**
     → "사무실 PC 에서 먼저 한 번 올린다"는 운영 규칙 하나로 안전해진다. 옛 데이터가 보호된다.
     → 위험: 팀원이 실수로 먼저 올리면 그쪽이 원본이 된다. 운영 규칙을 사람이 지켜야 한다.
  C. **옛 결함은 아예 동기화 대상에서 뺀다.** 새 필드가 있는 결함만 올린다.
     → 가장 안전하지만, 지금까지 만든 데이터가 **영원히 팀에 공유되지 않는다.**
       (다시 만들거나, 나중에 일괄 스탬프 도구를 따로 만들어야 한다)

**추천:** **B.** "먼저 올린 쪽이 원본"은 사람이 이해하기 쉽고, 첫 1회만 지키면 되는 규칙이다.
A 는 손이 스친 것만으로 데이터가 뒤집히는 사고가 실제로 잘 난다.
어느 쪽을 고르든 **`createdBy` 는 옛 결함에서 영구히 `null`** 이다(누가 만들었는지 알 방법이 없다) —
화면에서는 `작성자 미상` 으로 표시한다.

**차단 여부:** 🔴 **차단.** 동기화 데이터 모델(T1-2·T1-8)과 서버 스키마(T1-1)가 전부 여기서 갈린다.

---

## Q57 · plan-reviewer · 2026-09-02 · 팀 ↔ 조직 계층과 프로젝트 소유·가시성 단위 🔴

**막힌 지점:** 사용자 원문은 *"팀에는 팀장1인과 팀원(다수)"*, *"같은 팀은 프로젝트가 동기화"* 다.
그런데 코드에는 이미 `Project.orgId: string | null` — *"조직은 Phase 5. 지금은 null"* 이 예약돼 있다.
**"조직"과 "팀"이 같은 것인지 다른 것인지가 정해지지 않았고**, 프로젝트가 누구 소유인지도 안 정해졌다.

**왜 임의로 정하면 안 되는가:** 이 답이 그대로 서버 테이블의 컬럼이 되고 RLS(누가 무엇을 볼 수 있는가)
규칙이 된다. 나중에 바꾸면 **이미 올라간 모든 프로젝트의 소속을 옮겨야 한다.**
또 (나)를 고르면 "프로젝트를 누구에게 배정한다"는 **화면이 하나 더 필요**해진다.

**선택지 — (1) 계층:**
  A. **팀 = 조직. 1계층.** `Project.orgId` 를 **"소유 팀 id"** 로 확정해 그대로 쓴다.
     → 필드가 이미 있어 로컬 스키마가 안 바뀐다. 나중에 회사 계층이 필요해지면
       **서버에 `teams.org_id` 만 추가**하면 되고 로컬은 그대로다(DB_VERSION 1 유지에 가장 유리).
  B. **회사(조직) > 팀. 2계층.** `orgId` 는 회사, 새 `teamId` 를 더한다.
     → 한 회사에 팀이 여럿인 구조를 지금부터 지원. 대신 화면·권한이 한 단계 복잡해진다.

**선택지 — (2) 프로젝트 소유·가시성:**
  가. **팀 소유 — 팀의 모든 프로젝트가 팀원 전원에게 보인다.** (리더 초안의 가정)
     → 단순하다. 담당 배정 화면이 필요 없다.
     → 대신 팀원이 **자기와 무관한 현장까지 목록에서 본다.** (다운로드는 오프라인 준비를
       누른 것만 받으므로 용량 문제는 아니다)
  나. **팀 소유 + 담당 배정** — 프로젝트마다 담당 팀원을 지정하고 그 사람에게만 보인다(팀장은 전부).
     → 현장이 많아지면 목록이 깔끔하다.
     → 배정 화면 + 배정 테이블 + "배정 안 된 프로젝트" 처리가 필요하다.

**추천:** **A + 가.** 사용자 원문에 회사 계층도 담당 배정도 등장하지 않는다. 가장 작은 모델로
시작하고, A 는 회사 계층이 필요해져도 **로컬 스키마를 안 건드리고** 서버만 늘리면 된다.
"가"에서 "나"로 가는 것도 나중에 필드 하나 추가로 가능하다(반대 방향은 어렵다).

**차단 여부:** 🔴 **차단.** 서버 테이블·RLS(T1-1)의 첫 줄이다.

---

## Q58 · plan-reviewer · 2026-09-02 · 🔴 삭제를 어떻게 전파하는가 — 지운 결함이 되살아난다

**막힌 지점:** 지금 **소프트 삭제(`deletedAt`)를 가진 것은 `Project` 뿐**이다.
`Building`·`Floor`·`Drawing`·`Defect`·`Photo`·`Memo` 는 전부 **하드 삭제**(스토어에서 사라진다).

동기화가 붙으면 이렇게 된다.

```
A 태블릿: 결함 #12 를 지운다 → 로컬에서 완전히 사라짐 → 동기화
B PC   : 아직 #12 를 갖고 있다 → 동기화 → 서버에 #12 를 다시 push
A 태블릿: pull → **지운 #12 가 되살아난다**
```
→ 지워도 지워지지 않는다. 사용자가 몇 번을 지워도 다음 동기화마다 돌아온다.

**왜 임의로 정하면 안 되는가:**
① 사용자가 눈으로 보는 동작이다(지운 것이 되살아나는 것은 명백한 오동작).
② **선택지에 따라 `DB_VERSION` 이 1 로 유지되느냐 2 로 올라가느냐가 갈린다** — 리더가 지목한
   핵심 리스크가 정확히 이 지점이다.
③ 되돌리기(Ctrl+Z)와 얽힌다. 결함 삭제는 되돌릴 수 있는 조작이라(`repo.ts` K13 주석) 삭제 즉시
   툼스톤을 확정하면 되돌렸을 때 툼스톤을 지워야 한다.

**선택지:**
  A. **모든 레코드에 `deletedAt` 을 추가하고 소프트 삭제로 전환한다.**
     → 가장 정석. 어디서 지웠든 정확히 전파된다.
     → 대가: **모든 목록 조회(`by_project` 인덱스)가 `deletedAt === null` 필터를 타야 한다**
       (파일 다수 수정). 성능을 위해 인덱스를 새로 잡으면 **`DB_VERSION` 이 2 로 올라가고
       마이그레이션이 생긴다** — "DB 버전 1 유지" 규칙과 정면 충돌.
  B. **삭제 로그를 `meta` KV 에 따로 쌓는다.** `deleted:{projectId}` = `[{kind,id,at,deviceId}, …]`
     → **스토어·인덱스·`DB_VERSION` 이 전혀 안 바뀐다**(`exportRun:` · `offline:` 과 같은 수법).
       레코드 본체는 지금처럼 하드 삭제라 조회 코드가 한 줄도 안 바뀐다.
       되돌리기(Ctrl+Z)로 살아나면 그 항목만 로그에서 뺀다.
     → 대가: 로그가 계속 쌓인다(동기화 완료 + 90일 지난 항목은 정리). 두 곳(본체·로그)을
       맞춰야 하는 자리가 생긴다.
  C. **프로젝트 통째 스냅샷 교체 — 마지막에 올린 기기가 전부 이긴다.**
     → 구현이 제일 쉽다.
     → **두 사람이 같은 프로젝트를 동시에 작업하면 한쪽 작업이 통째로 사라진다.**
       팀 동기화의 목적 자체를 부순다. 추천하지 않는다.

**추천:** **B.** `DB_VERSION` 1 유지 규칙을 지키면서 삭제를 정확히 전파하는 유일한 안이고,
이 프로젝트가 이미 세 번(ExportRun · 오프라인번들 · 마지막 뷰포트) 쓴 검증된 수법이다.
A 는 옳지만 "인덱스를 안 잡으면 느리고, 잡으면 DB 버전이 올라간다"는 딜레마가 남는다.

**차단 여부:** 🔴 **차단.** T1-3·T1-7·T1-8 이 전부 여기 걸리고, 로컬 스키마 유지 여부가 갈린다.

---

## Q59 · plan-reviewer · 2026-09-02 · 한 태블릿을 여러 사람이 쓸 때 로컬 데이터를 어떻게 하는가 🔴

**막힌 지점:** 지금 IndexedDB 는 **브라우저 1개당 1벌**이고 그 안에 **모든 용역**이 들어 있다.
로그인이 생기면 다음이 정해져 있지 않다.

- 팀원 A 가 로그아웃하고 팀원 B 가 같은 태블릿에 로그인하면 → **B 가 A 의 현장 데이터를 그대로 본다**
- 로그아웃할 때 로컬 데이터를 지우면 → **아직 동기화 안 한 현장 데이터가 그 자리에서 증발한다**

**왜 임의로 정하면 안 되는가:** 잘못 고르면 **현장에서 하루 종일 찍은 결함이 사라진다.**
이 프로젝트에서 가장 비싼 사고다. 반대로 안 지우면 남의 데이터가 다른 사람 기기에 남는다.

**선택지:**
  A. **로그아웃 버튼을 만들지 않는다 — 1기기 = 1사용자.**
     계정을 바꾸려면 `설정 → 로컬 데이터 초기화`(이미 있는 2단 확인 버튼)를 거친다.
     → 실수로 데이터가 날아갈 경로가 없다. 구현이 0 에 가깝다.
     → 태블릿을 돌려 쓰는 운영이면 불편하다.
  B. **로그아웃은 있되 로컬 데이터는 남긴다.** 다음 사람이 로그인하면 이전 사람 데이터가 보인다.
     → 데이터 유실이 없다. → **팀 내부라 보안 문제는 작지만**, 누가 만든 건지 헷갈린다.
  C. **로그아웃 시 미전송 변경이 있으면 막고, 없으면 로컬을 전부 지운다.**
     `먼저 동기화해야 합니다 — 미전송 37건` 을 띄우고 로그아웃을 거부한다.
     → 유실도 없고 데이터도 안 섞인다. 가장 안전하다.
     → **Q56(미전송 집계 가능해짐)과 Q58(삭제 로그)이 먼저 풀려야 구현된다.** 의존이 있다.

**추천:** **A 로 시작해 C 로 간다.** 1차에는 로그아웃 버튼을 아예 안 만들고(A), 동기화가 안정된
뒤에 C 를 붙인다. B 는 "누구 데이터인지 모르는 태블릿"을 만들어서 나중에 반드시 문제가 된다.

**차단 여부:** 🔴 **차단.** 로그인 화면(T1-5·T1-6)의 기본 구조가 갈린다.
다만 A 로 시작하면 사실상 만들 것이 없어 **가장 빨리 푸는 답**이기도 하다.

---

## Q60 · plan-reviewer · 2026-09-02 · 사진 **원본**까지 서버에 올릴 것인가 (용량·비용)

**막힌 지점:** 사진 1장은 Blob 3종(원본 · 장변 2048 렌더본 · 320 썸네일)으로 저장된다.
서버에 무엇까지 올릴지가 안 정해졌다. 결함당 2~4장 × 수백 결함 = **현장 1개가 수 GB** 다.

**선택지:**
  A. **렌더본 + 썸네일만 올린다.** 원본은 찍은 기기에만 남는다.
     → 용량이 **1/3~1/5** 로 준다. 보고서 산출물은 렌더본으로 만들므로 **출력물 품질은 동일**하다.
     → 원본은 그 태블릿을 잃어버리면 사라진다. 자르기를 나중에 크게 다시 하면 화질이 아쉬울 수 있다.
  B. **원본까지 전부 올린다.**
     → §2-C "원본 보존" 원칙에 충실. 기기를 잃어도 안전.
     → Supabase Storage 요금이 그만큼 는다(무료 1GB → 유료 구간).

**추천:** **A 로 시작.** 산출물 품질이 같고 비용·시간이 크게 준다. 원본 백업이 필요하면
`[원본도 올리기]` 체크박스를 나중에 붙이면 된다(필드 추가 없이 업로드 대상만 늘리면 끝).

**차단 여부:** **비차단.** A 를 가정하고 진행한다(`ASSUMPTIONS.md` V7). 되돌리는 비용 낮음.

---

## Q61 · plan-reviewer · 2026-09-02 · 매니지드 DB 벤더 — Supabase 최종 승인

**막힌 지점:** 리더가 Supabase 를 권장했고, plan-reviewer 가 Neon+자체auth 와 비교 검토했다.
**반박 근거를 찾지 못했다 — Supabase 가 명백히 낫다.**

결정타는 **Storage** 다. 이 앱은 수 GB 이미지가 본체인데 Neon 은 Postgres 만 준다 —
S3/R2 를 따로 붙이고 서명 URL 을 직접 구현해야 한다. Supabase 는 Storage + 서명 URL + RLS 가
한 벌로 들어 있고, "팀장이 아이디·비번 직접 발급"도 `auth.admin.createUser()` 한 줄이다.

**선택지:**
  A. **Supabase** (Postgres + Auth + Storage + RLS)
  B. Neon + 자체 auth + 별도 오브젝트 스토리지

**추천:** **A.** 비밀번호 해싱·세션 발급 같은 보안 코드를 직접 짜지 않는다는 기준(리더)에 부합하고,
이미지 저장소까지 한 번에 해결된다.

**차단 여부:** **비차단.** A 로 진행한다(`ASSUMPTIONS.md` V8). 다만 **계정 생성·요금제 선택은
사용자만 할 수 있다** — 착수 시점에 Supabase 프로젝트 URL·키가 필요하다.

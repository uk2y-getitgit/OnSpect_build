# 예약 실행 결과 — S4 + 수정사항 F1~F7 (2026-08-23)

`_workspace/13_AUTORUN_BRIEF.md` 지시로 사람 없이 진행했다. 판단마다 근거를
`_workspace/14_AUTORUN_DECISIONS.md`(AD1~AD22)에 남겼다. 이 문서는 그 요약이다.

**작업 순서 권고(F3+F6 → F4 → S4 → F2 → F1 → F7 → F5) 그대로 진행했고, F5 앞에서 멈췄다.**
7개 항목 중 5개(F3+F6·F4·S4·F7)를 완전히, 2개(F2·F1)를 부분적으로 끝냈다.
전부 로컬 커밋으로 남아 있다 — **단, GitHub 푸시는 막혀 있다 (아래 §0 참조, 가장 먼저 읽을 것).**

---

## §0. 🔴 가장 먼저 확인할 것 — GitHub 푸시가 막혀 있다

`git push` 와 GitHub MCP 쓰기(`push_files`) 둘 다 403 으로 거부됐다:
- `git push`: *"Claude doesn't have GitHub access to uk2y-getitgit/OnSpect_build for your organization"*
- MCP 쓰기: *"Resource not accessible by integration"*

읽기는 되는데 **쓰기 권한이 없다.** 이 세션의 모든 커밋(아래 8개)이 **로컬에만** 있고
원격(GitHub)에는 하나도 올라가지 않았다. 컨테이너가 회수되면 사라질 수 있다.

**조치 필요:** 조직 관리자가 https://github.com/apps/claude/installations/select_target
에서 Claude GitHub App 을 이 저장소에 **쓰기 권한**으로 설치하거나, claude.ai 설정에서
GitHub 커넥터를 재연결해야 한다. 그 전까지는 이 컨테이너(세션)에 다시 접속해
`git push origin main` 을 수동으로 실행하거나, 로컬 커밋을 다른 방법으로 꺼내야 한다.

근거: `_workspace/14_AUTORUN_DECISIONS.md` **AD4**.

---

## 완료한 것

### 1. F3 — 우측 패널 좌표 숨김 ✅
`Inspector.tsx` 의 "도면 위치" 섹션(마크·번호풍선 정규화 좌표) 제거.

### 2. F6 — 설정 메뉴 뷰포트 잘림 ✅
`MoreMenu`(⋯ 메뉴)를 `document.body` 포털 + 실측 좌표(`position: fixed`)로 전환.
뷰포트 하단/좌측을 넘으면 위/왼쪽으로 뒤집는다. 스크롤·리사이즈 시 닫는다.

### 3. F4 — 번호 풍선은 어떤 도구든 항상 잡힌다 ✅
ARROW·AREA_RECT·AREA_ELLIPSE·SKETCH 도구가 켜져 있어도 라벨(LABEL) 히트는
도구의 생성 동작보다 먼저 처리된다. 다른 표기 위에서는 기존 동작(도구가 이긴다) 유지.

### 4. S4 — 결함정보 입력 폼 (이번 실행의 본 작업) ✅
- `project-core/items/size.ts` — 면적 계산(§3-6 실측 4행 고정, 개소 미곱)
- `project-core/items/presets.ts` — 폭은 **D7 단일값 버튼**(구간 표기 아님)
- `project-core/items/apply.ts` — 구조유형/부재/결함유형/규모모드 연동 규칙(순수 함수)
- `ui/defectForm/` — ChoiceGrid · SegmentField · NumberField · SizeBlock · DefectInfoForm
  (전부 캔버스·store·repo 를 import 하지 않는다, `DefectAttrs` 는 타입만)
- `routes/settings/PreviewTab.tsx` — 입력 미리보기 + 손상결함표 1행 미리보기, 저장 안 함
- 설정 좌측 내비의 "입력 미리보기"를 활성화했다(S3 때 `준비 중`으로 비워 둔 자리)

### 5. F2 — 화살표 클릭 생성 (부분) ⚠️
화살표를 드래그 생성에서 **점과 동일한 클릭 생성**으로 바꿨다. 기본 방향/길이로 생기고
머리(TO) 핸들을 끌어 나중에 조정한다.
**그리기 사후선택·메모 필기화는 하지 않았다** — §못 한 것 참조.

### 6. F1 — 새 도면 A4 정규화 (부분) ⚠️
`project-core/a4.ts::calcFitRect` (Numdraw 실측 배치 공식 이식) + 새 업로드를
A4 가로 캔버스(1754×1240)에 흰 배경 + contain 배치로 합성. `Drawing.imgLayout` 추가
(F5-4 도면 영역과 동일 — F5-4 는 이걸로 이미 끝났다).
**기존 도면의 [A4로 맞추기] 재정규화 버튼은 하지 않았다** — §못 한 것 참조.

### 7. F7 — 전차 결함 승계 ✅
`Defect.prevDefectId` 필드 신설(상세기획에 정의만 있고 실제 타입엔 없었다).
`copyStructure()` 에 `includeDefects` 옵션 — 결함까지 원자적으로 복사하고 전부
`PREV_PENDING`(보라)으로 들어간다. 용역 만들기 화면에 체크박스 추가.
좌측 리스트가 "전회차 N건 · 금회차 N건"으로 갈린다(전회차가 있을 때만).

---

## 못 한 것 — 어디서 멈췄고 왜

| 항목 | 멈춘 지점 | 이유 | 관련 AD |
|---|---|---|---|
| **F2-2** 그리기 사후선택 | 착수 전 | "그린 후 결함 선택/새 결함으로"는 캔버스 상태 머신에 새 대기 상태(pendingSketch)가 필요한, S4 급 분량의 별도 기능. 설계 메모를 남겼다 | AD14 |
| **F2-3** 메모 필기화(손글씨) | 착수 전 | `Memo.text` → 자유곡선으로 바꾸는 **데이터 형식 변경**. IndexedDB DB버전은 유지해야 하는데 옛 텍스트 메모와 새 손글씨 메모가 한 스토어에 공존해야 한다. 렌더·히트·기존 메모 테스트 10여 개를 다시 설계해야 해서 사람 확인 없이 굳히지 않았다 | AD14 |
| **F1 재정규화** 버튼([A4로 맞추기]) | 착수 전 | 기존 도면을 A4 로 맞추려면 그 도면의 **모든 결함 마크·라벨·스케치·메모 좌표**를 일괄 재계산해야 한다. 변환식은 적어 뒀다. 실사용자 데이터를 일괄 변형하는 조작이라 확인 없이 자동 실행하지 않았다 | AD15 |
| **F5** 도곽·범례 | 착수 전 | F5-4(도면 영역)는 F1 작업으로 이미 끝났다. F5-2(범례)는 "결함유형별 색"이라는, 지금 데이터 모델·디자인시스템에 없는 새 색 축을 전제해서 **Q28 로 질문을 남겼다**(중립색+기호 방식을 추천). F5-1(도곽)은 색 문제는 없지만 이번에 처음 건드리는 캔버스 배경 렌더링 하부구조라 성능 회귀를 검수 없이 자신할 수 없었다 | AD22 |

---

## 검증한 것 — 실제로 통과시킨 것만

각 항목 완료 직후 `npm run typecheck && npm test && npm run build` 를 전부 통과시켰다.
**개발 서버·브라우저는 지시대로 띄우지 않았다.**

| 시점 | typecheck | 테스트 | 빌드 |
|---|---|---|---|
| F3+F6 이후 | ✅ | ✅ 209개 | ✅ |
| F4 이후 | ✅ | ✅ 215개(+6) | ✅ |
| S4 이후 | ✅ | ✅ 243개(+28) | ✅ |
| F2(부분) 이후 | ✅ | ✅ 247개(+5, 기존 3개는 새 동작에 맞춰 AREA_RECT 기준으로 수정) | ✅ |
| F1(부분) 이후 | ✅ | ✅ 256개(+9) | ✅ |
| F7 이후 | ✅ | ✅ 259개(+3) | ✅ |

기존 209개는 전부 그대로 통과했다(F2 항목에서 ARROW 드래그를 가정한 테스트 3개는
**의도된 동작 변경**에 맞춰 수정했다 — 되돌린 것이 아니라 새 스펙에 맞춘 것이다).

### 코드로 확인한 것 (§7-1 등가)
- `word-break: break-all` — 신규 코드 0건(grep)
- `DefectInfoForm.tsx` 등 `ui/defectForm/*` — `@onspect/canvas-core` 는 `import type` 한 줄뿐,
  `store`·`repo`·`canvas` 모듈 import 0건(grep)
- IndexedDB `DB_VERSION` — 1 유지, `migrations[]` 안 늘림, 새 스토어 0개
- 면적 계산 §3-6 실측 4행 — 단위테스트로 고정
- D7(폭 단일값 버튼), F19(즐겨찾기가 정렬을 안 바꿈) 등 기존 불변식 — 회귀 없음

### 코드로 확인할 수 없는 것 (사용자 몫)
- 화면 실제 조작감 전부 — 아래 체크리스트 참조

---

## 직접 확인해주실 것

### GitHub (가장 급함)
0. **§0 의 GitHub 쓰기 권한 문제를 먼저 조치해 주세요.** 안 하면 이 8개 커밋이 원격에
   없는 채로 남습니다.

### F3 · F6
1. 결함 선택 시 우측 패널에 "도면 위치" 섹션이 더 이상 안 보이는가
2. 동/층/도면 목록에서 `⋯` 메뉴를 목록 **맨 아래 행**에서 눌러도 메뉴가 화면 밖으로
   안 잘리고 위로 뒤집히는가
3. 메뉴를 연 채로 목록을 스크롤하면 메뉴가 자동으로 닫히는가 (부자연스럽지 않은가)

### F4
4. 화살표·영역·그리기·메모 도구가 켜진 상태에서도 번호 풍선을 클릭해 끌 수 있는가
5. 풍선 위에서 커서가 `move` 로 바뀌는가 (도구와 무관하게)

### S4 (본 작업 — 가장 꼼꼼히 봐주실 곳)
6. 설정 → **입력 미리보기** 탭이 열리는가. 구조유형 → 부재 → 결함유형 순서로 고르면
   아래 손상결함표 미리보기 1행이 즉시 바뀌는가
7. 부재를 바꾸면 "구조체 여부"가 새 부재 기본값으로 되돌아가는가 (직접 지정했던 것도)
8. 결함유형을 `망상균열`(면적 모드)로 고르면 폭·길이 대신 면적 입력으로 바뀌는가.
   다시 `수직균열`(폭×길이)로 돌아오면 이전에 넣었던 폭·길이 값이 그대로 복원되는가
9. 폭 입력에서 `[0.5 초과 → 직접입력]` 을 누르면 숫자 입력칸에 포커스가 가고 0.5 가
   채워지는가
10. AREA 모드에서 `[가로 × 세로로 계산]` 을 펼쳐 1000×500 을 넣으면 면적 칸에 0.5 로
    자동 계산되는가. 그 상태에서 면적을 직접 타이핑하면 가로·세로 칸이 비워지는가
    (의도된 동작 — AD13)
11. 부재·결함유형·발생원인·보수방안 그리드가 **기본은 접혀서** "선택 / 변경" 버튼만
    보이다가, 눌러야 펼쳐지는가. 이 접힘 동작이 자연스러운가 (AD11 — 확인 필요도 보통)
12. 300px 우측 패널 폭 기준으로 버튼 크기(최소 32px)가 태블릿 손가락으로 누를 만한가
    (AD12 — 확인 필요도 보통)

### F2 (화살표만 바뀜)
13. 방향(화살표) 도구로 도면을 한 번 클릭하면 결함이 즉시 생기고 화살표가 보이는가
14. 화살표 머리를 끌어서 방향을 바꿀 수 있는가

### F1 (새 업로드만)
15. 새 도면을 올리면 원본 크기·비율과 무관하게 항상 A4 가로 비율로 보이는가(여백 포함)
16. **가로로 아주 긴 이미지**(예: 파노라마)를 올렸을 때 위아래 여백이 과하게 크지
    않은가 — 극단적 종횡비에서 실제로 알아보기 쉬운지
17. 업로드 후보 목록에 뜨는 해상도 숫자는 원본 그대로인가(합성 후 A4 크기가 아니라)
18. **(알려진 한계, AD18)** `[샘플 용역 만들기]` 로 만든 데모 결함 위치가 도면
    그림 위에서 살짝 밀려 보일 수 있습니다 — 데모 데이터 한정이며 실사용 데이터에는
    영향이 없습니다

### F7
19. 용역 만들기에서 이전 용역을 고르고 "동·층·도면 복사"를 켜면 "결함도 함께
    가져오기" 체크박스가 나타나는가
20. 체크하고 저장하면 결함이 **보라색**(전회차)으로 도면에 미리 얹혀 있는가
21. 좌측 리스트 상단이 "전회차 N건 · 금회차 N건"으로 보이는가
22. 전회차 결함을 클릭하면 우측 패널에 "전회차 표기입니다. 선택만 가능하며 이동·삭제할
    수 없습니다" 안내가 뜨고 실제로 못 옮기는가

---

## 사용자가 뒤집을 만한 판단 (확인 필요도: 높음만 추림)

### AD4 · GitHub 푸시 인프라 문제
판단이 아니라 **막힌 인프라**다. §0 참조. 조직 관리자 조치가 필요하다.

### AD15 · F1 — 기존 도면 재정규화([A4로 맞추기]) 를 미뤘다
기존 도면을 A4 로 맞추려면 그 도면의 모든 결함 좌표를 일괄 재계산해야 한다.
변환식(`newNorm = (imgLayout.off + oldNorm × imgLayout.d) / a4Size`)은 적어 뒀다.
**사용자가 원하면 이 변환을 실제 데이터로 검증하며 진행해야 한다** — 실사용자
데이터의 좌표를 바꾸는 조작이라 자동 실행하지 않았다.

### AD22 · F5 — 범례의 "결함유형별 색"을 정하지 않았다
`_workspace/QUESTIONS.md` **Q28** 로 남겼다. **B안(중립색 + 문자 기호)을 추천**하지만
Numdraw 실측 화면이 색 샘플 방식이었다면 사용자 기대와 다를 수 있다. 답이 오면
F5-1(도곽)·F5-2(범례)를 이어서 진행할 수 있다.

---

## 다음 실행이 이어받을 것

1. **F5 착수** — `_workspace/QUESTIONS.md` Q28 답변 확인 후 F5-2 진행. F5-3(도면 크기
   상수)부터 먼저 해도 된다(질문과 무관).
2. **F2 나머지** — 그리기 사후선택(설계 메모 AD14 참조), 메모 필기화(사용자 확인 먼저).
3. **F1 재정규화 버튼** — 변환식 AD15 참조. 실제 사용자 도면으로 검증하며 진행.
4. **GitHub 푸시 문제**가 안 풀렸다면 이 8개 커밋이 여전히 로컬에만 있을 것이다 —
   다음 세션 시작 시 `git log --oneline` 으로 먼저 확인할 것.

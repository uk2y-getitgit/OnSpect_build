# ASSUMPTIONS

비차단 질문에 대해 에이전트가 세운 가정. **최종 보고에 전부 포함해야 한다.**

---

## Phase 3 도면 캔버스 · plan-reviewer · 2026-08-22

전부 `_workspace/01_plan-reviewer_spec.md` 에 확정값으로 반영되어 있다.
QUESTIONS.md 의 답이 오면 해당 항목만 바꾸면 된다. **builder가 추측할 지점은 없다.**

### 질문과 연결된 가정 (Q2~Q8)

| # | 가정 | 관련 Q | 뒤집는 비용 |
|---|---|---|---|
| A1 | 리더선은 **1개**이고 `Label.anchorMarkId`(기본 `marks[0].id`)가 가리키는 마크에 붙는다. `null` 이면 마크 집합의 중심 | Q2 | 낮음 — 필드를 미리 정의해 마이그레이션 없음 |
| A2 | **마크를 옮기면 라벨이 같은 델타만큼 따라온다** (리더선 길이·각도 보존) | Q3 | 낮음 |
| A3 | **번호 풍선은 도면 좌표 고정** — 줌하면 함께 커진다(WYSIWYG). 단 히트 영역은 스크린 기준 최소 12px 보장 | Q4 | 중간 — 렌더·히트 양쪽 수정 |
| A4 | 각도 스냅은 **90도 배수 4방향만**. 45도 배수는 `ANGLE_SNAP_45 = false` 로 꺼둔다 | Q5 | 낮음 — 플래그 |
| A5 | 스냅 수치: 정렬 **진입 6px / 이탈 10px**, 각도 **진입 5도 / 이탈 8도**, 각도 스냅 **최대 이동 12px**, 최소 리더 길이 24px | Q5 | 낮음 — `constants.ts` 한 파일 |
| A6 | 정렬 vs 각도 충돌 시 **각도 우선** (`SNAP_PRIORITY = 'ANGLE_FIRST'`) | Q6 | 낮음 — 상수 |
| A7 | **마지막 마크 삭제 = 확인 후 결함 전체 삭제** (Undo 가능) | Q7 | 낮음 |
| A8 | 전회차 표기는 1차 범위에서 **선택만 가능** (이동·삭제 불가). 정렬 가이드 후보에는 포함 | Q8 | 낮음 |

### 질문하지 않고 정한 것 (기획서 범위 안에서 결정 가능하다고 판단)

| # | 가정 | 근거 |
|---|---|---|
| B1 | **캔버스가 그리는 숫자는 `Defect.seq`**(입력순번)다. `Label` 에 번호 필드를 두지 않고 `displayNumber: string` 을 렌더 시 주입한다 | 상세기획 §4 "출력번호·사진번호는 저장하지 않는다"(함정 #1). 주입식이면 Phase 4 출력 렌더가 같은 코드를 재사용한다 |
| B2 | **마크 좌표는 `[0,1]` 클램프, 라벨 좌표는 클램프하지 않는다**(소프트 리밋 `[-0.5, 1.5]`) | 젠트릭스 실측에서 번호 풍선이 도면 밖으로 끌려나간다. 마크는 결함의 실제 위치라 도면 밖이 무의미 |
| B3 | **라벨 위치 이동은 `defect.style` 을 절대 건드리지 않는다** | 위치는 geometry이지 style이 아니다. 건드리면 정리한 결함이 전부 전역 스타일 상속에서 이탈한다(함정 #5) |
| B4 | **각도·거리·정렬 판정은 전부 스크린 px 에서** 한다. 정규화 좌표에서 각도를 재지 않는다 | 종횡비가 1이 아닌 도면에서 정규화 공간의 45도는 화면상 45도가 아니다 |
| B5 | 정규화 좌표는 **소수 6자리 반올림** 저장 | 10,000px 도면에서 0.01px 정밀도. 동기화 시 부동소수 잡음 diff 방지 |
| B6 | **`Alt` = 스냅 일시 해제.** 그 대가로 캔버스에서 `Alt`+드래그 복제는 제공하지 않는다(복제는 툴바 버튼) | 두 기능이 같은 키를 다투지 않게 하기 위해 |
| B7 | 히트 우선순위 **라벨 > 마크 > 리더선 > 빈 도면** | 실수로 마크를 옮기면 보고서 데이터가 틀린다. 실수 비용이 싼 쪽을 앞에 둔다 |
| B8 | 빈 도면 좌드래그 = **팬**. 박스(다중) 선택은 이번 범위 밖 | D2 1차 범위에 다중 선택이 없다 |
| B9 | **뷰포트 변경은 Undo 대상이 아니다.** 편집(이동·생성·삭제)만 커맨드 스택에 쌓는다 | Ctrl+Z 가 화면 스크롤을 되돌리면 사용자가 혼란스럽다 |
| B10 | **뷰포트를 층(도면)마다 기억한다** (`Map<drawingId, Viewport>`) | §2.5 G6 "마지막 작업 위치로 복귀"의 최소 형태. 비용이 거의 없다 |
| B11 | `REPAIRED` 결함의 도면 표현 = **회색 + 불투명도 40%** | 상세기획에 정의가 없다(누락). 리스트의 "취소선"에 대응하는 도면 표현 |
| B12 | 리더선은 **단일 직선 1개**. 꺾인선(elbow) 없음. 풍선 테두리에서 멈춘다 | 젠트릭스 실측 리더선이 전부 직선 |
| B13 | 가이드선 색 **`#00B8D9`(시안)** | 표기가 빨강(현회차)/보라(전회차)라 같은 계열을 쓰면 결함 표기로 오독된다 |
| B14 | 라벨 자동 배치 = 마크에서 **우상단 45도**, 거리 = 풍선 반지름 × 3. 사용자가 한 번 끌면 `placed = true` | |
| B15 | **2레이어 캔버스** — 배경(도면)은 뷰포트 변경 시에만, 오버레이(표기)는 매 프레임. 뷰 컬링 적용 | 대형 도면에서 드래그 프레임 유지 |
| B16 | 캔버스 코어는 `DrawOp[]`(직렬화 가능한 그리기 명령)만 만들고 **실제 그리기는 하지 않는다** | D1 "코어는 플랫폼 독립" + Phase 4 출력 렌더 재사용 |
| B17 | 도구는 `SELECT` / `POINT` 둘뿐. 방향·영역·그리기는 팔레트에 **비활성 버튼으로 자리만** 잡는다 | D2 1차 범위. 자리를 잡아두면 나중에 레이아웃이 안 바뀐다 |
| B18 | PDF→이미지 변환은 이번 범위 밖. 캔버스는 **렌더이미지(래스터)와 width/height 숫자만** 받는다 | 상세기획 §3-2 `Drawing.렌더이미지` 가 이미 있다. 변환은 §2.5 G2 과업 셋업의 일 |
| B19 | 간격 균등 배분(distribution) 가이드는 **이번 범위 제외** | 복잡도 대비 효용이 낮고 D2에 없다 |
| B20 | 미니맵·최근 위치 점프(§2.5 G6)는 **이번 범위 제외** | D2 1차 범위에 없다 |

### 보류 (질문하지 않음)

| 항목 | 이유 |
|---|---|
| 상세기획 §7 미결 **Q6** — 도면 확대 상태 정밀 표기(핀치줌·크로스헤어·롱프레스) | **모바일 전용 사안.** D1이 PC 웹 우선이므로 이번 범위에서 필요 없다 |
| 도면 밖으로 나간 번호 풍선을 출력물에 어떻게 담는가 | **Phase 4(산출물)의 문제.** 여기서 정하면 출력 양식을 앞질러 결정하게 된다 |

---

## Phase 3 구현 · builder · 2026-08-22

스펙(`01_plan-reviewer_spec.md`)과 A1~A8·B1~B20 은 **전부 그대로 구현**했다.
아래는 구현 중 스펙에 정의가 없어 새로 세운 가정이며 전부 **비차단**이고
`QUESTIONS.md` Q9~Q12 에 질문으로 남겼다.

| # | 가정 | 관련 Q | 뒤집는 비용 |
|---|---|---|---|
| C1 | 우측 결함정보 패널은 **읽기 전용 표시 + 캔버스 조작(번호 위치 초기화·삭제)** 만 둔다. 결함 속성 입력 폼은 Phase 2 조사입력의 영역이므로 여기서 두 벌로 만들지 않는다 | Q9 | 낮음 — `ui/Inspector.tsx` 한 파일 |
| C2 | `POINT` 도구에서 **기존 표기 위를 클릭하면 생성이 아니라 선택**한다. 빈 도면일 때만 생성한다 | Q10 | 낮음 — `interaction.ts` 의 `onPointerDown` 분기 하나 |
| C3 | 전회차 표기(이동 불가) 위의 커서는 `move` 가 아니라 **`pointer`**. "선택은 되지만 이동은 안 된다" 를 조작 전에 알려준다 | Q11 | 낮음 — `computeCursor()` |
| C4 | 창 크기 변경(RESIZE) 시 뷰포트는 **유지**한다(팬 클램프만). 최초 진입·층 최초 표시에만 Fit | Q12 | 낮음 — `reduce()` 의 RESIZE 분기 |
| C5 | 도면 밖(Fit 여백)을 POINT 도구로 클릭하면 결함을 만들지 않고 `도면 안쪽을 클릭해 주세요` 토스트를 띄운다. 마크는 `[0,1]` 클램프 대상이므로 도면 밖은 결함의 실제 위치가 될 수 없다 | Q10 | 낮음 |
| C6 | **미완성 여부는 저장하지 않고 파생한다.** `isIncomplete(defect) = 부재 또는 결함유형이 비어 있음`. `DRAFT` 플래그 컬럼을 만들지 않았다 (D3 의 "플래그 또는 파생" 중 파생 선택) | D3 | 낮음 — `completeness.ts` 한 파일. 플래그가 필요하면 필드만 추가 |
| C7 | 툴 팔레트에 `선택` 버튼을 하나 더 둔다(스펙의 점/방향/영역/그리기 4개 위에 구분선으로 분리). `tool: 'SELECT' \| 'POINT'` 상태 모델상 POINT 를 끌 수단이 필요하다 | — | 낮음 |
| C8 | 도면 렌더 이미지는 로드 시 **한 번 래스터화**해서 쓴다(장변 상한 4096, 최대 2배). 실제 파이프라인이 PDF→래스터(B18)이므로 그 조건을 재현하고, 브라우저별 SVG 재래스터화 차이를 없앤다 | B18 | 낮음 — `imageLoader.ts` |
| C9 | 전역 스타일 기본값(마크 반지름 9, 풍선 반지름 34, 리더 굵기 2.4 — 전부 **이미지 px**)은 `constants.ts` 의 `RENDER_DEFAULTS` 에 모았다. 스펙 §2-10 표에 없던 값이라 새로 정했다 | — | 낮음 — 한 파일 |
| C10 | 토스트의 `[되돌리기]` 는 **가장 최근 토스트에만** 남긴다. 옛 토스트의 버튼이 마지막 조작을 취소해 사용자를 속이는 것을 막는다 | — | 낮음 |
| C11 | 요소(라벨·마크) 드래그 중에는 **휠 줌을 막는다.** 정렬 스냅 후보를 드래그 시작 시 스크린 좌표로 스냅샷하므로(§2-6-c) 드래그 중 뷰포트가 바뀌면 스냅샷이 무효가 된다 | §2-6-c | 낮음 |
| C12 | 화면 폭 820px 이하에서는 좌측 패널을 자동으로 접는다. PC 웹 기준이라 예외 상황이며 캔버스를 우선한다 | — | 낮음 — CSS |

---

## S1 용역·동·층·도면 · plan-reviewer · 2026-08-22

전부 `_workspace/05_plan-reviewer_spec_S1.md` 에 확정값으로 반영되어 있다.
**접두사 `D`** 는 이 라운드의 가정 번호이며, `DECISIONS.md` 의 D1~D5(사용자 결정)와는 다른 계열이다.
혼동을 피하려면 **`ASSUMPTIONS S1·D3`** 처럼 표기한다.

### 질문과 연결된 가정 (Q13~Q15)

| # | 가정 | 관련 Q | 뒤집는 비용 |
|---|---|---|---|
| D1 | 도면 업로드는 **이미지 전용**(PNG/JPG/WEBP/SVG). PDF 는 인식해서 **명시적으로 거절**하고 후속(S1-T13)으로 미룬다 | Q13 | **낮음** — 인입 파이프라인을 `파일 → PageCandidate[] → 층 배정 → Drawing[]` 로 잘라 뒷단을 공유하게 했고, `Drawing.source` 에 `PDF_PAGE` 유니온을 미리 정의했다. 모듈 1개 추가로 끝난다 |
| D2 | 점검구분은 **3종**(`REGULAR`/`DETAILED`/`PRECISE`). 상세기획 §3-1 의 `기타` 는 넣지 않는다 | Q14 | 낮음 — enum 1개 + 표시명 매핑 |
| D3 | §3-1 의 `발주처`·`점검기간`·`구조유형 기본값` 은 **모델에만 예약**(`null`)하고 S1 폼에 넣지 않는다 | Q14 | 낮음 — 필드가 이미 있으므로 폼만 추가 |
| D4 | **층당 도면 1장**(UI 제약). 모델은 `Drawing.floorId` + `sortOrder` 로 1:N 을 유지한다 | Q15 | 중간 — 좌측 트리가 `동▸층▸도면` 3단이 되고 뷰포트 기억 키가 바뀐다 |

### 질문하지 않고 정한 것

| # | 가정 | 근거 |
|---|---|---|
| D5 | **코드·데이터 식별자는 `Project`, UI 표기는 전부 "용역"** 이다. i18n 레이어를 도입하지 않고 한국어 문자열을 컴포넌트에 직접 쓴다 | 상세기획 §3-1 이 `Project` 이고 canvas-core 가 이미 `projectId`·`floorId` 어휘를 쓴다. 새 영문 어휘를 만들면 두 계통이 공존한다 |
| D6 | **표시명은 저장하지 않고 파생한다.** `{연도} {시기} {용역명} {구분}`, 반각 공백 1개 | 계산할 수 있는 것은 저장하지 않는다(함정 #1 과 같은 원리). 표기 규칙이 바뀌면 기존 용역이 옛 형식으로 굳는다 |
| D7 | **층 `sortOrder` 는 `STEP=10` 격자.** 지하 `-n×10`, 지상 `n×10`, 옥탑 `9000`, 옥상 `8000`, PIT `-9000`, 파싱 실패는 목록 끝. **부호 규약(지하=음수)은 유지** | 상세기획 §3-2 의 `-4 / 1 / 99` 로는 층 사이에 층을 끼울 수 없다. 실무의 중간층·구역분할을 못 받는다 |
| D8 | **중간층·중이층 같은 애매한 이름은 파싱하지 않는다.** 목록 끝에 넣고 사용자가 드래그로 제자리에 놓는다. **드래그가 최종 권한**이고 부호 규약과 모순되면 사용자 순서가 이긴다 | 잘못 해석하면 출력 순서가 조용히 틀린다. 조용한 오류가 가장 나쁘다 |
| D9 | **도면 교체 시 결함 좌표를 유지한다.** `Defect.drawingId` 만 갱신하고 `Mark`·`Label` 좌표는 손대지 않으며, 확인 문구로 사용자에게 약속한다 | 정규화 좌표(함정 #2)의 최대 효용이 바로 이 지점이다. 상세기획에 정의가 없다 |
| D10 | **용역 삭제는 소프트 삭제**(`deletedAt`) + 10초 되돌리기 토스트. **동·층 삭제는 하드 삭제** + 하위 건수를 숫자로 명시한 확인 다이얼로그 | 되돌릴 수 없는 데이터 손실인데 서버 백업이 없다. 다만 휴지통 화면까지 만들면 S1 이 부푼다 |
| D11 | **구조 삭제는 캔버스 Undo 스택에 넣지 않는다.** 토스트 되돌리기와 캔버스 Ctrl+Z 는 다른 것이다 | 도면 위 편집과 구조 변경을 한 스택에 얹으면 두 화면의 되돌리기 의미가 뒤섞인다 |
| D12 | **해시 라우터를 직접 구현한다**(약 60줄). react-router 를 추가하지 않는다 | 뒤로가기·새로고침 복원·딥링크만 필요하다. 새 의존성으로 번들과 개념을 늘릴 이유가 없다 |
| D13 | **`packages/project-core` 를 신설**하고 `canvas-core` 와 **서로 import 하지 않는다.** 잇는 것은 `apps/web` 의 책임 | D1(코어는 플랫폼 독립)과 같은 이유. 두 코어가 서로를 알면 RN 재사용 단위가 하나로 뭉친다 |
| D14 | 기존 **픽스처 8건을 지우지 않고 `sampleProject.ts` 로 이사**시켜 `[샘플 용역 만들기]` 에서만 시딩한다. 좌표·수치·상태를 한 글자도 바꾸지 않는다 | Phase 3 스냅·히트·상태색을 검증하는 유일한 회귀 자산이다. 동시에 빈 목록에서 즉시 테스트할 수단이 된다 |

### 사용자 결정으로 확정된 것 (가정 아님)

| 출처 | 내용 | 스펙 반영 |
|---|---|---|
| **DECISIONS D5** | 브라우저 IndexedDB 영속 저장. 도면 원본 Blob 함께 보관. 동기화·병합·큐는 만들지 않는다. Phase 5 가 이어받을 스키마 | §2-9 전체. Blob 3종 필수화(§2-4), `RecordBase` 공통 필드(§2-9-c), 빈 스토어 예약(§2-9-b) |
| **DECISIONS D4** | 실행 순서 `S1→S2a→S3→S4→S2b→S5` | §2-9-b 에 `memos`(S2a)·`itemSettings`(S3·S4)·`photos`(S5) 스토어를 v1 에서 함께 생성 |

### 보류 (질문하지 않음)

| 항목 | 이유 |
|---|---|
| 용역 내보내기/가져오기(백업) | S1 범위 밖. Phase 5 소관. 대신 "이 브라우저에만 저장됩니다" 고지를 상시 노출한다 |
| `ExportRun` 스토어 | Phase 4 소관. 출력 파라미터 형태가 미확정이라 지금 인덱스를 잡으면 잘못 굳는다 |
| 결함 승계 · 전회차 상태 3종 | Phase 2-D 소관. S1 의 구조 복사는 **동·층·도면까지만** 하고 결함을 건드리지 않는다 |
| §G3 오프라인 반출 | 모바일 전용 전제조건. D1 이 PC 웹 우선 |
| 조직 · 사용자 · 권한 | Phase 5. `Project.orgId`·`createdBy` 는 `null` 로 예약만 |

---

## S2a — 마커 도구 3종 · 메모 (builder, 2026-08-22)

### 질문과 연결된 가정 (Q16)

| # | 가정 | 근거 | 되돌리는 비용 |
|---|---|---|---|
| **E1** | **자유그리기는 선택된 결함에 붙는다.** 선택이 없으면 만들지 않고 안내 토스트를 띄운다 | 상세기획 §3-3 `marks: 1개 이상`. 스케치만으로는 유효한 결함이 아니다 | 낮음 — `interaction.ts::commitCreateSketch` 한 함수 |

### 질문하지 않고 정한 것 (스펙 범위 안에서 결정 가능)

| # | 결정 | 근거 |
|---|---|---|
| **E2** | `MarkGeometry` 를 스펙 §S2a-1 표대로 바꿨다. `ARROW: {from,to}` (기존 `x1,y1,x2,y2`), `AREA_ELLIPSE: {x,y,w,h}` 외접 사각형 (기존 `cx,cy,rx,ry`) | 스펙 §S2a-1 이 명시적으로 "geometry 를 타입별로 확정한다" 고 했다. 두 타입 모두 Phase 3 에서 **예약만 되고 한 건도 저장되지 않았다** — 마이그레이션 대상이 없다. 외접 사각형으로 통일하면 사각/타원의 리사이즈·해치·히트 코드를 그대로 공유한다 |
| **E3** | **리더선 히트 순위는 `자유그리기 선` 과 `메모` 사이**에 둔다 | 스펙 §S2a-3 의 1~8번 목록에 리더선이 빠져 있다. 기존 순위(`라벨 > 마크 > 리더선`)의 정신 = "얇은 것이 큰 것을 이긴다" 를 지키되, 메모가 결함의 리더선을 가로채지는 않게 했다 |
| **E4** | `Memo` 에 `drawingId` 를 넣는다 (스펙 표에는 `floorId` 만 있다) | S1 이 만들어 둔 IndexedDB `memos` 스토어에 `by_drawing` 인덱스가 **이미 존재한다.** 캔버스는 도면 단위로 그리므로 이 인덱스가 없으면 층의 모든 메모를 훑어야 한다 |
| **E5** | `SketchPath` 에 `id` 를 넣는다 (스펙은 `{points, width}`) | 히트 테스트 대상 지정·Undo 대상 지정·이동 커맨드가 전부 특정 획 하나를 가리켜야 한다. 배열 인덱스로 가리키면 다른 획이 지워질 때 어긋난다 |
| **E6** | 영역 **사각/타원 선택**은 팔레트 `영역` 버튼 아래 펼쳐지는 모양 줄로 고른다 | 스펙 §S2a-1 이 두 타입을 모두 요구하지만 팔레트 버튼은 `영역` 하나다. 버튼을 6개로 늘리면 세로 팔레트가 도면 가장자리를 더 먹는다 |
| **E7** | 컨텍스트 툴바 색상 팔레트에서 **결함 상태색(빨강·보라·회색)·가이드 시안·선택 파랑을 뺐다** | 디자인시스템 §3 예약. 상태색을 손으로 고를 수 있으면 "빨강인데 전회차" 같은 거짓 표기가 만들어진다 |
| **E8** | 메모 색 = 포스트잇 노랑 (`#fff4c2` / `#c9a227` / `#4a3a05`) | 스펙 §S2a-6 "중립 색 또는 노란 포스트잇 계열, 결함 상태색 금지, 가이드 시안과도 겹치지 않게". 단위 테스트로 예약색 회피를 고정했다 |
| **E9** | 메모 줄바꿈은 **글자 수 근사**로 한다 (`MEMO_CHARS_PER_LINE = 13`) | `canvas-core` 는 `document` 를 참조할 수 없어(경계 규칙 1) 텍스트를 실측할 수 없다. 렌더와 히트가 **같은 근사값**을 쓰므로 어긋나지 않는다 |
| **E10** | 구름(revision cloud) 테두리와 45° 해치를 **폴리라인·선분으로 근사**한다. `DrawOp` 에 호·패턴·클립 프리미티브를 추가하지 않는다 | 어댑터(2D · 나중의 Skia · Phase 4 출력)가 전부 폴리라인만 알면 되게 둔다. 프리미티브를 늘리면 어댑터마다 다시 구현해야 하고 출력이 화면과 어긋난다 |
| **E11** | 저장된 옛 결함의 `sketch` 누락은 **읽기 정규화**로 채운다 (`repo.ts::normalizeDefect`). DB 버전을 올리지 않는다 | S1 레코드에는 `sketch` 가 없다. 스키마 버전을 올리면 마이그레이션 경로가 하나 늘고, 저장은 어차피 다음 수정 때 새 형식으로 나간다 |
| **E12** | `undo`/`redo` 가 `Doc = {defects, memos}` 를 받도록 시그니처를 바꿨다 | 사용자에게 Ctrl+Z 는 하나다. 스택을 둘로 나누면 "메모를 되돌리려면 다른 키" 가 되어 버린다 |

### 보류 (S2a 범위 밖)

| 항목 | 이유 |
|---|---|
| 출력 시 자유그리기·메모 ON/OFF | Phase 4. 스펙 §S2a-7 이 명시적으로 제외 |
| 결함정보 입력 패널 연결 | S2b |
| 마크 그룹 편집(`복제`·`추가` 버튼) | 스펙 §S2a-7. 데이터 구조는 이미 지원하고 UI 만 비활성 |
| 자유그리기 점 단위 편집 | 스펙 §S2a-4 가 "전체 이동만" 으로 못박음 |
| 메모 스타일 편집 UI | 스펙에 `style?` 만 있고 조작이 정의되지 않았다. 타입과 렌더 경로만 뚫어 둠 |

---

## S3 설정 화면 · S4 결함정보 필드 · plan-reviewer · 2026-08-23

스펙: `_workspace/10_plan-reviewer_spec_S3_S4.md`

### 질문과 연결된 가정 (Q17~Q23)

| # | 가정 | 질문 | 되돌리는 비용 |
|---|---|---|---|
| **F1** | **설정 화면은 "이 용역의 설정"만 편집한다.** 전역(`ORG`) 문서는 새 용역의 초기값 역할만 하고, `[이 설정을 기본값으로 저장]` 으로만 갱신된다. 전역 전용 편집 화면을 만들지 않는다 | Q17 | **낮음** — 데이터 모델은 A·B·C 안이 전부 동일하다(문서 2종). 화면은 `scope` 프롭 하나 |
| **F2** | 상세기획 §2-A 의 *"이름을 고치면 전 프로젝트에 한 번에 반영"* 은 **이후 만드는 용역에 한정**으로 좁혀 읽는다. 이미 스냅샷을 가진 용역에는 반영되지 않는다 | Q17 | 낮음 — F1 과 한 몸 |
| **F3** | **폭 구간 버튼은 구간 상한을 넣는다.** `~0.1미만`→0.1 · `0.1~0.2`→0.2 · `0.2~0.3`→0.3 · `0.3~0.5`→0.5. `0.5mm이상` 만 직접입력으로 전환하며 0.5 를 초기값으로 | Q18 | 낮음 — `items/presets.ts` 상수표 한 곳 |
| **F4** | AREA 를 **가로×세로**로 입력해도 손상결함표 `폭`·`길이` 열은 **`0 · 0`** 이다. 가로·세로는 `areaWMm`/`areaHMm` 로 저장만 해 둔다(재편집용) | Q19 | 낮음 — 저장은 이미 하므로 `outputSize()` 한 함수 |
| **F5** | 씨앗을 **넣는다.** 부재 17 · 결함유형 13 · 발생원인 4 · 보수방안 5 · 링크 전조합. 구조체 구분과 규모모드 기본값은 실측 + 실무 추정 | Q20 | 낮음 — `items/seed.ts` 표 한 곳. 전부 화면에서 수정 가능하고 기존 결함에 영향 없음 |
| **F6** | **`CauseMaster.code` 는 마스터 고정값**이다. 문서 내 유일, 새 항목은 `max+1`, 사용자가 직접 수정 가능. 보고서별 재부여는 Phase 4 출력 파라미터로 남긴다 | Q21 | 중간 — 재부여로 바꾸면 `ExportRun` 스냅샷이 함께 필요해진다(Phase 4 일감) |
| **F7** | **`누수여부`·`위치보조`·`개소` 를 폼에 넣는다.** 사용자 요구 목록에는 없지만 손상결함표 실측 열에 있다 | Q22 | 낮음 — 폼에서 빼는 것은 쉽다. 반대(나중에 추가)는 기존 결함이 전부 null 이 된다 |
| **F8** | **지연 스냅샷.** 기존 용역은 처음 열 때 `ensureProjectSettings()` 가 그 시점의 ORG 를 복사한다. 일괄 마이그레이션·DB 버전 업을 하지 않는다 | Q23 | 낮음 — 함수 하나 |

### 질문하지 않고 정한 것 (스펙 범위 안에서 결정 가능)

| # | 결정 | 근거 |
|---|---|---|
| **F9** | **`itemSettings` 스토어에 문서 단위로 저장한다** (레코드 1건 = 설정 한 벌). 행 단위로 쪼개지 않는다 | 스토어·인덱스가 S1 v1 에 이미 있어 **마이그레이션이 0** 이다. 설정은 항상 통째로 읽고 쓰며, 드래그 재정렬 한 번이 수십 링크를 동시에 바꾸므로 `put` 1회가 원자적이다 |
| **F10** | **문서 안은 정규화(마스터 배열 + 링크 배열)를 유지한다. 중첩하지 않는다** | 중첩하면 저장 형식에서 함정 #4 를 재현한다 — "건조수축"이 결함유형 수만큼 생긴다 |
| **F11** | **결함은 `id` 와 `name` 을 둘 다 저장한다** (`memberId`+`memberName`, `causeId`+`causeName` …) | 이름은 출력용 스냅샷(설정을 지워도 보고서가 안 깨진다), id 는 코드번호 조회·역참조용. 상세기획 §3-3 `ref \| text 둘 다 지원` 과 정합 |
| **F12** | **`DefectAttrs` 를 새 타입으로 선언하고 `Defect = {구조 필드} & DefectAttrs` 로 합성한다.** 저장 형태는 지금과 같은 플랫이라 기존 접근 코드가 전부 그대로 동작한다 | 필드가 7개에서 20개로 느는데 별도 스토어로 옮기면 마이그레이션이 걸린다. 합성은 비용 0 이면서 "canvas-core 는 이 묶음을 해석하지 않는다"는 경계를 타입으로 표현한다 |
| **F13** | **규모모드는 2종(WL/AREA) 그대로.** 사용자의 "넓이×높이"는 AREA 안의 **보조 계산기**로 흡수한다 | 손상결함표 열이 `폭·길이·면적·개소` 4개로 고정돼 있어 3번째 모드를 실을 자리가 없다. WL 과 가로×세로는 계산식이 완전히 같다 |
| **F14** | **WL 모드에서 `areaM2` 를 저장하지 않는다.** 표시·출력은 `(폭÷1000)×(길이÷1000)` 로 파생한다. AREA 모드의 면적만 저장값이다 | 함정 #1 과 같은 원리 — 계산할 수 있는 것을 저장하면 폭을 고쳤을 때 면적이 낡은 값으로 남는다 |
| **F15** | **모드를 전환해도 반대편 값을 지우지 않는다.** 되돌아오면 복원된다. 출력은 현재 `sizeMode` 만 본다 | 실수로 전환해 입력값을 잃는 것이 가장 나쁘다. 저장 필드가 이미 분리돼 있어 비용이 없다 |
| **F16** | **부재를 바꾸면 `structural`(구조체 여부)이 새 부재의 기본값으로 되돌아간다** (수동 지정 해제). 세그먼트 옆에 `부재 기본값 / 직접 지정` 라벨을 띄운다 | 부재가 바뀌면 이전 수동 선택의 근거가 사라진다. 조용히 틀린 값이 보고서에 남는 것이 더 나쁘다 |
| **F17** | 면적 절사는 **mm² 로 곱한 뒤 1e6 으로 나누고** `Math.trunc(m2 * 1e4 + 1e-9) / 1e4` 로 한다 | 먼저 나누면(0.0001 × 2.5) 부동소수 오차가 커져 `0.0006` 이 `0.0005` 로 절사될 수 있다. ε 없이는 실측 4행 중 일부가 틀린다 |
| **F18** | 필수항목(`completeness.ts`)을 **늘리지 않는다.** 부재·결함유형 2개 그대로 | 늘리면 D3 이 허용한 "점 찍고 나중에 채우기" 흐름이 막힌다 |
| **F19** | **즐겨찾기(★)는 정렬을 바꾸지 않는다.** 정렬은 `sortOrder` 하나뿐이고, ★ 는 "1단 그리드에 노출되는가"만 결정한다 | 즐겨찾기가 순서까지 바꾸면 드래그로 맞춘 순서가 무의미해진다 |
| **F20** | 카운트 표기를 **`항목 17 · ★ 17`** 로 두 숫자로 나눈다 (젠트릭스는 `자주 사용하는 항목 17` 한 줄) | 실측 화면에서 별표가 켜진 것은 1개인데 17로 표시된다. 한 숫자로 쓰면 무엇의 개수인지 읽히지 않는다 |
| **F21** | `[추가]` 시 **마스터에 같은 이름이 있으면 링크만 만들고** 안내한다 (`'건조수축' 은 이미 등록된 항목입니다. 이 결함유형에 연결했습니다.`) | **함정 #4 를 실제로 막는 지점.** 없으면 마스터가 결함유형 수만큼 생기고 코드번호가 흩어진다 |
| **F22** | 항목 삭제를 **"이 목록에서 빼기"(링크)** 와 **"마스터에서 완전히 삭제"** 로 UI 에서 분리하고, 후자는 사용처 건수를 숫자로 명시한 확인 다이얼로그로 막는다 | 마스터+연결 구조의 핵심이 여기서 사용자에게 드러난다. 구분하지 않으면 한 번의 삭제가 다른 구조유형·다른 결함유형까지 지운다 |
| **F23** | **S4 는 `Inspector.tsx` 를 건드리지 않는다.** 폼은 설정 화면의 `[입력 미리보기]` 에서 검증하고, 캔버스 연결은 S2b 가 한다 | 읽기 전용 패널과 편집 폼이 동시에 존재하는 반쪽 상태를 만들지 않는다. D4 순서를 지킨다 |
| **F24** | `DefectInfoForm` 은 `Defect` 가 아니라 **`DefectAttrs` 만** 받고, `onChange` 로 **다음 `DefectAttrs` 전체**를 올린다. 연동 규칙은 폼이 아니라 `project-core/items/apply.ts` 순수 함수가 계산한다 | 폼이 캔버스·store·repo 를 몰라야 RN 이 이 컴포넌트만 다시 그린다. 연동 규칙을 패치로 올리면 규칙이 호출자마다 흩어진다 |
| **F25** | 설정 라우트는 `#/p/:pid/settings`. 캔버스에서 들어오면 `?from=f/{floorId}` 로 복귀 지점을 남긴다 | 캔버스 → 설정 → 캔버스 왕복이 실제 사용 흐름이다. 복귀가 P3 로 튀면 층을 다시 찾아야 한다 |
| **F26** | 좌측 내비에 `프리셋` · `표시 스타일` · `시설물 사진 분류` 를 **비활성 `준비 중`** 으로 지금 배치한다 | 자리를 잡아 두면 나중에 레이아웃이 흔들리지 않는다 (Phase 3 툴 팔레트 B17 과 같은 판단) |
| **F27** | `[기본 항목 세트 다시 불러오기]` 는 **병합(없는 것만 추가)** 이며 덮어쓰기가 아니다 | 사용자가 고친 이름·순서·기본값을 되돌리면 그 버튼은 함정이 된다 |

### 보류 (S3·S4 범위 밖)

| 항목 | 이유 |
|---|---|
| 폼 → 결함 저장 배선 · `SET_DEFECT_ATTRS` 커맨드 | **S2b.** 캔버스 저장 대기열과 두 경로가 생기면 어느 쪽 버그인지 못 가린다 |
| 직전 입력 기억 (입력 3원칙 #3) · 빠른입력 | **S2b.** 세션 상태이며 캔버스 연속 입력 흐름의 일부 |
| 결함 속성 편집의 Undo 병합 (같은 필드 연속 변경을 한 단계로) | **S2b.** S4 는 커맨드를 만들지 않는다. 병합 없이 두면 `폭` 6번 클릭이 Ctrl+Z 6단계가 된다 |
| 프리셋 등록·관리 | **S2b 이후.** 설정의 별도 메뉴이고 S3 범위는 "항목 4단 계층" |
| 원인 코드 범례 재부여 · 손상결함표 출력 | **Phase 4.** `ExportRun` 스냅샷과 함께 설계해야 한다 |
| 전회차 결함의 속성 승계 | **Phase 2-D.** 반쪽으로 만들면 갈아엎는다 |
| 두 탭 동시 편집 시 문서 단위 LWW 손실 | **Phase 5.** 1인 1브라우저 전제에서는 발생하지 않는다. 병합이 필요해지면 저장 입도만 행 단위로 내리면 되고 형식은 그대로다 |
| 설정 가져오기/내보내기 | **Phase 5.** 백업 수단 전반과 함께 다룬다 |

---

## S3 구현 · builder · 2026-08-23

스펙: `_workspace/10_plan-reviewer_spec_S3_S4.md` (S3 작업만) · 확정 결정: **D6 · D7**
구현 로그: `_workspace/11_builder_log_S3.md`

### 질문과 연결된 가정 (Q24~Q27)

| # | 가정 | 질문 | 되돌리는 비용 |
|---|---|---|---|
| **G2** | **새로 추가한 부재의 구조체 구분 기본값은 `구조체`** 다. 행 배지로 항상 보이고 `⋯` 메뉴에서 즉시 바꿀 수 있다 | Q24 | 낮음 — `edit.ts::addMember` 한 줄. 다만 **이미 입력된 결함은 안 바뀐다** |
| **G3** | **`[기본 항목 세트 다시 불러오기]` 는 새로 추가된 마스터가 낀 링크만 깐다.** 사용자가 뺀 조합을 되살리지 않는다 | Q25 | 낮음 — `seed.ts::mergeSeedInto`. 단위테스트 2개로 고정 |
| **G5** | **캔버스의 `[설정]` 진입 버튼은 상단바 왼쪽**(`← 용역 구성` 옆)에 뒀다. 스펙 문구의 "하단 툴바"는 현재 화면에 존재하지 않는다 | Q26 | 낮음 — 버튼 위치 이동 |
| **G4** | **이름을 다른 마스터와 같게 바꾸는 것을 거부한다.** 발생원인이면 같은 이름에 코드번호가 2개가 되어 출력 범례가 깨진다 | Q27 | 낮음 — `edit.ts::renameMaster` 의 중복 검사 |

### 질문하지 않고 정한 것 (스펙·D6 범위 안에서 결정 가능)

| # | 결정 | 근거 |
|---|---|---|
| **G1** | **씨앗 항목의 id 는 고정 슬러그**(`seed-m-1` … `seed-r-5`)다. 난수가 아니다 | ① `다시 불러오기` 가 "이름만 고친 항목"을 다시 만들지 않는다(id 로 판정) ② 단위테스트가 읽힌다 ③ 문서가 기기 간에 같은 모양이 되어 Phase 5 병합이 쉬워진다 |
| **G6** | **드래그 재배치 때 그 목록 전체에 10 격자를 다시 부여한다** (중간값 삽입을 쓰지 않는다) | 저장 단위가 문서 하나라 재부여 비용이 0 이다. 격자가 무너지는 경로 자체가 없어진다. 층(`floorOrder.reorder`)은 레코드 단위 저장이라 중간값을 쓰지만 여기는 사정이 다르다 |
| **G7** | **`renumber` 를 층에서 재사용하지 않고 `items/order.ts` 에 따로 뒀다** | 층의 `renumber` 는 **이름을 파싱해 지하를 음수로 되돌린다.** 항목에 지하 개념이 없으므로 그 규칙을 끌고 오면 `B1` 같은 이름의 부재에서 순서가 튄다 |
| **G8** | **마스터 완전 삭제에도 되돌리기 토스트를 붙였다** (확인 다이얼로그 + 되돌리기) | 문서 단위 저장이라 직전 문서를 통째로 되돌리는 비용이 0 이다. ui-quality §4 는 삭제에 되돌리기를 요구한다. 확인 다이얼로그는 스펙 §2-5-d 대로 유지했다(사용처 건수를 보여주는 것이 목적) |
| **G9** | **발생원인 코드번호 입력 범위는 1~999 정수.** 중복은 거부 | 손상결함표 열 폭이 유한하다. 스펙은 "정수·유일"만 정했고 상한은 정하지 않았다 |
| **G10** | **`입력 미리보기` 메뉴는 `준비 중` 으로 비활성.** 스펙은 활성으로 계획했지만 **S4 가 이번 범위 밖**이라 붙일 폼이 없다 | 사용자 지시: "S4 는 착수하지 마라". 자리는 지금 잡아 뒀으므로 S4 때 `disabled` 만 떼면 된다 |
| **G11** | **`DefectAttrs` 초기값·정규화를 `canvas-core/src/defectAttrs.ts` 새 파일로 뺐다** (타입은 스펙대로 `types.ts`) | `types.ts` 를 타입 선언에 가깝게 유지하고, 결함을 만드는 5곳이 **한 모듈만** import 하게 한다. 필드가 늘어도 고칠 곳이 한 곳이다 |
| **G12** | **`normalizeDefectAttrs` 는 이미 정규화된 레코드면 같은 객체를 그대로 돌려준다** | 참조가 바뀌면 캔버스가 통째로 다시 그린다. 결함 500건짜리 용역에서 매 로드마다 새 객체를 만들 이유가 없다 |
| **G13** | **새 용역 생성 시에도 `ensureProjectSettings` 를 부른다** (지연 스냅샷 + 생성 시 스냅샷 둘 다) | 스펙 §2-4 는 생성 시 복사를 요구하고, 지연은 옛 용역용이다. 진입점을 하나로 두면 두 경로가 어긋날 수 없다 |
| **G14** | **설정 화면의 컬럼 선택은 "사라지면 첫 항목으로 내려앉는다"** (삭제·목록에서 빼기 후에도 화면이 비지 않는다) | 파생 계산이라 상태가 어긋날 수 없다. 빈 컬럼을 보여 주면 사용자가 고장으로 읽는다 |
| **G15** | **구조유형 탭은 roving tabindex + ←/→ 키 이동**을 구현했다 | 선택된 탭만 Tab 순서에 두면(WAI-ARIA 관행) 화살표 처리가 없을 때 **키보드로 다른 구조유형에 영영 못 간다** |
| **G16** | **결함유형을 새로 만들면 원인·보수방안 목록이 빈 채로 시작한다** (씨앗 4·5종을 자동으로 붙이지 않는다) | 어떤 원인이 붙을지는 결함유형마다 다르다. 자동으로 4종을 붙이면 사용자가 그것을 지우는 일부터 하게 된다. 컬럼3 이 바로 옆에 있어 추가가 두 번의 클릭이다 |

### 이번 범위에서 하지 않은 것 (S4 로 넘김)

| 항목 | 이유 |
|---|---|
| `items/size.ts` · `items/apply.ts` · `items/presets.ts` | S4-T2·T3. 사용자 지시로 S4 착수 보류 |
| `ui/defectForm/*` · 입력 미리보기 화면 | S4-T4~T7 |
| 폭 단일값 버튼 `[0.1][0.2]…` (D7) | S4-T5. **`DefectAttrs.widthMm` 은 `number \| null` 이라 D7 과 충돌하지 않는다.** 씨앗·상수에 구간 표기(`0.1~0.2`)를 넣지 않았다 |
| 폼 → 결함 저장 배선 | S2b |

---

## H — F5 · F2 · F1 (2026-08-23, builder)

비차단 가정. **전부 최종 보고에 포함한다.**

| # | 가정 | 근거 · 되돌리는 비용 |
|---|---|---|
| **H1** | **도면 배율(F5-3)을 바꿔도 결함 좌표를 옮기지 않는다.** 대신 결함이 있는 도면에서는 다이얼로그가 "표기는 지면 기준 유지, 그림만 움직임"을 알린다 | 지시서 명시(§F5-3 "좌표를 손대는 코드를 이식하지 마라"). 다만 F1 이후에는 정규화 분모가 A4 지면이라 그림과 표기가 어긋나 보일 수 있다 → **Q29**. 되돌리는 비용 낮음(배율을 되돌리면 완전 무손실) |
| **H2** | **도곽·범례는 `Drawing` 레코드에 저장한다** (용역 전체 설정이 아니라 도면마다) | Numdraw 도 페이지 객체에 뒀다. 층마다 축척·도면명이 다를 수 있다. 되돌리는 비용 보통(용역 단위로 올리려면 설정 스토어로 옮겨야 함) |
| **H3** | **범례 기호는 결함유형 이름의 약어**(첫 글자 → 두 글자 → 이름 → 순번)다. D8 은 "약어나 번호"를 허용했다 | 번호는 도면 위 어디에도 없어 대조할 곳이 없다. 약어는 스스로 설명한다. 되돌리는 비용 낮음(`legendSymbol` 한 함수) |
| **H4** | **메모는 손글씨 전용이 됐다.** 텍스트 메모를 새로 만들 방법은 없다. 옛 레코드는 읽기·편집만 유지 | 사용자 요구 원문 그대로. 데이터 구조는 텍스트 병행을 이미 받는다(`text` 와 `paths` 공존 가능) → **Q30** |
| **H5** | **메모 필기 잉크는 앰버 `#8a6a12`**, 질감 차이는 "점선 획"이 아니라 **획 둘레 점선 상자**로 준다 | 손글씨를 점선으로 그리면 글씨가 안 읽힌다. §F2 표는 굵기·투명도·점선 중 **택1**을 허용한다. 결함 예약색(빨강·보라·회색)·선택 파랑·가이드 시안 어느 것과도 겹치지 않는다 |
| **H6** | **자유그리기 사후연결 대기 중 획이 쌓인다** (AD14 메모는 단일 획을 가정했다) | 한 결함을 여러 획으로 그리는 것이 자연스럽다. 마지막 획만 남기면 앞 획이 조용히 사라진다. 되돌리는 비용 낮음 |
| **H7** | **`[A4로 맞추기]` 는 Blob 을 건드리지 않는다.** 저장 래스터는 옛 비율 그대로 두고 화면에는 런타임 합성 결과를 쓴다 | 되돌리기가 레코드 복원만으로 끝나고 고아 Blob 이 생기지 않는다. 대가로 **썸네일이 옛 비율로 남는다** → **Q31** |
| **H8** | **재정규화 되돌리기는 "이 화면에 머무는 동안"만 가능하다** (세션 스냅샷). 다른 화면으로 갔다 오면 되돌리기 바가 사라진다 | 스냅샷을 영속화하면 그것 자체가 새 스토어이고 DB 버전 인상이 필요하다(금지). 좌표 변환은 순수 아핀이라 나중에 필요하면 역변환 버튼을 다시 만들 수 있다 |
| **H9** | **`도면 크기 조절`·`도곽 · 범례 설정`·`A4로 맞추기` 는 전부 용역 구성 화면(P3)의 `⋯` 메뉴**에 뒀다 | 셋 다 "도면 한 장의 속성"이고 캔버스 조작이 아니다. 캔버스에 두면 조사 중에 실수로 눌릴 위험이 커진다 |

---

## I — S2b (2026-08-23, builder)

비차단 가정. **전부 최종 보고에 포함한다.**

| # | 가정 | 근거 · 되돌리는 비용 |
|---|---|---|
| **I1** | **Undo 병합 키를 "바뀐 속성 키 묶음"으로 역산한다.** 폼이 어느 필드를 건드렸는지 말해 주지 않으므로(§4-2 는 다음 값 **전체**를 올리게 했다) `from`/`to` 를 비교해 알아낸다 | 폼 시그니처를 바꾸지 않고 필드 단위 병합을 얻는다. 부작용: 연동 규칙으로 여러 필드가 함께 바뀌면 키가 달라져 **앞뒤 조작과 섞이지 않는다**(오히려 안전한 쪽). 되돌리는 비용 낮음(`mergeAttrCommand` 한 함수) |
| **I2** | **병합 창은 800ms** (스펙 §7 권고값 그대로) | 프리셋 연타·타이핑을 묶기에 충분하고, 의도적으로 멈춘 뒤의 조작은 분리된다. `ATTR_MERGE_WINDOW_MS` 상수 하나 |
| **I3** | **새 결함에 용역 기본 구조유형을 얹는다** (`ReduceContext.defectSeed`). 지시서에 없는 항목이지만 스펙 §4-2 의 `seedAttrs()` 가 이 용도로 이미 존재한다 | 없으면 결함마다 구조유형을 다시 골라야 한다. 캔버스는 값을 **해석하지 않고 펼치기만** 하므로 §2-6 경계를 깨지 않는다. `defectSeed` 를 안 넘기면 기존 동작 그대로 → 되돌리는 비용 거의 0 |
| **I4** | **저장 피드백은 토스트가 아니라 패널 하단의 상시 표시**(`저장 중… / 이 기기에 저장됨`) | 필드마다 토스트를 띄우면 입력 흐름을 가린다. 값은 제목·리스트·미완성 배지에 즉시 반영되므로 그것이 1차 피드백이다. 되돌리는 비용 낮음 |
| **I5** | **우측 패널 폭을 300 → 320px 로 넓혔다** (1200px 이하 296 · 980px 이하 268) | 폼이 이 패널 안에서 돌게 됐다. 버튼 그리드가 2열은 나와야 `계단참 슬래브` 가 3줄로 접히지 않는다. CSS 변수 한 줄 |

---

## J — S6 직전 입력 기억 (2026-08-24, plan-reviewer)

비차단 가정. **전부 D9 안쪽의 구현 디테일이다 — D9 를 뒤집지 않는다.** 최종 보고에 포함한다.

> ⚠️ **2026-08-28 배치4(G-5) 정리 — D9 가 D18 로 폐기되면서 J 계열의 유효 범위가 바뀌었다.**
>
> | # | 2026-08-28 이후 상태 |
> |---|---|
> | **J1** | **유효.** 이름만 바뀌었다 — `defectSeed` → `pickCarryAttrs()` 의 반환값. "복사 안 하는 필드는 키 자체를 안 만든다" 규칙은 그대로다. 합성 자리가 `{ ...EMPTY_DEFECT_ATTRS, ...ctx.defaultAttrs }`(생성) 과 `{ ...현재값, ...pickCarryAttrs(고른결함) }`(불러오기) 둘로 늘었을 뿐 스프레드라는 성질이 같다 |
> | **J2** | **소멸.** 되돌릴 씨앗 자체가 없다. 불러오기는 `SET_DEFECT_ATTRS` 커맨드 1건이라 `Ctrl+Z` 로 **정확히 1스텝** 되돌아간다 — J2 가 걱정하던 "세션은 앞으로만 간다" 문제가 구조적으로 사라졌다 |
> | **J3** | **유효.** `DEFECT_SEED_CARRY` → `DEFECT_CARRY_FIELDS` 로 이름만 바꿨고 `Record<keyof DefectAttrs, boolean>` 구조는 유지했다. 필드가 늘면 여전히 타입 검사가 깨진다 |
> | **J4** | **이미 2026-08-25 에 뒤집혔다**(§2-7 · Q44 — `surveyKind: true`). D18 도 이 판정을 그대로 쓴다 |
> | **J5** | **유효.** 테스트는 여전히 `canvas-core/test/s6.test.ts` 에 있다(`pickCarryAttrs` 순수 검증). `store.ts` 쪽 배선(토스트 · 조기 반환)은 타입 검사 + 사용자 확인으로 덮는다 |
>
> **신규 가정 J6** — 불러오기 후보는 **현재 도면이 아니라 이 용역 전체**의 결함이고 `seq` **내림차순**(최근 입력 우선)으로 보인다. 스펙 §5-4-b 는 *"이 용역의 결함 목록"* 이라고만 적었고 정렬을 지정하지 않았다. 근거: 방금 찍은 결함을 다시 쓰는 경우가 압도적이다(D9 가 애초에 자동 이어받기였던 이유). 되돌리는 비용 0 — `sort` 한 줄.

| # | 가정 | 근거 · 되돌리는 비용 |
|---|---|---|
| **J1** | **`defectSeed` 는 "이어받는 필드만 담은 화이트리스트 partial" 이다.** 새로 받는 필드는 `undefined` 로 넣지 않고 **키 자체를 넣지 않는다** | `interaction.ts` 의 `{ ...EMPTY_DEFECT_ATTRS, ...ctx.defectSeed }` 는 스프레드다 — `undefined` 값이 있으면 `EMPTY` 의 `null` 기본값을 덮어써 **저장 레코드에 `widthMm: undefined` 가 들어간다.** 그러면 `changedAttrKeys`(`!==` 비교)가 헛돌고 `normalizeDefectAttrs` 의 조기 반환이 매번 깨진다. 타입은 `Partial<DefectAttrs>` 그대로 두므로 `seedAttrs()`·`ReduceContext` 는 손대지 않는다 |
| **J2** | **Undo/Redo 는 `defectSeed` 를 되돌리지 않는다.** 세션은 앞으로만 간다 | D9 §2 는 갱신 시점을 "`SET_DEFECT_ATTRS` **커맨드가 커밋될 때**" 로 못박았고, `UNDO`/`REDO` 는 별개 액션이다. D9 의 근거("별도 판정 로직이 필요 없다")와도 맞다. `defectSeed` 는 저장 데이터가 아니라 다음 입력의 시작값일 뿐이라 틀려도 폼에서 바로 고칠 수 있다. 되돌리는 비용 낮음(UNDO/REDO 케이스에서 `r.command.k === 'SET_DEFECT_ATTRS'` 일 때 `from`/`to` 를 집어넣는 4줄) |
| **J3** | **이어받는 필드 목록을 `Record<keyof DefectAttrs, boolean>` 으로 선언한다** (`DEFECT_SEED_CARRY`) | 배열로 두면 `DefectAttrs` 에 필드가 늘 때 아무도 모르게 "새로 받음"으로 떨어진다. `Record<keyof …>` 는 **모든 키를 요구**하므로 필드가 늘면 타입 검사가 깨지고 그 자리에서 이어받을지 정하게 된다. `EMPTY_DEFECT_ATTRS` 옆(`canvas-core/defectAttrs.ts`)에 둔다 — D13 이 "필드별 초기값은 canvas-core 소관" 을 이미 정해 뒀다 |
| **J4** | **`surveyKind` 는 이어받지 않는다** (D9 표의 어느 칸에도 없음 → `EMPTY_DEFECT_ATTRS` 의 `EXTERIOR`) | 폼에 노출되지 않고 현재 어느 화면도 이 값을 바꾸지 않아 실질 차이가 없다. 나중에 내부조사 모드가 생기면 J3 의 표에서 `true` 로 한 글자만 바꾸면 된다 |
| **J5** | **단위테스트는 `apps/web` 이 아니라 `canvas-core` 에 쓴다** | `npm test` 는 `canvas-core`·`project-core` 만 돈다 — **`apps/web` 에는 테스트 러너 자체가 없다**(테스트 파일 0개). S6 하나 때문에 vitest 를 새로 설치하는 것은 범위 초과다. 위험한 로직(어느 필드를 이어받는가 · `undefined` 금지)을 전부 `canvas-core` 로 빼면 그 자리에서 검증된다. `store.ts` 쪽에 남는 것은 배선 한 줄이라 타입 검사 + 사용자 확인으로 덮는다 |

---

## K — S5(사진) · Phase 4(산출물 4종) · plan-reviewer · 2026-08-25

스펙: `_workspace/21_plan-reviewer_spec_S5_Phase4.md`
**전부 비차단. 최종 보고에 포함한다.** 질문으로 남긴 것은 Q32~Q35.

### 질문과 연결된 가정

| # | 가정 | 질문 | 되돌리는 비용 |
|---|---|---|---|
| **K1** | **PDF 는 브라우저 인쇄(`window.print()` + CSS `@page`)로 만든다.** `jspdf`·`pdf-lib` 를 넣지 않는다. 버튼 라벨은 `[PDF로 인쇄]` 이고 안내 문구를 상시 노출한다 | Q32 | **중** — PDF 라이브러리 도입 + 한글 TTF 서브셋 임베딩이 새 과제가 된다. 대신 인쇄 뷰(HTML+CSS)는 그대로 남아 버려지지 않는다 |
| **K3** | **사진 자르기·주석은 1차 제외.** 회전(90° 단위)만 구현하고 `edits.crop`·`annotations` **필드는 지금 확정해 예약**한다. 버튼은 `준비 중` | Q33 | 낮음 — 데이터 마이그레이션 0. UI 만 붙이면 된다 |
| **K6** | **사진번호 카운터의 리셋 규칙은 번호모드를 그대로 따른다** (층별리셋이면 사진번호도 층마다 1부터) | Q34 | 낮음 — `numbering.ts` 분기 1개 |
| **K9** | **결함 리스트는 9열 축약**(`NO·위치·부재·결함유형·폭·길이·면적·개소·사진번호`). 구조체유형·진행상황·누수여부·발생원인은 손상결함표에만 | Q35 | 낮음 — `DEFECT_LIST_COLUMNS` 배열 1줄 |

### 질문하지 않고 정한 것 (스펙·기존 결정 범위 안에서 판단 가능)

| # | 결정 | 근거 |
|---|---|---|
| **K2** | **`ExportRun` 은 새 오브젝트 스토어를 만들지 않고 `meta` 스토어를 KV 로 재사용한다** (`key = 'exportRun:{id}'`). **DB_VERSION 은 1 그대로** | `meta` 는 `keyPath:'key'` 인 범용 KV 이고(`db.ts` 143행) 인덱스가 없다. 레코드 추가는 스키마 변경이 아니라 `onupgradeneeded` 가 돌지 않는다. `ASSUMPTIONS S1` 의 보류 사유("형태 미확정이라 인덱스를 잡으면 잘못 굳는다")를 그대로 지킨다 — 지금도 인덱스를 안 잡는다. Phase 5 에서 DB 버전을 올릴 일이 생기면 그때 전용 스토어로 승격 |
| **K4** | **사진 Blob 은 도면과 같은 3종**(원본 `sourceBlobKey` · 장변 2048 렌더 `renderBlobKey` · 장변 320 썸네일 `thumbBlobKey`). 원본을 **버리지 않는다** | 인쇄 6장/페이지는 2048px 로 충분하지만(A4 반쪽 ≈ 90mm, 300dpi ≈ 1060px), 원본을 버리면 사용자가 PC 에서 파일을 지운 뒤 **복구가 불가능**하다. `DrawingUpload` 파이프라인을 그대로 복제하므로 구현 비용이 거의 없고 `sumBytes` 의 키 dedupe 도 이미 있다. 용량은 업로드 **전에** `estimateStorage` 로 막는다(도면과 같은 규칙) |
| **K5** | **EXIF 를 파싱하지 않는다.** `takenAt = file.lastModified`, `device = null`. 단 **방향(orientation)은 `<img>` 디코드 경로가 브라우저에서 자동 적용**하므로 사진이 눕지 않는다 | EXIF 파서는 새 의존성이다. 촬영시각·기기는 지금 어느 산출물에도 인쇄되지 않는다(§5 13열·§6 사진첩 캡션 어디에도 없다). 방향만은 출력 품질에 직결되는데, `createImageBitmap` 이 아니라 `<img>` 를 쓰면 공짜로 해결된다. 되돌리는 비용 낮음 — 필드가 이미 있어 파서만 끼우면 된다 |
| **K7** | **사진첩은 대표사진만 낸다.** `includeNonPrimaryPhotos` 옵션은 타입에만 두고 UI 는 `준비 중` | §2-C 는 "나머지 사진은 출력 옵션으로 포함 여부 선택" 이라 했지만, 넣으면 **사진번호에 부번(92-1, 92-2)이 생긴다** — §4 의 "사진번호는 사진 있는 결함마다 1 증가" 를 깬다. 번호 체계를 확장하는 결정은 실물 보고서를 보고 해야 한다 |
| **K8** | **조사구분(`surveyKind`) 필터 UI 를 두되 기본은 `전체`** | 폼에 노출이 없어 현재 모든 결함이 `EXTERIOR` 다. 필터를 지우면 나중에 내부조사 모드가 생길 때 화면 레이아웃이 흔들린다(B17·F26 과 같은 판단) |
| **K10** | **엑셀 라이브러리 호출은 `apps/web/src/export/xlsx.ts` 한 파일 안에서만** 하고 `await import()` 로 동적 로드한다. 후보 순위 `write-excel-file` > `exceljs` > `xlsx(SheetJS)`, **전부 막히면 CSV(UTF-8 BOM) 폴백** | 라이브러리 선택이 실패해도 착수가 막히지 않아야 한다. 행 데이터를 만드는 `project-core/export/*` 는 라이브러리를 전혀 모르므로 갈아치우는 비용이 파일 1개다. 동적 import 라 출력 화면에 들어가기 전까지 번들에 안 실린다 |
| **K11** | **조사위치도의 도면 밖 라벨**: 도곽이 켜져 있으면 여백을 추가하지 않고 잘린 번호를 **경고 목록으로 알린다**. 도곽이 꺼져 있으면 바운딩 박스만큼 **자동 여백**(상한 도면 장변의 10%) | Phase 3 에서 "Phase 4 의 문제"로 보류했던 항목의 답이다. 도곽은 A4 지면 자체라 밖에 여백을 붙이면 도곽 밖에 흰 띠가 생긴다. B2 가 라벨 좌표를 `[-0.5,1.5]` 소프트 리밋으로 허용했으므로 밖으로 나가는 일이 실제로 있다 |
| **K12** | **출력 옵션 `자유그리기 끄기` 는 코어를 고치지 않고 출력용 결함 사본에 `sketch: []` 를 넣어 구현한다** | `RenderInput` 에 스케치 스위치가 없다. 코어에 플래그를 늘리면 화면·출력 두 경로가 갈라진다. 메모·도곽·범례는 이미 `RenderInput` 에 넣지 않으면 꺼지므로 스위치가 필요 없다 |
| **K13** ⚠️**개정 2026-08-25** | **되돌릴 수 있는 삭제와 없는 삭제를 나눈다.** ① `deleteDefects` 는 **사진을 지우지 않는다** — Ctrl+Z 되돌리기 대상이라 그 자리에서 Blob 을 지우면 결함만 돌아오고 사진은 영원히 못 돌아온다. 고아 사진은 **용역을 열 때** `purgeOrphanPhotos(projectId)` 가 쓸어 담는다. ② `deleteFloor`·`deleteBuilding` 은 되돌리기가 없는 조작(확인 대화상자)이므로 **즉시 연쇄삭제를 유지**한다. ③ **`copyStructure` 는 사진을 복사하지 않는다** | 원안(모든 삭제에서 즉시 연쇄삭제)은 §6 의 "고아 Blob 이 조용히 쌓인다"만 보고 **되돌리기 경로를 보지 못했다**(code-reviewer 지적 1). 새로고침하면 되돌리기 스택도 함께 죽으므로 고아의 수명을 **한 세션**으로 묶으면 §6 의 목적은 그대로 달성된다. 복사 쪽은 전회차 승계(Phase 2-D) 소관이라 반쪽으로 만들지 않는다 |
| **K14** | **`Photo` 타입은 `project-core` 에 둔다.** `canvas-core` 는 사진을 모른다. `numbering.ts` 는 `hasPhoto: ReadonlySet<string>` 만 받는다 | 경계 규칙 8(두 코어 상호 import 금지) 유지. `numbering.ts` 가 `Defect` 를 직접 import 하지 않도록 로컬 최소 타입 `NumberingDefect` 를 선언한다 — `items/size.ts::SizeInput` 이 이미 쓴 수법이라 구조적 타이핑으로 실제 `Defect` 가 그대로 들어간다 |
| **K15** | **사진 섹션 UI 는 `ui/photos/` 에 둔다. `ui/defectForm/` 에 넣지 않는다** | `ui/defectForm/*` 은 store·repo·캔버스 import 금지(모바일 재사용 경계)인데 사진은 Blob·objectURL 을 다뤄야 한다. 경계를 깨는 대신 자리를 나눈다 |
| **K16** | **불변식 #8(대표 정확히 1장)은 읽기 정규화(`normalizePhotos`)로 강제한다.** 저장 시점 검증만 두지 않는다 | 쓰기 경로가 업로드·삭제·대표지정·순서변경·Undo 로 여러 개다. 저장 쪽에서만 막으면 반드시 새는 경로가 생긴다. E11(옛 레코드 읽기 정규화)과 같은 방식이고 DB 버전을 올리지 않는다 |
| **K17** | **손상결함표 `위치` 열은 동이 2개 이상인 용역에서만 `{동} {층}`** 으로 합친다 | 실측(§5)은 `지하1층` 뿐이지만 동이 여러 개면 층 이름만으로 구분이 안 된다. 동이 하나뿐인 대부분의 용역에서 실측과 똑같이 나온다 |
| **K18** | **`제2장 현장조사` 는 출력 옵션의 텍스트 입력 필드**(`doc.headerLine2`, 기본값 그대로) | 보고서마다 장 번호가 다르다. 하드코딩하면 다른 발주처에서 조용히 틀린다 |
| **K19** | **사진첩 캡션의 길이는 m 로 환산해 적는다** (`lengthMm/1000`) | 기획서 §6 예시 `"수직균열 0.2×0.5×3ea"` 의 `0.5` 는 m 다. mm 로 적으면 `0.2×2000×2ea` 가 되어 예시와 어긋난다. 폭은 mm 원값 그대로(D7 의 `0.2` 가 그대로 인쇄된다) |
| **K20** | **4종을 한 번의 `[생성]` 으로 뽑으면 같은 `ExportRun` 을 공유한다.** 재다운로드는 `mapping` 을 그대로 쓰고 다시 계산하지 않는다 | 이것이 §4-3 4단계의 목적이다. 각자 계산하면 사진첩의 `사진 92` 와 손상결함표의 `92` 가 다른 결함을 가리킬 수 있고, **조용히 틀린다** |
| **K21** | **손상결함표 발생원인 범례는 이 출력에 실제로 등장한 코드만** code 오름차순으로 표 끝에 낸다 | 마스터에 등록만 되고 안 쓰인 원인까지 범례에 넣으면 발주처가 "이 원인은 어디 있나" 를 찾게 된다. F6(코드는 마스터 고정)은 그대로 지킨다 — 재부여하지 않는다 |

---

## L — Phase A 구현 (T1~T8) · builder · 2026-08-25

스펙: `_workspace/21_plan-reviewer_spec_S5_Phase4.md` · 구현 로그: `_workspace/22_builder_log_PhaseA_S5.md`
**전부 비차단이고 K1~K21 을 뒤집지 않는다. 최종 보고에 포함한다.**

| # | 가정 | 근거 · 되돌리는 비용 |
|---|---|---|
| **L1** | **`assignNumbers` 의 3번째 인자 `ctx` 를 선택(`Partial<NumberingContext>`)으로 뒀다.** 빠진 집합은 빈 Set 으로 본다 | 사진이 한 장도 없을 때·출력 화면이 아직 사진을 안 읽었을 때도 번호 미리보기를 부를 수 있어야 한다. 스펙은 필수로 적었지만 필수로 두면 호출자마다 `new Set()` 을 만들게 된다. 되돌리는 비용 0(시그니처만 좁히면 된다) |
| **L2** | **`ProjectBundle` 에 `photos: Photo[]` 를 추가했다.** 묶음 로드가 사진까지 한 번에 읽는다 | 결함을 고를 때마다 저장소를 두드리면 로컬 우선(불변식 #3)의 즉시성이 깨진다. 사진첩(T15)도 같은 목록을 쓴다. Blob 은 포함되지 않으므로(키만) 메모리 비용이 작다 |
| **L3** | **`Inspector` 는 사진 섹션을 `photoSlot: ReactNode` 슬롯으로 받는다.** 직접 렌더하지 않는다 | `Inspector` 가 `data/usePhotos` 를 import 하면 폼 경계 쪽 파일이 저장소를 알게 된다. 슬롯이면 배선은 `CanvasRoute` 한 곳이고 K15(사진 UI 를 `ui/photos/` 로 분리)와도 맞는다 |
| **L4** (문구 정정 2026-08-25) | **사진 삭제의 되돌리기는 지운 사진의 Blob 을 다시 만들지 않는다.** 10초 창이 닫힌 뒤에야 `deletePhotos` 를 부르므로, 그 사이 레코드와 Blob 이 그대로 남아 있다 | ~~"저장소를 다시 쓰지 않는다"~~ 는 정확하지 않았다(code-reviewer 경미 5): **승계된 대표(B)는 삭제 즉시 저장되고, 되돌릴 때 A 도 `upsertPhotos` 로 다시 쓰인다.** 다시 만들지 않는 것은 **Blob** 이다. 그래서 창이 열려 있는 동안 DB 에 대표가 2장인 순간이 생기는데, 읽기 정규화(K16)가 화면·출력에서 언제나 1장으로 덮으므로 불변식 #8 은 지켜진다. 대가: **10초 안에 브라우저를 강제 종료하면 삭제가 확정되지 않는다**(`beforeunload` 로 최선을 다한다). IDB 트랜잭션은 전부 커밋 아니면 전부 중단이라 최악이 "삭제 안 됨"이지 부분 손상이 아니다 — 안전한 실패다 |
| **L5** | **사진 교체(`교체…`)에는 되돌리기를 두지 않는다.** 새 파일을 등록하고 옛 레코드를 즉시 지운다 | 파일 선택 대화상자에서 새 파일을 고른 것 자체가 확정 의사다. 새 레코드가 옛 `sortOrder`·`isPrimary`·`caption` 을 물려받아 목록이 튀지 않는다. 되돌리는 비용 낮음(삭제와 같은 지연 구조를 붙이면 된다) |
| **L6** | **인입 용량 사전 확인은 파일 묶음 단위로 한 번** 한다(원본 합계 × 1.6 + 여유 8MB). 부족하면 그 묶음 전체를 거절 목록으로 돌린다 | 도면과 같은 규칙("처리를 다 하고 마지막에 실패하지 않는다"). 장별로 재려면 디코드 후 크기를 알아야 해서 규칙 자체가 뒤집힌다 |
| **L7** | **테스트 파일은 `packages/project-core/test/` 에 둔다** (스펙은 `src/export/numbering.test.ts` 로 적었다) | 기존 테스트 8개가 전부 `test/` 에 있고 tsconfig 가 `["src","test"]` 를 포함한다. 관행을 깨면 두 자리가 공존한다 |

---

## M — Phase B 구현 (T9~T16) · builder · 2026-08-25

스펙: `_workspace/21_plan-reviewer_spec_S5_Phase4.md` · 구현 로그: `_workspace/24_builder_log_PhaseB_Export.md`
**전부 비차단이고 K1~K21 · L1~L7 을 뒤집지 않는다. 최종 보고에 포함한다.**

| # | 가정 | 근거 · 되돌리는 비용 |
|---|---|---|
| **M1** | **표의 숫자 표시는 `toFixed(소수자리)` 후 꼬리 0 을 지운다.** `0.2` · `0.0005` · `0.5` · `2500` · `0` | 스펙 §3-4 는 열마다 `decimals` 를 못박았지만 실측 인용은 `0 / 0 / 0.5`(면적 4자리)와 `0.2`(폭 1자리)를 **동시에** 보여준다. `toFixed` 만 쓰면 `0.5000` 이 되고, 절사만 하면 부동소수 꼬리(`0.30000000000000004`)가 새어 나온다. 두 규칙을 하나로 합친 것이 이 방식이다. 엑셀에는 **숫자 원값**을 넣고 일반 서식에 맡기므로 화면·엑셀이 같은 모양이 된다. 되돌리는 비용 0(`numText()` 함수 1개) |
| **M2** | **엑셀 인쇄 반복 행(1~5행)을 설정하지 않는다.** `write-excel-file@4` 가 `printTitles` 를 노출하지 않는다 | Q36. 라이브러리 경계가 `xlsx.ts` 한 파일이라 `exceljs` 로 갈아타는 비용이 파일 1개다. 실물 서식을 받기 전에 번들을 키울 이유가 없다. `damageTableFile.ts` 에 `DAMAGE_REPEAT_ROWS = 5` 상수를 남겨 자리를 표시했다. **대안:** 사용자가 엑셀에서 `인쇄 제목 → 반복할 행 $1:$5` 를 한 번 지정하면 된다 |
| **M3** | **`[생성]` 은 파일로 낼 수 있는 것만 내려받는다** — 손상결함표·결함리스트 엑셀, 조사위치도 PNG. **사진첩과 PDF 는 이력 줄의 인쇄 버튼**으로 연다 | Q37. `[생성]` 이 인쇄 탭을 자동으로 열면 팝업 차단에 걸린다(사용자 제스처와 `await` 사이에 시간이 벌어진다). 사진첩을 PNG 로 따로 그리면 인쇄 뷰와 같은 화면을 두 번 만들게 된다 |
| **M4** | **출력 화면을 처음 열면 층이 `sortOrder` 오름차순(지하→지상)으로 전부 선택**돼 있다 | §4-1 의 화면 그림이 이미 구간(`①–12`)이 붙은 상태다 — 빈 선택으로 열면 화면이 무엇을 하는 곳인지 안 보인다. "누른 순서가 출력 순서"라는 규칙(§4-4)은 그대로다. 이건 그 **시작값**일 뿐이고 `[해제]` 한 번이면 사라진다 |
| **M5** | **`@page` 규칙을 전역 CSS 에 두지 않고 인쇄 뷰가 런타임에 `<style>` 로 주입한다** | 산출물마다 방향이 다르고(조사위치도만 가로), 전역에 두면 **다른 화면에서 Ctrl+P 를 눌렀을 때도** A4 12mm 가 강제된다. CSS 는 `@page` 를 클래스로 스코프할 수 없다 |
| **M6** | **조사위치도 출력 캔버스 한 변의 상한은 8192px.** 넘으면 배율(`mapScale`)을 자동으로 낮춘다 | 브라우저가 캔버스를 못 만들면 조용히 **빈 PNG** 가 나온다. A4 가로 도면(1754×1240)은 배율 4 에서도 7016px 이라 실제로 걸리는 일이 거의 없지만, 큰 원본 도면에서는 걸린다 |
| **M7** | **조사위치도 PNG 배경을 흰색으로 채운다** (투명 아님) | 투명 PNG 를 워드·한글에 붙이면 배경이 검게 나오는 뷰어가 있다. 보고서 부록에 들어가는 그림이라 흰 배경이 맞다 |
| **M8** | **한 층에 도면이 여러 장이면 `sortOrder` 가 가장 앞선 1장만 쓴다** | §2-8 이 층:도면 1:1 을 전제하고 `sortOrder` 는 항상 0 이다. 여러 장을 여러 페이지로 내는 것은 스펙에 없다 — 넘겨짚어 만들지 않는다 |
| **M9** | **`[같은 번호로 다시 받기]` 는 그 이력이 실제로 냈던 산출물만** 다시 만든다. 이력에 산출물이 하나도 없으면(사진첩만 골랐던 경우) 파일 3종을 전부 낸다 | 재다운로드의 목적은 "그때 그 파일"이다. 지금 화면의 체크박스를 따라가면 그때와 다른 묶음이 나온다 |
| **M10** | **손상결함표는 인쇄 뷰를 만들지 않는다.** 엑셀 전용 | §3-4 는 엑셀 서식(병합 헤더·인쇄 반복 행·열 너비)으로만 정의돼 있고, §5-C 의 인쇄 뷰 작업(T12·T13)은 결함 리스트·사진첩·조사위치도만 든다. 13열은 A4 세로에 안 들어가고 가로 인쇄 뷰는 스펙에 없다 |
| **M11** | **`ExportRun` 을 파일 생성 *전에* 저장한다** | 다운로드가 중간에 막혀도(용량·권한) **번호 스냅샷은 살아 있어야** `[같은 번호로 다시 받기]` 가 성립한다. 산출물 목록은 실제로 내려받은 뒤 `appendExportArtifact` 로 덧붙인다 |
| **M12** | **`assignNumbers` 의 `excluded` 는 화면에 띄우지 않는다** | 필터로 빠진 결함은 사용자가 방금 스스로 고른 조건의 결과다. 경고로 띄우면 "보수완료 제외"를 켤 때마다 경고가 뜬다. **알려야 하는 것은 포함됐는데 손봐야 하는 것**(미완성·대표사진 없음)이고 그것만 띄운다(D3) |

---

## Phase 5 태블릿 현장 앱 (착수 전 점검) · plan-reviewer · 2026-08-25

전문은 `_workspace/26_plan-reviewer_spec_Phase5_Mobile.md`.
**아직 코드가 아니다** — 목업 제작과 작업 분해의 기준값이다. 되돌리는 비용은 전부 "목업 다시 그리기" 수준.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **N1** | **현장 화면의 번호 풍선은 `#42` 형태로 `#` 접두를 붙인다** — 입력순번(`Defect.seq`)이지 출력 결함번호가 아님을 눈으로 구분 | 불변식 #2(번호 미저장). G6 "최근 번호로 점프"가 이 숫자를 쓰는데, 사용자가 그것을 보고서 번호로 착각하면 신뢰가 깨진다. 비용 낮음(라벨 문자열 1곳) |
| **N2** | **현장에서 없는 부재를 즉시 추가하는 것을 허용하되, "이 용역에만" 추가된다.** 전역(ORG) 승격은 PC에서만 | D6 근거 원문이 "현장에서 없는 부재를 즉시 추가하는 것도 포기할 수 없다"고 못박았다. 스냅샷 불변식(#7)을 지키면서 둘 다 만족하는 최소 형태. 비용 낮음 |
| **N3** | **태블릿에 출력(엑셀·PDF·PNG) 화면을 넣지 않는다** | G5 "보고서 부록은 사무실 작업". 출력이 없으면 `ExportRun` 재현성 위험이 태블릿에 아예 생기지 않는다. 오프라인 준비 목록도 그만큼 가벼워진다. 비용 낮음(나중에 추가는 가능) |
| **N4** | **오프라인 상태 칩은 pill + 아이콘 형태로, 결함 상태색(빨강/보라/회색)과 시각적으로 구분한다** | 색 예약 규칙. 목록 화면이라 결함 마커와 같은 화면에 안 나오지만 학습을 흐리지 않는 편이 낫다. 비용 0 |
| **N5** | **Q6 정밀 표기 = 탭 즉시 찍기(기본) + `⊕조준` 크로스헤어 + `[여기]` 확정.** 롱프레스는 컨텍스트 메뉴로 남긴다 | Q38. **코어 변경 0**(화면 중앙 좌표로 POINTER_DOWN/UP 합성)이라 뒤집는 비용이 앱 UI 1~2파일. 방향·영역은 조준 미적용, 대신 넛지 패드(1탭=1px) |
| **N6** | **태블릿 가로 = 우측 사이드시트(400pt) / 세로 = 바텀시트 3단(PEEK 16% · HALF 55% · FULL 92%)** | Q39. §2-B 원문("모바일=바텀시트")은 폰 기준 서술로 읽었다. `ui/defectForm/*` 내용물은 같고 셸만 두 벌. 비용 중간 |
| **N7** | **시트가 열린 높이·폭을 그대로 `SET_SAFE_INSETS` 로 코어에 넘긴다** | 코어에 이미 있는 기능(`CanvasState.safeInsets` · `visibility.applyEnsureVisible`). "방금 찍은 결함이 시트 뒤에 숨는" 현장 최악 버그를 코어 변경 없이 막는다. 비용 0(배선만) |
| **N8** | **한 번 `CURRENT` 가 된 전회차 결함은 사진을 지워도 `PREV_PENDING` 으로 되돌아가지 않는다.** 되돌리려면 명시적 `[전회차로 되돌리기]` | Q42. D3의 "자동 제외하지 않고 경고만" 원칙과 같은 결. 사진 없는 CURRENT 는 기존 미완성 경고에 걸린다. 비용 낮음 |
| **N9** | **오프라인 번들 상태를 `meta` KV 에 `offline:{projectId}` 키로 넣는다. 새 스토어를 만들지 않고 `DB_VERSION` 은 1 그대로** | `ExportRun` 이 이미 같은 수법을 썼다(`exportRuns.ts`). "IndexedDB 마이그레이션 금지" 규칙 준수. 비용 0 |
| **N10** | **오프라인 준비는 도면 *렌더본*만 받는다. 원본(PDF/원본 이미지)은 받지 않는다** | 현장에서 안 쓰고 용량의 절반 이상을 차지한다. 필요해지면 온라인일 때 개별 요청. 비용 낮음(매니페스트 항목 추가) |

---

## Phase 5 트랙 A 구현 (코어 터치 지원) · builder · 2026-08-25

전문은 `_workspace/27_builder_log_Phase5_TrackA.md`. **이미 코드다.** 전부 `packages/canvas-core` 안이고,
아래는 스펙이 값을 정해 주지 않아 구현자가 고른 것들이다. 되돌리는 비용은 전부 코어 1파일 수정 수준.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **P1** | **`GESTURE_PINCH{factor, pan}` 은 직전 프레임 대비 *상대값*이다** (제스처 시작 시점 대비 누적값이 아니다) | 기존 `zoomAt(vp, cursor, factor, …)` 이 "지금 뷰포트에 factor 를 곱한다" 는 상대 연산이다. 누적값으로 받으려면 코어가 제스처 시작 뷰포트를 기억해야 하고, 그러면 END 를 놓쳤을 때 코어가 잠긴다. 어댑터는 두 접점 거리비를 프레임마다 내면 된다. 비용 낮음(어댑터 계산식) |
| **P2** | **코어는 핀치 상태를 들고 있지 않는다. `GESTURE_PINCH_END` 는 아무 일도 하지 않는다** | 활성 포인터 추적은 T1(어댑터) 몫이라고 스펙이 이미 정했다. 코어에 `pinching` 플래그를 두면 END 유실 시 캔버스가 잠긴다 — 현장에서 가장 무서운 고장이다. 대신 `GESTURE_PINCH` 자체가 방어적으로 드래그를 취소한다. 비용 낮음 |
| **P3** | **두 번째 포인터로 드래그가 취소될 때 토스트를 띄우지 않는다** | 손가락 두 개를 얹은 사용자는 "화면을 조작하려는 것"이지 실수한 것이 아니다. 핀치할 때마다 토스트가 뜨면 소음이다. 비용 0(Effect 1줄 추가) |
| **P4** | **취소는 선택(selection)을 남긴다** | Escape 취소가 이미 그렇게 동작한다(`onKeyDown`). 두 규칙이 달라야 할 이유가 없다. 비용 0 |
| **P5** | **팬 드래그 중 두 번째 포인터가 닿아도 뷰포트는 시작 위치로 되감지 않는다** | 지금까지 민 만큼을 되감으면 손가락을 얹는 순간 화면이 튄다. "롤백" 의 대상은 *문서를 바꿀 뻔한 드래그*지 이미 확정된 화면 위치가 아니다. 비용 낮음 |
| **P6** | **`CENTER_ON_NORM` 은 `safeInsets` 를 무시하고 캔버스 기하 중앙에 놓는다** | `viewport.centerOn()` 이 원래 그렇게 만들어져 있다(변경 0 이 스펙 T4 의 요구). 시트에 가리는 문제는 이미 `SET_SAFE_INSETS` + `applyEnsureVisible` 이 따로 처리한다. 비용 낮음 |
| **P7** | **`HitProfile` 은 7개 필드를 전부 가진 완전한 객체다** (`Partial` 이 아니다) | 부분 주입을 허용하면 "터치인데 clickSlop 만 마우스" 같은 어정쩡한 조합이 생긴다. 프로파일은 통째로 갈아끼우는 것이다. 어댑터는 `{...DEFAULT_HIT_PROFILE, …}` 로 쉽게 만든다. 비용 낮음 |
| **P8** | **`arrowRoute.ts` 의 `CLICK_SLOP_PX` 는 프로파일화하지 않았다** | 스펙 T5 가 지목한 파일은 `constants.ts` · `hitTest.ts` · `interaction.ts` 셋이다. `arrowRoute` 는 `ctx` 를 받지 않는 순수 함수이고, 여기의 slop 은 "화살표 첫 구간이 열렸는가" 판정이라 손가락 크기와 무관하다. 비용 낮음(인자 1개 추가) |

---

## R — 사진 마무리(자르기·주석·EXIF·캡션) + 출력 보완 · plan-reviewer · 2026-08-25

전문은 `_workspace/29_plan-reviewer_spec_PhotoPolish.md`. **아직 코드가 아니다** — builder 착수 기준값이다.
이번 라운드도 원격/비대화형이라 **차단 질문 0건**으로 전부 스스로 확정했다.
관련 질문 2건은 `QUESTIONS.md` **Q43 · Q44**(둘 다 비차단).

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **R1** | ⭐ **`crop`·`annotations` 좌표의 기준 프레임 = 렌더본(EXIF 방향 적용 후 · `edits.rotate` 적용 *전*)** | `photo.ts` 는 "원본 기준"이라고만 적어 회전 전/후가 미정이었다. 원본 픽셀 프레임은 EXIF 방향이 안 붙어 세로 사진이 누워 있어 UI 가 좌표를 얹을 수 없다. 렌더본은 원본과 종횡비·방향이 같고 정규화라 축소가 무의미하다. `Photo.width/height` 주석("렌더 래스터 픽셀")과도 자기 정합적. **뒤집는 비용 높음** — 이미 저장된 편집이 전부 틀어진다. 착수 전에 확정해야 하는 유일한 항목 |
| **R2** | **합성 순서는 `주석 → 자르기 → 회전` 고정. 화면·출력이 같은 함수를 쓴다** | 주석을 자르기 전에 그리면 잘려 나간 획이 자동으로 사라진다(별도 클리핑 불필요). 순서를 화면/출력이 다르게 잡으면 "화면과 인쇄물이 다르다"는 최악의 버그가 난다. 비용 낮음(함수 1개) |
| **R3** | **`PhotoAnnotation.width` 는 렌더 프레임 *장변 대비 비율*(0~1)이다. 픽셀이 아니다.** 프리셋 0.004 / 0.008 / 0.014 | 타입에 단위가 없었다. 픽셀이면 자른 뒤 확대될 때 선이 상대적으로 얇아진다. 지금 `annotations` 는 **전 레코드가 빈 배열**이라 마이그레이션 비용이 0 인 지금 못 박는 것이 가장 싸다. 비용 0(지금) / 높음(나중) |
| **R4** | **주석 좌표·굵기는 소수 4자리 반올림 저장** | 0.0001 × 2048 ≈ 0.2px — 눈에 안 보인다. 자유획 레코드 크기가 1/3 로 준다. 비용 0 |
| **R5** | **주석 되돌리기는 편집 세션 로컬 스택(최대 50)이다. 캔버스 Undo 스택을 쓰지 않는다** | `[적용]` 전에는 레코드에 아무것도 안 들어가므로 세션 밖에 되돌릴 대상이 없다. 도면 히스토리에 사진 획이 섞이면 "Ctrl+Z 했더니 도면 결함이 사라진다"가 된다. `PhotoPreviewDialog` 가 이미 window 캡처에서 Ctrl+Z 를 막고 있어 새는 경로도 없다. 비용 낮음 |
| **R6** | **주석 색은 빨강 `#e5342a` + 노랑 `#ffd400` 2종만.** 굵기 3단 | 사진 위 빨간 화살표는 안전진단 보고서 관행이고 "현회차 결함"이라는 예약 의미와도 일치한다. 노랑은 어두운 콘크리트·야간 사진용. 색이 늘면 "이 색은 무슨 뜻이냐"가 생겨 출력에서 의미가 흐려진다. 비용 0(상수 배열) |
| **R7** | **썸네일 그리드는 합성하지 않는다. 대신 `✎` 배지를 띄운다** | 320px 썸네일 수십 장을 매번 캔버스로 돌리면 우측 패널이 무거워진다. 미리보기를 열면 합성본이 보인다. 비용 낮음 |
| **R8** | ⭐ **EXIF 는 라이브러리 없이 자체 파서.** JPEG APP1 에서 태그 3개(`0x9003`·`0x010F`·`0x0110`)만 읽는다 | 필요한 태그가 3개뿐 · 번들 0KB(라이브러리는 25~50KB) · **순수 함수라 `project-core` 에서 단위테스트 가능**(`apps/web` 엔 테스트 러너가 없다) · 틀려도 폴백(`file.lastModified`)이 이미 있어 **실패 비용이 0 에 수렴**. PDF 라이브러리를 거절한 것(K1)과 같은 판단. 비용 낮음(나중에 라이브러리로 교체 가능) |
| **R8-b** | **EXIF 시각은 타임존 없이 로컬 시간으로 해석한다.** 연도 1990 미만 / `지금+1일` 초과면 버리고 폴백 | EXIF 에 타임존이 없고 조사자와 카메라는 같은 지역에 있다. 카메라 시계가 초기화된 사진이 1970년으로 잡히는 것을 막는다. 비용 0 |
| **R8-c** | **EXIF 는 파일 앞 256KB 만 읽는다. JPEG 만 파싱하고 PNG·WEBP 는 null** | EXIF 는 파일 맨 앞에 있다. 30MB 를 통째로 메모리에 올리지 않는다. 비용 0 |
| **R9** | **`대표 외 사진 포함` 은 부번(`12-1`·`12-2`)으로 낸다. `assignNumbers()`·`ExportRun` 무변경. 손상결함표 `사진번호` 열은 대표 정수만 유지** | Q43. 사진마다 정수를 매기면 `ExportRun.mapping`(결함 1건 : 번호 1개)이 표현 불가가 되어 재현성이 통째로 깨진다. 부번 순서가 재출력 시 흔들릴 수 있는 점은 **대표를 바꾸면 사진첩 이미지가 바뀌는 기존 성질과 같은 급**이다. 비용 낮음 |
| **R10** | **`DEFECT_SEED_CARRY.surveyKind` 를 `true`(이어받음)로 바꾼다** | Q44. 현재 `false` 의 근거(J4: "폼에 노출되지 않아서")가 이번 작업으로 소멸한다. D9 표에는 `surveyKind` 가 **아예 없어** D9 를 뒤집는 것이 아니다. 잘못됐을 때 A(이어받음)는 사용자가 고른 값이 폼에 보이지만, B(리셋)는 아무도 안 고른 기본값이 조용히 들어간다. 비용 낮음(1줄 + 테스트 1곳) |
| **R11** | **조사구분 세그먼트는 결함정보 폼 최상단(구조 유형 위)에 둔다** | "이 결함이 어떤 조사에서 나왔는가"라는 틀이고, R10 으로 계속 따라오므로 위에 있어도 소음이 아니다. 비용 0(JSX 위치) |
| **R12** | **손상결함표 인쇄 뷰는 A4 *가로*(landscape)** | 13열 · 열폭 합 128문자. 세로 186mm 에 넣으면 `결함의 유형 및 형상`(18)이 26mm 라 두 줄로 깨진다. 조사위치도가 이미 가로를 쓴다. 비용 0(`pageRule()` 1줄) |
| **R12-b** | **`PrintDefectList` 는 새 `PrintDamageTable` 을 `groupHeader=false legend=false` 로 감싸는 래퍼가 된다.** 병합 머리를 자동 판정하지 않고 플래그로 켠다 | `DEFECT_LIST_COLUMNS` 의 폭·길이·면적·개소도 `DAMAGE_COLUMNS` 에서 온 객체라 `group: '손상규모'` 를 **이미 갖고 있다** — 자동 판정으로 짜면 기존 결함 리스트 PDF 에 없던 병합 머리가 조용히 생긴다. 비용 0 |

---

## U — 사용자 수정사항 2파일 검토 · plan-reviewer · 2026-08-28

전문은 `_workspace/30_plan-reviewer_spec_UserFeedback0828.md`. **아직 코드가 아니다.**
이번 라운드는 **차단 질문 6건**(Q45·Q46·Q47·Q48·Q49·Q50·Q52 — 7건)이 있어
가정으로 덮지 **않았다.** 아래는 그 질문들과 무관하게 진행하는 **버그 수정 3건과 소품 1건**의 가정뿐이다.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U1** | ⭐ **파일2-①(도곽 설정 타이핑 불가)의 원인은 `CanvasView.tsx:286` 의 `onKeyUp` 무가드 + `Form.tsx:99-131` 의 `[onClose]` 포커스 이펙트 + `querySelector` 문서순서 3단 연쇄다** | 코드로 경로를 끝까지 추적했다: keyup → `store.ts:350` 이 **무조건** 새 state 객체를 만든다 → CanvasRoute 리렌더 → `CanvasRoute.tsx:884` 인라인 `onClose` 신 참조 → 포커스 이펙트 재실행 → `Form.tsx:148` 의 ✕ 버튼이 본문보다 앞이라 거기로 포커스. **검증 가능한 예측: 용역 구성 화면(P2)에서 연 같은 다이얼로그는 정상이다**(CanvasView 미마운트). 이 예측이 틀리면 진단이 틀린 것이다 |
| **U2** | **`onKeyUp` 가드를 넣되 `spaceRef.current = false` 는 가드보다 *먼저* 실행한다** | 스페이스 팬 상태가 눌린 채로 남으면 커서가 손 모양으로 굳고 클릭이 팬으로 먹힌다. 가드를 맨 앞에 두면 그 버그를 새로 만든다. 비용 0 |
| **U3** | **`Modal` 초기 포커스를 마운트 1회로 고정하고, 포커스 대상에서 `.modal__x`(닫기)를 제외해 본문 우선으로 바꾼다** | 지금은 **모든 모달이 열릴 때 닫기 버튼에 포커스가 간다**(접근성 관점으로도 틀렸다 — 첫 입력 필드가 맞다). 이펙트를 포커스(`[]`)와 키 트랩(`[onClose]`)으로 쪼개면 둘 다 해결된다. 비용 낮음 · **영향 범위는 모든 모달**이라 사용자 확인 체크리스트에 넣는다 |
| **U4** | **`PrintRoute` 의 자동 `window.print()` 를 제거하되 `waitForImages` 는 남겨 `[PDF로 인쇄]` 버튼의 `disabled` 로 쓴다** | 자동 인쇄를 없애면서 "이미지 디코드 전에 인쇄하면 빈 칸이 나온다"는 원래 방어를 잃지 않는 유일한 방법. `?autoprint=1` 같은 옵션은 **만들지 않는다** — 여는 경로에 따라 동작이 갈리면 다시 혼란이 생긴다. 비용 낮음 |
| **U5** | **`pageDecor.ts` 의 `titleBlock`/`legend` `null` 폴백은 *읽기 시점 정규화*로만 한다.** 저장된 `null` 을 일괄로 채우지 않는다 | `isInkMemo`(옛 텍스트 메모 판별)가 이미 쓴 수법. DB_VERSION 1 유지 · 마이그레이션 0건. **다만 사용자가 보는 화면이 바뀌므로 Q47 답변 전에는 적용하지 않는다** |
| **U6** | **파일1-② "사진첩 출력안됨"은 코드 결함이 아니라 `[생성]`이 사진첩 파일을 안 내는 설계(M3·Q37) 때문이다** | `produce.ts:27-32` `FILE_ARTIFACTS` 에 `PHOTO_BOOK` 이 없고 `Export.tsx:71` 이 안내 문구로 이미 알린다. 6장/페이지·셀 테두리는 `print.css:150-174` 에 **이미 구현**돼 있어, 사용자가 인쇄 뷰를 본 적이 없다는 해석이 가장 정합적이다. **Q51 로 확인 요청함** — 틀리면 진짜 렌더 버그를 찾아야 한다 |
| **U7** | **파일2-④(전차→금차 자동전환)는 새 질문을 만들지 않고 기존 Q42 를 재상신한다** | Q42 가 이 기능의 유일한 미결 지점(사진을 지우면 되돌아가는가)을 이미 담고 있다. 중복 질문은 사용자 답변 부담만 늘린다. 전환 자체는 상세기획 §2-D 에 이미 있고 사용자가 이번에 재확인했다 |
| **U8** | **파일2-⑥(사진번호 숨기기)는 캡션 1행을 *비우지 않고 제거*한다** — 2행(`위치 부재명`)이 1행으로 올라온다 | 1행을 빈 문자열로 두면 캡션 블록 높이가 남아 칸이 들쭉날쭉해진다(`print.css:157-163` 이 `height: 84mm` 고정). `assignNumbers`·`ExportRun.mapping` 은 **건드리지 않는다** — 숨기는 것은 표시뿐이다(불변식 #2). 손상결함표·결함리스트의 `사진번호` 열도 그대로 둔다. 비용 낮음 |

### U9~U15 — 사용자 답변(D14~D19) 반영 2차 · 2026-08-28

D14~D19 가 답하지 않은 **구현 세부**를 plan-reviewer 가 확정한 것들이다.
전부 "임의로 정해도 되는 것"(내부 구조·필드 배치) 범주이거나, 되돌리는 비용이 낮다.
관련 신규 질문 1건: **Q53**(비차단).

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U9** | ⭐ **D16 의 "도곽크기" = `DrawingTitleBlock.tbScale`.** 가로/세로 mm 도 용지 크기 프리셋도 아니다 | 설정 다이얼로그가 이 슬라이더에 **`도곽 크기`** 라벨을 직접 붙이고 있고(`TitleBlockDialog.tsx:134`), 타입 주석이 "도곽 전체 비례 배율"이며, 렌더러에서 `const s = cfg.tbScale`(`canvas-core/src/titleBlock.ts:216`)가 여백·표제란 높이·선굵기·글꼴에 **전부 곱해진다.** 지면 크기 자체는 A4 고정(`Drawing.imgLayout` = `A4_LANDSCAPE`)이라 "치수" 필드가 애초에 없다. 비용 0(해석) |
| **U10** | **D16 이 언급하지 않은 5필드(`enabled`·`col0`·`col1`·`labelFontSz`·`valueFontSz`)를 전부 프로젝트 스코프로 올린다.** `Drawing` 에 남는 도곽 필드는 `drawingName` 하나뿐 | 앞 4개는 `tbScale` 과 같은 "도곽 생김새"라 도면마다 다르면 보고서가 들쭉날쭉해진다 — D16 이 `tbScale` 을 공유시킨 취지 그대로다. **`enabled` 을 도면별로 두면 사용자가 여전히 도면마다 도곽을 켜야 하고, 그게 파일1-③ "도곽 출력안됨"의 원인이었다.** 비용 낮음(필드 하나를 다시 내리면 된다) |
| **U11** | **기존 도면 값의 프로젝트 승격은 `층 sortOrder → 도면 sortOrder → 도면 id` 순 첫 번째 `titleBlock !== null` 도면**의 값으로 한다. 없으면 기본값. 번들 로드 직후 1회, `project.titleBlock === null` 일 때만 | `Drawing.sortOrder` 는 `factory.ts:121` 이 전부 `0` 으로 만들어 실질 기준이 **층 순서**다. 그건 출력 순서와 같아 사용자에게 설명하기 쉽다("맨 아래층 도면 설정을 용역 기본값으로 삼았습니다"). 시점은 `ensureProjectSettings`(지연 스냅샷, `CanvasRoute.tsx:122-127`)와 **같은 관용구**. **저장 레코드는 `Project` 1건만 쓴다 — 도면은 안 건드린다.** 비용 낮음 |
| **U12** | ⭐ **D18 이 미확정으로 남긴 "불러오는 필드 범위" = D9 표 그대로**(복사 14 · 새로 받음 8). 전체 복사가 **아니다** | 사용자 표현이 *"비슷한 **유형** 결함 불러오기"* 다. **전체 복사하면 폭·길이·개소·메모·위치보조가 따라와 전부 지우고 다시 입력해야 한다 — 불러오기의 목적과 반대 방향이다.** D9 표는 "무엇이 반복되고 무엇이 매번 다른가"를 사용자가 직접 판정한 표이고, D18 이 뒤집은 것은 **트리거(자동→수동)** 이지 이 판정이 아니다. 표 자체는 손대지 않고 이름만 `DEFECT_CARRY_FIELDS`/`pickCarryAttrs` 로 바꾼다. 비용 낮음 |
| **U12-b** | **`defectSeed` 를 지우되 프로젝트 기본 구조유형은 `ReduceContext.defaultAttrs` 로 이름만 바꿔 남긴다** | `CanvasRoute` 의 `seedAttrs(s, b.project)` 는 D9(직전 입력 기억)가 아니라 **"이 용역의 기본 구조유형(RC 등)"** 이다. 같이 지우면 **결함을 찍을 때마다 구조유형을 다시 고르게 된다.** grep 결과에 `seedAttrs` 가 섞여 나오므로 builder 가 오인할 위험이 크다 → §5-4-d 에 명시함. 비용 0 |
| **U13** | ⭐ **접두어가 하나라도 있으면 출력 번호모드를 `PER_FLOOR` 로 자동 전환한다.** 접두어 없는 층은 접두어 없이 `01, 02`(그 층 안에서 리셋). 사용자가 출력 화면에서 다시 바꿀 수 있다 | **Q53.** D19 를 "층마다 리셋/연속을 섞는다"로 읽으면 `1F-01,02,03` 다음이 `04` 가 되어 **번호가 자기 설명적이지 않게 된다.** 이 안은 D19 문장을 만족하면서 모순이 없고 **`assignNumbers` 를 한 줄도 안 바꾼다**(모드 결정과 표기를 바깥에서 한다). `ExportRun.params.mode` 에 실제 모드가 저장돼 재출력도 같다. 비용 낮음(`Export.tsx` 초기값 1곳) |
| **U14** | **`ROOF → 'RF'` · `ROOFTOP → 'PH'` 로 매핑한다** | 사용자 예시가 `RF-01` 인데 파서는 문자열 `'RF'` 를 **옥탑**으로 읽는다(`floorOrder.ts:58`). 국제 관례(RF = Roof Floor, PH = Penthouse)를 쓰면 사용자 예시와 파서를 동시에 만족한다. 비용 0 |
| **U14-b** | **`EXTERIOR` 패턴은 `외부`·`외곽`·`옥외`·`외벽`·`EXT`·`EXTERIOR`. `W` 자체는 넣지 않는다.** `SORT_EXTERIOR = 9500`(옥탑 9000 보다 뒤) | `W` 는 *출력 코드*이지 층 *이름*이 아니다 — 사용자는 "외부"라 입력한다. 기존 패턴과의 충돌을 `norm()`(공백제거+대문자) 기준으로 직접 검사했다: `옥외` 에는 `옥상`도 `지붕`도 없어 ROOF 로 오인되지 않는다. 9500 근거는 실무에서 외부가 보고서 맨 뒤라는 점이고, 드래그가 최종 권한이라 되돌릴 수 있다. `renumber()` 가 9500 을 잃는 것은 ROOFTOP·ROOF 도 이미 같아 **기존 동작 그대로 둔다.** 비용 낮음 |
| **U15** | ⭐ **`ExportParams` 에 `floorCodes: Record<floorId, string\|null>` 스냅샷을 추가한다** | 접두어가 출력 시점 파생값이라 **층 이름이나 `Floor.code` 를 나중에 고치면 재출력 시 접두어가 바뀐다** — *"같은 옵션으로 다시 뽑으면 같은 번호"* 를 깬다. `ExportParams` 는 `meta` KV 에 JSON 으로 들어가 **필드 추가에 마이그레이션이 없다.** `ExportRun.mapping` 은 `{no, photoNo}` 그대로라 불변식 #2 도 유지된다. 옛 run 은 `floorCodes` 가 없으므로 현재 층에서 파생(폴백). 비용 낮음 |
| **U16** | **D15 상태 범례 기본값은 3개 전부 꺼짐. 켜져 있어도 그 도면에 없는 상태는 그리지 않는다** | 기본이 켜짐이면 **기존 출력물이 조용히 바뀐다** — 파일1-⑤는 "On/off 기능"을 요청한 것이지 "항상 켜라"가 아니다. 그리고 전회차가 0건인 도면에 "미보수" 범례가 뜨면 **거짓말이 된다** — 결함유형 범례가 `rows.length === 0` 이면 안 그리는 것(`pageDecor.ts:76`)과 같은 정신. 비용 0 |

### U17~U21 — builder 배치3(G-6·G-3·G-2) 구현 중 · 2026-08-28

전부 **비차단**이고 되돌리는 비용이 낮다. 스펙이 두 가지로 읽히거나 스펙이 침묵한 구현 세부다.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U17** | ⭐ **승격 대표 도면을 도곽·범례 각각 따로 고른다** (정렬 규칙은 동일). 스펙 §5-3(c)는 "같은 대표 도면", 리더 지시문은 "범례도 동일 규칙" 이라 두 가지로 읽힌다 | 실제 데이터에서는 **두 해석이 같은 결과**를 낸다 — 지금까지 다이얼로그가 `titleBlock` 과 `legend` 를 **항상 함께** 저장했으므로 `titleBlock !== null` 인 도면은 `legend !== null` 이기도 하다. 한쪽만 설정된 예외 데이터에서만 갈리고, 그때 **따로 고르는 쪽이 잃는 값이 없다.** 비용 0(한 줄) · 테스트 `projectDecor.test.ts` 로 고정 |
| **U18** | **`onPreview(tb)` 가 아니라 `onPreview(tb, lg)`** — 범례 값도 함께 흘려보낸다 | 같은 다이얼로그 안에 `범례 크기` 슬라이더가 있고 **완전히 같은 문제**(저장해야 보인다)를 갖는다. 콜백 하나로 둘 다 덮는 것이 코드가 더 적다. 미리보기 대상이 캔버스인 `CanvasRoute` 에만 건다 — `ProjectSetup` 은 미리볼 캔버스가 없어 넘기지 않는다. 비용 0 |
| **U19** | **접두어가 붙으면 손상결함표 NO 열의 셀 값이 `number` 가 아니라 `string`(`'1F-01'`) 이 된다.** 접두어가 없으면 지금까지처럼 정수 그대로 | `DamageCellValue` 가 이미 `string \| number \| null` 이라 타입 변경이 없다. 엑셀에서 `1F-01` 은 애초에 숫자로 넣을 수 없다. **접두어가 없는 기존 출력물은 셀 타입이 안 바뀐다**(합계·정렬 유지) — `damageTable.test.ts` 가 이 두 경로를 각각 고정한다. `ExportRun.mapping` 은 여전히 정수다(불변식 #2). 비용 낮음 |
| **U20** | **`floorCodesFor(src, params)` 한 함수가 접두어의 유일한 조회 경로다** (`params.floorCodes ?? floorCodesOf(bundle.floors)`) | 화면·엑셀·인쇄 뷰·조사위치도가 각자 파생하면 층 이름을 고친 날 산출물끼리 접두어가 어긋난다 — K20("번호는 한 곳에서만 센다")과 같은 정신. `produce.ts` 에 있던 `displayNumbersOf` 를 `exportModel.ts` 로 옮겨 일원화했다. 비용 0 |
| **U21** | **도곽 설정 모달을 화면 오른쪽에 붙이고 스크림을 거의 투명하게 한다**(`Modal dock="right"`, 720px 이하에서는 기존대로 가운데) | 스펙 §5-3(d)가 "모달이 캔버스를 가리면 미리보기가 안 보인다 → 우측 붙임이나 스크림 투명화, **builder 재량**"이라고 명시했다. 두 가지를 함께 적용했다. 비용 0(CSS 클래스 1개 · prop 1개) |
| **U22** | **`OptionsPanel` 의 `층별 1번부터` 설명 문구를 고쳤다** — "결함번호와 사진번호가 함께 1로" → "결함번호만 1로, 사진번호는 이어짐" | D19 로 K6 가 폐기돼 **기존 문구가 거짓말이 됐다.** 스펙이 이 파일을 명시하지 않았지만 고치지 않으면 화면이 코드와 다른 말을 한다. 비용 0 |

### U23~U26 — builder 배치4(G-5·G-1) 구현 중 · 2026-08-28

전부 **비차단**이고 되돌리는 비용이 낮다. 스펙이 침묵한 구현 세부이거나, 스펙의 문자와 스펙의 요구가 부딪히는 지점이다.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U23** | ⭐ **`DELETE_MEMO_PATH` 의 payload 를 단수가 아니라 배열로 만들었다.** 스펙 §5-1(d)는 `{ memoId, path, index }` 로 적었지만, 같은 표가 **"한 번의 드래그 = Undo 1스텝"** 을 함께 요구한다. 확정 형태는 `{ eraseId, items: {memoId,path,index}[], memos: Memo[] }` 이고 `pushHistory` 가 같은 `eraseId` 를 하나로 합친다 | **단수 payload 로는 그 요구를 만족시킬 수 없다** — 획마다 커맨드가 쌓여 Ctrl+Z 를 스무 번 눌러야 한다. 사용자에게 보이는 동작(획 단위 삭제 · 빈 메모 제거 · 되돌리기 1스텝)은 스펙 그대로이고 바뀐 것은 **내부 자료구조뿐**이다. `memos` 는 "마지막 획이 지워져 레코드째 사라지는 메모" 자리이므로 스펙이 말한 `DELETE_MEMO` 재사용을 **같은 커맨드 안에서** 이룬다(별도 커맨드로 내면 그것만 2스텝이 된다). 되돌리는 비용 낮음 — 커맨드 1종 |
| **U24** | **지우개는 한 번에 "가장 가까운 획 하나"만 지운다** (허용치 안의 모든 획이 아니다) | 사용자가 못 박은 *"다른 점·화살표·번호가 지워지면 안 됨"* 의 정신이다. 겹쳐 쓴 글씨에서 허용치 안의 획을 전부 날리면 한 번의 스침에 단어가 통째로 사라진다. 계속 문지르면 나머지도 지워지므로 기능은 온전하다. 비용 0(`nearestMemoPath` 한 줄) |
| **U25** | **지우개 드래그가 끝날 때 토스트를 한 번 낸다** — `필기 N획을 지웠습니다` [되돌리기]. 지운 게 0이면 아무 말도 하지 않는다 | 스펙이 토스트를 지정하지 않았다. 다른 삭제 조작(메모·표기·결함)이 전부 되돌리기 토스트를 내므로 지우개만 조용하면 일관성이 깨지고, **무엇이 지워졌는지 모른 채 되돌릴 방법을 못 찾는다.** 드래그 중이 아니라 뗄 때 한 번만 내 토스트가 쌓이지 않게 했다(`DragState.erasedCount`). 비용 0 |
| **U26** | **불러오기 후보는 이 용역 전체의 결함이고 `seq` 내림차순**(최근 입력 우선)이다. 지금 선택된 결함은 뺀다 | 스펙 §5-4(b)는 *"이 용역의 결함 목록"* 이라고만 적고 정렬을 지정하지 않았다. 방금 찍은 결함을 다시 쓰는 경우가 압도적이다 — D9 가 애초에 **직전 입력** 자동 이어받기였던 이유가 그것이다. 자기 자신을 골라도 아무 일도 안 일어나므로 목록에서 뺐다. 비용 0(`sort` 한 줄) |

### U27~U28 — builder 배치4 검수 반영(수정 라운드) 중 · 2026-08-30

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U27** | ⭐ **`MOVE_MEMO` 미리보기 기준점을 상자(bbox)에서 `memo.pos`(앵커)로 바꿨다.** 검수 [심각1] 권장안 B. `memoScreens` 의 `preview.pos` 의미도 "상자 좌상단" → "`memo.pos` 의 미리보기 위치" 로 바뀌고, 상자·획을 **델타**로 민다 | 커밋은 원래부터 `from: memo.pos → to: previewNorm` **델타**로 나갔다. 기준점을 상자로 잡으면 앵커와 bbox 의 어긋남이 그대로 이동량에 더해진다 — 지우개가 이 오차를 임의 크기로 키웠다(획이 멀리 튐). 텍스트 메모는 `box.x == toScreen(pos).x` 라 **결과가 완전히 동일**하고, 필기 메모는 기존에도 있던 `MEMO_BOX_PAD`(6 이미지 px) 오차까지 함께 사라진다. 대안 A(커맨드에 이전 `pos` 를 실어 복원)는 payload 를 또 늘리므로 쓰지 않았다. 비용 0(두 줄) · 회귀 테스트 3건으로 고정 |
| **U28** | **지우개(ERASER)로 캔버스를 누르면 선택이 풀린다** | 검수 [경미2]. 지우개 모드에 들어온 사용자가 `Delete` 를 눌러 **이전에 골라 둔 결함이 지워지는** 사고를 막는다 — D14 의 *"다른 것은 절대 안 지운다"* 정신. 도구와 선택이 독립이라는 기존 동작을 지우개에서만 깬다. 되돌리는 비용 0(한 줄) |

### U29~U32 — builder 태블릿 배치1(T-3·T-4·T-5·T-6) 구현 중 · 2026-09-01

전부 **비차단**이고 되돌리는 비용이 낮다. 스코프 문서(`00_input/scope_TabletFeedback0901.md`)가
침묵했거나 builder 재량으로 넘긴 구현 세부다.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U29** | **T-5 에서 `MemoEditor` 의 `Ctrl+Enter·Esc` 힌트도 함께 숨긴다** | 스코프 문서가 *"사용자가 직접 언급하진 않았으나 같은 종류라 함께 처리 — U(가정)로 남기고 최종 보고에 포함할 것"* 이라고 명시적으로 지시했다. 터치 태블릿에는 `Ctrl` 도 `Esc` 도 없어 **읽어도 실행할 수 없는 안내**다. 비용 0(CSS 선택자 1줄) |
| **U30** | ⭐ **T-5 판정을 JS `window.matchMedia` 훅이 아니라 CSS `@media (hover:none) and (pointer:coarse)` 로 했다** | 스코프가 요구한 것은 *"표준 미디어쿼리 기반 판정, UA 스니핑 금지"* 이고 CSS 미디어쿼리는 **같은 술어**다. 게다가 `.stage__help` 는 `data-floating` 이라 안전영역 계산에 들어가는데, `display:none` 이면 `CanvasView.tsx:144`(크기 0 건너뛰기)가 이미 알아서 제외해 **하단 여백까지 함께 돌려받는다.** JS 훅이면 상태·리렌더·리스너 해제가 추가로 필요하고 그만큼 틀릴 자리가 는다. 뒤집는 비용 낮음(훅 파일 1개 + prop) |
| **U31** | **T-4 의 "명시적 선택" 에 마커 탭(`POINTER_DOWN`) 외에 더블클릭·우클릭(`CONTEXT_MENU`)·좌측 결함 리스트 클릭(`SELECT_DEFECT`)도 포함했다** | 스코프 문장은 *"기존 마커를 다시 탭"* 이지만, 금지하려는 대상은 **생성 직후의 자동 선택** 하나다. 나머지 셋은 전부 사용자가 그 결함을 직접 지목한 조작이라 툴바가 떠도 놀랍지 않고, 오히려 빼면 *"리스트에서 골랐는데 색을 못 바꾼다"* 가 된다. 판정을 이벤트 종류 화이트리스트(`EXPLICIT_SELECT_EVENTS`) 한 곳에 모아 뒤집기 비용을 0 으로 만들었다(집합에서 한 줄 빼면 된다) |
| **U32** | **T-6 옵트인 prop(`closeOnScrimClick`)을 만들되 지금은 어디에서도 켜지 않았다** — `dock="right"` 도곽·범례 다이얼로그(U21)도 포함해서 끈다 | 스코프가 *"배경 클릭으로 닫아야만 하는 특별한 이유가 있는 모달은 지금 없다"* 고 못 박았다. 도곽 다이얼로그는 스크림이 투명해 **캔버스를 조작하려다 스크림을 눌러 설정이 통째로 닫히는** 사고가 오히려 더 쉽다. 닫는 길은 Esc · 헤더 ✕ · 푸터 버튼 셋이 남는다. 비용 0(prop 하나 켜기) |

### U33~U36 — builder 태블릿 배치2(T-1·T-2) 구현 중 · 2026-09-01

전부 **비차단**. 스코프 문서(`00_input/scope_TabletFeedback0901.md` T-1·T-2)가 침묵한
구현 세부이거나, 스코프의 문장을 코드로 옮기려면 경계를 한 줄로 못박아야 하는 지점이다.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U33** | ⭐ **T-1 의 "드로잉 중" 을 `필기 세션` 으로 정의했다** — 세션이 살아 있는 조건은 *도구가 아직 `MEMO` 이고* **그리고** *그때 만든 메모가 아직 선택돼 있는 것* 둘 다. 둘 중 하나라도 깨지면 D14(선택·hover 시 상자)로 즉시 돌아간다 | 스코프 문장이 *"포인터 down~up, 여러 획 이어그리기 포함"* 과 *"도구를 바꾸거나 다른 곳을 탭하면 기존 규칙대로"* 를 함께 요구한다. down~up 만 보면 **획을 뗀 순간 상자가 되살아나** 이어 쓰는 동안 계속 깜빡인다(원래 증상 그대로). 두 조건을 `CanvasState.inkMemoId` 파생 필드 하나로 표현하고 판정을 `endInkSessionIfStale()` **한 곳**에 모았다 — 선택을 바꾸는 지점이 열 곳이 넘는데 거기에 `inkMemoId: null` 을 흩뿌리면 한 곳만 빠뜨려도 상자가 영영 안 나온다. 되돌리는 비용 낮음(함수 1개) |
| **U34** | **획을 긋는 도중(`inkSession.drawing`)에는 지금 그리는 것과 무관한 다른 메모의 상자까지 전부 숨긴다** | 스코프의 *"선택 상태와 무관하게 박스를 그리지 않는다"* 를 문자 그대로 읽었다. 메모를 하나 골라 둔 채 `MEMO` 도구로 옆에 필기를 시작하면, 좁히지 않는 한 화면에 상자가 남아 같은 불만이 재현된다. 손을 떼면 그 메모의 상자는 곧바로 돌아온다(세션 주인이 아니므로). 비용 0(조건 한 항) |
| **U35** | ⭐ **T-2 핀치가 진행 중인 동안 `POINTER_DOWN` · `POINTER_MOVE` · `POINTER_UP` 을 코어로 넘기지 않는다** (`pointercancel` 은 그대로 넘긴다) | 코어의 T3 가드는 *"진행 중인 드래그가 있을 때"* 만 두 번째 포인터를 버리는데, `GESTURE_PINCH_START` 가 이미 그 드래그를 롤백해 뒀다 — 그래서 **세 번째 손가락이 닿으면 드래그가 새로 시작돼 결함이 그려진다.** 손가락을 뗄 때의 `POINTER_UP` 도 "이동 없는 클릭" 으로 읽혀 점 결함이 생기거나 선택이 풀린다. 포인터 캡처 해제는 핀치 여부와 무관하게 **항상** 하고, `blur` 에서도 플래그를 풀어 입력이 굳는 경우를 막았다. 되돌리는 비용 0(가드 3줄) |
| **U36** | **T-2 는 `PointerEvent` 가 아니라 네이티브 `TouchEvent`(`touchstart/move/end/cancel`)로 배선했다** | 스코프가 `touchstart/touchmove/touchend` 를 지정했다. 포인터 이벤트로는 손가락마다 별개 `pointerId` 가 따로 들어와 두 접점을 앱이 직접 모아야 하는데, `TouchEvent.touches` 는 **한 이벤트 안에 현재 접점 전부**를 주므로 중점·거리 계산이 짧고 어긋날 자리가 없다. 세 손가락 이상에서 추적 쌍이 바뀌면 `Touch.identifier` 비교(`sameTouchPair`)로 알아채고 **그 프레임은 기준만 다시 잡는다** — 그대로 `factor` 로 넘기면 화면이 튄다. 마우스 동작은 한 줄도 안 바뀐다(터치가 없으면 이 리스너는 한 번도 안 돈다) |

### U37~U39 — builder 태블릿 배치1 검수 반영(보통1·보통2) 중 · 2026-09-01

전부 **비차단**. 검수 문서(`43_code-reviewer_findings_Tablet1.md`)가 제시한 수정안을 옮기면서
그 문장만으로는 결정되지 않는 한 칸을 메운 지점이다.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U37** | ⭐ **`POINTER_DOWN` 의 "직접 고름" 판정을 `선택이 이번에 옮겨왔다(changed)` 또는 `그 결함을 실제로 잡았다(grabbed = drag.defectId === 선택 id)` 둘 중 하나로 정의했다** | 검수 제안 그대로다. 드래그 종류를 열거(`MOVE_MARK`·`MOVE_LABEL`…)하지 않고 **`drag.defectId` 하나만** 보는 쪽을 택했다 — 종류를 열거하면 드래그가 새로 생길 때마다 여기도 같이 고쳐야 하고, 빠뜨리면 "잡았는데 툴바가 안 뜨는" 조용한 회귀가 된다. `defectId` 는 코어 `newDrag` 기본값이 `null` 이라 결함을 대상으로 하지 않는 드래그(PAN·CREATE_SHAPE·CREATE_SKETCH·ERASE·MOVE_MEMO)에서 자동으로 거짓이 된다. 되돌리는 비용 0(조건 두 줄) |
| **U38** | **자동포커스를 끈 모달(T-3)은 "아무 데도 포커스 안 함" 이 아니라 **모달 컨테이너**(`tabIndex={-1}`)를 포커스한다. 그리고 Tab 트랩이 "포커스가 모달 밖 또는 컨테이너 자신" 인 경우를 추가로 잡아 방향에 맞춰(Tab→첫, Shift+Tab→마지막) 안으로 끌어온다 | 컨테이너 포커스만으로는 **Shift+Tab** 이 여전히 샌다 — 컨테이너는 `[tabindex="-1"]` 이라 트랩의 `items` 에 안 들어가고, 첫/마지막 비교가 둘 다 거짓이라 브라우저 기본 동작으로 배경으로 넘어간다. 검수 제안(컨테이너 포커스)의 목적("트랩이 산다")을 실제로 달성하려면 이 한 갈래가 필요하다. 기존 요소 간 순환 로직은 손대지 않았다. 되돌리는 비용 낮음(가드 5줄) |
| **U39** | **모달 컨테이너에 `outline: none` 을 줬다**(`.modal:focus`, `.modal:focus-visible`) | 컨테이너는 조작 대상이 아니라 포커스를 가둘 자리다. 키보드로 모달을 연 경우 브라우저가 `:focus-visible` 로 **상자 전체에 테두리**를 그리는데 이는 "지금 이 요소를 조작한다" 는 잘못된 신호다. 내부 버튼·입력의 초점 표시는 그대로다. 되돌리는 비용 0(CSS 3줄) |

### U40~U44 — builder 태블릿 배치3(T-7 · G-8) 구현 중 · 2026-09-01

전부 **비차단**. 스코프(`00_input/scope_TabletFeedback0901.md` T-7)가 *"4번(되돌리기 버튼)의
정확한 문구·위치는 builder 재량"* 이라고 명시했고, 나머지는 그 문장이 침묵한 구현 세부다.
**N8(사진을 지워도 자동으로 되돌아가지 않는다)은 그대로 지켰다.**

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U40** | ⭐ **`isLocked` 를 고치지 않고, 사진 추가 전용 판정 `canAddPhotos(defect)` 를 새로 만들었다** (`CURRENT` 또는 `PREV_PENDING` 이면 true, `REPAIRED` 는 false) | `isLocked` 는 이동·리사이즈·삭제·스타일·속성편집 **여섯 경로**가 함께 쓰는 게이트다(`interaction.ts` 5곳 + `store.ts` 1곳). 여기에 손을 대면 A8("전회차는 선택만") 이 전부 무너진다. 그래서 게이트를 넓히는 대신 **문 하나를 따로 냈다.** 보수완료(`REPAIRED`)는 계속 막는다 — 스코프가 전환 대상으로 `PREV_PENDING` 만 지목했고, 사진으로 보수완료를 되살리는 것은 다른 결정이다. 되돌리는 비용 0(함수 1개) |
| **U41** | **상태 전이를 `SET_DEFECT_ATTRS` 에 얹지 않고 새 커맨드 `SET_DEFECT_STATUS { defectId, from, to }` 를 만들었다** | `DefectAttrs` 에 `status` 가 없다(경계 — 폼은 상태를 모른다). 억지로 넣으면 `attrsOf`·`changedAttrKeys`·병합키까지 전부 오염되고, 무엇보다 `setDefectAttrs` 는 `isLocked` 로 조기 반환하므로 **전회차 결함에서는 영원히 안 먹는다.** 새 커맨드는 `applyCommand`/`invertCommand`/`defectTargetOf` 세 곳만 건드리고 Undo·저장 대기열에 자동으로 올라탄다. 되돌리는 비용 낮음 |
| **U42** | ⭐ **상태 커맨드는 `isLocked` 게이트를 통과하지 않는다. 대신 허용 전이를 `PREV_PENDING → CURRENT` · `CURRENT → PREV_PENDING` 두 방향으로만 좁혔다** (`store.ts` `setDefectStatus`) | 이 커맨드는 잠금의 *근거*(status) 자체를 바꾸는 유일한 통로다. 다른 편집과 같은 게이트를 걸면 전회차 결함은 영원히 금회차가 될 수 없다. 게이트를 없앤 대신 **화이트리스트**로 막았다 — `REPAIRED` 가 얽힌 전이는 조용히 무시한다. 되돌리는 비용 0(조건 두 줄) |
| **U43** | **`[전회차로 되돌리기]` 버튼은 Inspector 하단 액션줄, `번호 위치 초기화` 와 `결함 삭제` 사이에 둔다. `status === 'CURRENT'` **그리고** `prevDefectId !== null` 일 때만 뜬다** | 위치: 되돌리기는 파괴적이지 않으므로 `btn`(기본), 삭제(`btn--danger`) 앞에 둔다. 조건: **이번 회차에 새로 그린 결함(`prevDefectId === null`)에는 주지 않는다** — 있지도 않은 "전회차" 로 보내면 보라색이 되고 `includePrevPending` 필터에서 출력에서 통째로 빠진다. 사진 유무는 조건에 넣지 않았다(스코프 4번 명시: *"사진 삭제 여부와 무관하게 항상 가능"*). 되돌리는 비용 0(조건 한 줄) |
| **U44** | **전이 판정 근거를 "`addFiles` 가 실제로 등록한 장수 > 0" 으로 잡고, 이를 위해 `usePhotos.addFiles` 의 반환형을 `Promise<void>` → `Promise<number>` 로 바꿨다. 전환 시 안내 토스트(`이번 회차 사진이 붙어 금회차 결함으로 전환했습니다`)를 [되돌리기] 가능으로 띄운다** | 스코프 2번이 *"사진 추가가 성공하면"* 이라고 못 박았다. 호출자가 성공을 알 방법이 없었다 — 파일이 전부 거절되거나(형식·용량) 저장에 실패했는데 색만 빨갛게 바뀌면 **사진 없는 금회차 결함**이 보고서로 나간다. 반환형 변경은 호출부가 한 곳(`CanvasRoute`)뿐이라 파급이 없다. 토스트를 undoable 로 둔 것은 다른 상태 변경 커맨드와 같은 대우다(문구가 전환을 명시하므로 사진 삭제와 헷갈리지 않는다). 되돌리는 비용 낮음 |

> **범위 밖으로 명시적으로 두고 온 것:** 전회차 **사진 자체를 복사해 오는 사진 승계(K13)** 는
> 손대지 않았다. `repo.ts` 의 `copyStructure` 는 한 줄도 바뀌지 않았다.

---

## V — Phase 5 팀 동기화 서버 + PWA 태블릿 (착수 전 검토) · plan-reviewer · 2026-09-02

전부 **비차단**. 차단 사항은 `QUESTIONS.md` **Q55~Q59** 로 올렸고, 아래는 그 밖에서
"묻지 않고 정한 것"이다. 스펙 전문: `_workspace/50_plan-reviewer_spec_Phase5_TeamSync.md`.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **V1** | ⭐ **"아이디·비번" 로그인을 Supabase Auth 위에서 성립시키기 위해, 서버가 `loginId` 를 `{loginId}@{teamSlug}.onspect.invalid` 로 **내부 합성**한다.** 사용자는 아이디만 입력하고 이메일은 화면에 한 번도 안 나온다 | Supabase Auth 는 email/phone 만 받는다. 사용자 요구("팀장이 아이디·비번 직접 발급, 이메일 가입 아님")를 UX 상 100% 만족시키면서 인증 구현을 위임하는 유일한 방법이다. `.invalid` 는 RFC 2606 예약 TLD 라 **실제 메일이 나갈 수 없다** — 확인메일·비번재설정 메일 경로가 구조적으로 차단되는데, 그 경로를 안 쓰는 것이 요구사항이라 오히려 맞다. 되돌리는 비용 낮음(합성 함수 1개) |
| **V2** | **서비스워커를 손으로 쓴다. `vite-plugin-pwa`(workbox)를 넣지 않는다** | 캐시 대상이 앱 셸(index.html + 해시 js/css)뿐이고 데이터는 전부 IndexedDB 라 workbox 의 전략 엔진이 할 일이 없다. `write-excel-file` 때처럼 값이 명확할 때만 의존성을 늘린다. 대신 **빌드 산출 파일명을 SW 에 주입하는 작은 vite 플러그인(약 30줄)** 이 필요하다. 되돌리는 비용 낮음(플러그인 도입으로 교체 가능) |
| **V3** | **새 서비스워커는 `skipWaiting` 하지 않는다.** 갱신이 있으면 `새 버전이 있습니다 [지금 새로고침]` 배너만 띄운다 | 현장 작업 중에 앱이 저절로 리로드되면 **입력 중이던 값이 날아간다.** 사용자가 누를 때만 갱신한다. 되돌리는 비용 0 |
| **V4** | ⭐ **`ItemSettings`(항목설정)는 1차 동기화 대상에서 제외한다. 태블릿은 읽기 전용, 편집은 사무실 PC 에서만** | `putItemSettings` 는 **문서 통째 put**("put 1회라 원자적")이라 LWW 로 병합하면 **한쪽의 항목 편집이 통째로 사라진다**(함정 #4 와 정면 충돌). 항목 배열 단위 병합은 별도 설계가 필요하고 1차 범위를 크게 키운다. 대가: 26 스펙의 N2("현장에서 없는 부재를 즉시 추가")가 1차에서는 불가능해진다. 되돌리는 비용 중간(2차에 배열 병합 추가) |
| **V5** | **`ExportRun`(출력 이력)은 동기화하지 않는다** | 기기 로컬 산출 이력이다. 동기화하면 **로컬에 없는 `blobKey` 를 가리키는 이력**이 생겨 `[같은 번호로 다시 받기]` 가 깨진다. `ExportRun.deviceId` 필드가 이미 기기 귀속을 전제하고, 출력은 사무실 PC 전담(N3)이다. 되돌리는 비용 낮음 |
| **V6** | **동기화 후 `seq`(입력순번)를 재부여하지 않는다** | 두 기기가 같은 층에서 오프라인 작업하면 `seq` 가 겹치지만, `numbering.ts::compareForOutput` 이 `seq → drawingId → id` 로 **완전 결정론적** tie-break 를 이미 하므로 **번호 재현성은 유지된다**(불변식 #2 안전). 부작용은 보고서 순서가 두 사람 작업분과 교대로 섞이는 것뿐이고, 재부여는 그 자체로 "저장된 값을 병합이 바꾸는" 더 큰 위험을 만든다. 되돌리는 비용 중간(2차에 "층별 seq 정리" 도구를 별도로) |
| **V7** | **사진은 렌더본(2048)+썸네일(320)만 서버에 올린다. 원본은 찍은 기기에만 남긴다** (Q60 의 A안) | 산출물 4종이 전부 렌더본을 쓰므로 **출력물 품질이 동일**한데 용량은 1/3~1/5 로 준다. 원본 백업이 필요하면 `[원본도 올리기]` 옵션을 나중에 붙이면 된다(필드 추가 없이 업로드 대상만 늘림). 되돌리는 비용 낮음 |
| **V8** | **벤더는 Supabase(Postgres + Auth + Storage + RLS)** (Q61 의 A안) | Neon 과 비교 검토했으나 **Storage** 에서 결판난다 — 이 앱은 수 GB 이미지가 본체인데 Neon 은 Postgres 만 준다. 비밀번호 해싱·세션을 직접 짜지 않는다는 리더 기준에도 부합. **단 계정 생성·요금제 선택은 사용자만 할 수 있다** — 착수 시점에 프로젝트 URL·키가 필요하다. 되돌리는 비용 상(이관) |

> **함께 정한 검수 규칙 (가정이 아니라 규칙):**
> `apps/web/api/` **밖**에서 `SUPABASE_SERVICE_ROLE_KEY` 문자열이 등장하면 **검수 즉시 심각**이다.
> 그 키는 RLS 를 전부 우회한다. 클라이언트는 `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` 만 안다.
> 또 Vercel Root Directory 가 `apps/web` 이므로 **서버리스 함수는 `apps/web/api/` 에 두어야 한다** —
> 저장소 루트의 `api/` 는 Vercel 이 보지 않는다.

---

## V (이어서) — Phase 5 · PWA 껍데기 P1~P6 구현 · builder · 2026-09-02

전부 **비차단**. 구현하면서 "묻지 않고 정한 것"이다. 구현 로그: `_workspace/51_builder_log_Phase5_PWAShell.md`.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **V9** | ⭐ **가정 V3("`skipWaiting` 안 함")을 "install 시점에 부르지 않는다"로 해석했다.** 배너의 `[지금 새로고침]` 을 누르면 대기 중인 워커에 `postMessage({type:'SKIP_WAITING'})` 를 보내고, `controllerchange` 를 받아 1회 리로드한다 | V3 의 목적은 *"현장 작업 중 저절로 리로드되지 않는 것"* 이고, 그 목적은 install 에서 `skipWaiting()` 을 안 부르는 것으로 100% 달성된다. **메시지까지 보내지 않으면 버튼이 먹통이 된다** — 대기 워커는 그 페이지가 완전히 닫힐 때까지 활성화되지 않으므로 `location.reload()` 만으로는 교대가 일어나지 않고, 새로고침해도 배너가 다시 뜬다. 안전망으로 3초 안에 교대 신호가 없으면 그냥 리로드한다. 되돌리는 비용 0(메시지 한 줄) |
| **V10** | **촬영 버튼(P6)은 터치 기기에서만 보인다** — `@media (hover: none) and (pointer: coarse)`. PC 에서는 `display:none` | PC 브라우저는 `capture` 속성을 무시하고 그냥 파일 선택창을 띄운다 → `[촬영]` 과 `[+ 사진 추가]` 가 **똑같이 동작하는 버튼 두 개**가 된다. D1 이 *"PC 웹이므로 촬영은 없다"* 를 이미 못 박았다. 판정은 기존 터치 규칙(`styles.css` T-5)과 **같은 표준 미디어쿼리**만 쓴다 — UA 스니핑 금지. 되돌리는 비용 0(CSS 두 줄) |
| **V11** | **촬영 입력의 `accept` 만 `image/*` 로 넓혔다** (다른 사진 입력은 `PHOTO_ACCEPT_ATTR` 유지) | 스펙 §6-0 P6 원문이 `accept="image/*"` 다. `image/jpeg,image/png,...` 로 좁히면 기기에 따라 카메라 대신 파일 선택창이 뜬다. **검사를 우회하는 것이 아니다** — 형식이 안 맞는 결과물(HEIC 등)은 `photoIngest` 가 지금과 똑같이 인라인 경고로 거절한다. 되돌리는 비용 0 |
| **V12** | **저장 여유 경고 임계값 = 500MB** (`ProjectList.LOW_STORAGE_BYTES`) | 인입이 실제로 막히는 선은 8MB(`photoIngest.STORAGE_HEADROOM`)인데 **막히고 나서 알려주면 늦다.** 현장에 나가기 전에 사진 한 묶음이 들어갈 여유가 있는지 알아야 한다. 숫자에 근거가 되는 실측이 없어 "한 회 촬영분이 확실히 들어가는 크기"로 잡았다. 되돌리는 비용 0(상수 하나) |
| **V13** | **개발 서버에서는 서비스워커를 등록하지 않고, 이미 붙어 있으면 `unregister()` 한다** | `vite preview`(프로덕션 빌드)와 `vite dev` 가 **같은 오리진·같은 포트(5173)** 다. 프리뷰에서 붙은 SW 가 남으면 개발 서버가 캐시된 옛 셸을 받아 **원인을 알 수 없이 깨진다.** `dist/sw.js` 는 빌드에서만 생기므로 dev 에서는 등록해도 404 다. 되돌리는 비용 0 |
| **V14** | **`requestPersistence()` 의 반환형을 `Promise<boolean>` → `Promise<PersistenceState>`(`UNKNOWN`·`GRANTED`·`DENIED`·`UNSUPPORTED`) 로 바꾸고, 결과를 약속째로 메모이즈했다** | 설정 화면에 결과를 표시하려면(P3) "거절됨"과 "지원 안 함"을 구분해야 한다. 또 기존 구현은 **요청이 진행 중인데 두 번째 호출이 무조건 `true` 를 받아갔다** — 화면에 거짓말이 뜬다. 호출처는 `DrawingUpload` 하나뿐(`void` 로 버린다)이라 파급이 없다. 되돌리는 비용 낮음 |
| **V15** | **`index.html` 에 `apple-touch-icon`·`apple-mobile-web-app-*` 메타를 함께 넣었다** (P1 의 "아이콘 3종 + link rel=manifest" 를 넘어선 한 줄들) | iOS/iPadOS 는 **매니페스트의 `icons` 를 홈 화면 아이콘으로 쓰지 않는다.** `apple-touch-icon` 이 없으면 아이콘이 화면 스크린샷이 된다 — §8 체크리스트의 *"아이콘·이름이 정상인가"* 가 그대로 실패한다. 되돌리는 비용 0 |
| **V16** | **`viewport-fit=cover` 는 넣지 않았다** | `styles.css` 에 `env(safe-area-inset-*)` 처리가 한 곳도 없다. 지금 켜면 노치 기기에서 UI 가 노치 아래로 들어간다. 태블릿 셸(T2-1)에서 안전영역 CSS 와 **함께** 켠다. 되돌리는 비용 0(메타 한 줄) |

> **범위 밖으로 명시적으로 두고 온 것:** `canvas-core` 는 **한 줄도 건드리지 않았다.**
> `DB_VERSION` 은 1 그대로이고 오브젝트 스토어·인덱스 변경이 0건이다.
> `beforeinstallprompt` 를 가로채는 **자체 설치 버튼은 만들지 않았다** — 스펙 P1~P6 에 없다.
> 설치는 브라우저 기본 경로(`공유 → 홈 화면에 추가`)를 쓴다.

---

## W — Phase 5 · T1-2 `Defect` 병합 재료 3필드 · builder · 2026-09-02

전부 **비차단**. 스펙(§2)·D23 이 답을 이미 준 사안이라 `QUESTIONS.md` 에 새로 올린 항목은 없다.
아래는 구현하면서 "묻지 않고 정한 것"이다. 구현 로그: `_workspace/52_builder_log_Phase5_DefectMergeFields.md`.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **W1** | ⭐ **`DefectBase` 는 `DefectAttrs` 에 섞지 않고 `Defect` 에 별도 교차했다** | `DefectAttrs` 에 넣으면 `DEFECT_ATTR_KEYS`·`changedAttrKeys`·`pickCarryAttrs`·`attrsOf` 가 전부 오염된다 — `[유사결함 불러오기]` 가 남의 `updatedAt`·`deviceId` 를 복사하고, Undo 병합 키가 스탬프 때문에 매번 달라져 조작이 안 묶인다. `Memo` 가 `RecordBase` 를 직접 나열한 것과 같은 모양이다. 되돌리는 비용 큼(설계 전제) |
| **W2** | ⭐ **`newDefectBase(now, …)` 에서 `now` 가 없으면 `0` 이 아니라 `null` 을 넣는다** | 기존 `Memo` 관행은 `(ctx.now ?? (() => 0))()` 인데, 결함에 그대로 쓰면 시계 없는 호출이 **병합에서 항상 지는** 레코드를 만든다(스펙 §2-3 이 경고한 것의 거울상). 시각을 모를 때의 정답은 언제나 "서버가 첫 동기화에 부여"다. 실사용 경로(`store.ts:395`)는 항상 `Date.now()` 를 넘기므로 화면 동작에 영향 0. 되돌리는 비용 0 |
| **W3** | **읽기 정규화가 `deviceId` 의 빈 문자열도 현재 기기로 메운다** | `ctx.deviceId` 가 없을 때 코어가 `''` 를 넣는데, `??` 만 쓰면 `''` 가 살아남아 "기기 없음" 레코드가 생긴다. `deviceId` 는 과거 사실이 아니라 **현재 관측값**이라 덮어써도 위험이 없다(D23 이 명시). 되돌리는 비용 0 |
| **W4** | **`duplicateStructure`(F7 전차 승계)도 새 스탬프를 받는다** — 스펙이 말한 "생성 3곳" 밖이지만 결함 레코드를 새로 만드는 자리다 | `{...src}` 스프레드로 **원본의 `updatedAt`·`deviceId` 가 딸려오므로** 방치할 수 없었다. 승계 결함은 지금 이 기기에서 새로 만들어지는 레코드이지 사본이 아니다 — id·projectId·status·prevDefectId 를 이미 전부 새로 부여하고 있다. `createdBy` 는 `null`(로그인 전). 되돌리는 비용 0 |
| **W5** | **`repo.listDefects()` 에는 정규화를 붙이지 않았다** (기존부터 `normalizeDefectAttrs` 도 안 탄다) | 유일한 호출자 `Settings.tsx:112` 는 "이 항목이 몇 건에 쓰였나" 를 세기만 하고 되쓰지 않는다. 이번 배치 범위 밖이라 손대지 않았다. **동기화(T1-7·T1-8)가 이 함수를 재사용하려 하면 그때 반드시 정규화를 붙여야 한다.** 되돌리는 비용 0(한 줄) |
| **W6** | **`createdAt` 은 추가하지 않았다** | D23 이 세 필드만 신설하기로 정했고, 입력순서는 `seq` 가 이미 갖고 있다. 스펙 §2-1 표는 `createdAt` 도 ❌ 로 표시했지만 결정문이 우선한다. 나중에 필요해지면 같은 수법(optional + 읽기 정규화)으로 붙일 수 있고 `DB_VERSION` 도 그대로다 |

> **범위 밖으로 명시적으로 두고 온 것:** 서버·Supabase·동기화 API·로그인·삭제 전파(T1-3)·태블릿 UI(T2-*).
> `DB_VERSION` 은 1 그대로, 오브젝트 스토어·인덱스·마이그레이션 변경 0건 — `db.ts` 를 열지도 않았다.
> `updatedAt: null` 을 실제로 해소하는 것은 **T1-7/T1-8 에서 서버가 첫 수신 시각을 부여할 때** 다.

---

## T2-2 조준 크로스헤어 (D22 · Q55 안 A) — 2026-09-02

구현 로그: `_workspace/53_builder_log_Phase5_AimCrosshair.md`. **`packages/` 변경 0.**

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **X1** | **조준 중에는 `DOUBLE_CLICK`·`CONTEXT_MENU` 도 코어로 보내지 않는다** | 지시 5번("조준 중에는 도면 탭이 팬/줌 전용")의 문자 그대로다. 도면을 밀다 잠시 멈추면 Android 가 contextmenu 를 내 삭제 메뉴가 뜨고, 두 번 톡 치면 메모 편집기가 열린다. 되돌리는 비용 0(가드 두 줄) |
| **X2** | **"팬 전용" 을 `keys.space:true` 주입으로 만들었다** | 코어에 조준 분기를 새로 만들지 않으려면 이 길뿐이다 — 코어는 `button===0 && keys.space` 를 팬으로 읽는다. `keys.space` 는 코어에서 이 판정과 커서(grab) 딱 두 곳에서만 쓰인다(grep 확인). 되돌리는 비용 0 |
| **X3** | **조준 상태는 저장하지 않는다**(화면을 나가면 꺼진다) | D22·스펙에 지속 요구가 없다. 필요하면 T2-5(`lastView:`)와 같은 자리에 `meta` 로 붙이면 된다. 되돌리는 비용 낮음 |
| **X4** | **`[여기]` 는 우하단 고정** — 좌/우손 선택 없음 | 목업 M-05a 가 우하단 하나만 그린다. 왼손잡이 요구가 나오면 CSS 한 줄 + 토글 하나. 되돌리는 비용 낮음 |
| **X5** | **십자선은 캔버스 호스트의 기하학적 중앙**(안전영역 중앙이 아니다) | 합성 탭 좌표(`aimCenterOf`)와 보이는 십자선이 **같은 값**이어야 어긋나지 않는다. 안전영역 중앙으로 옮기면 두 계산이 갈라져 어긋날 여지가 생긴다. 되돌리는 비용 중간 |
| **X6** | **`aimSynth.ts` 에 단위 테스트를 붙이지 못했다** | `apps/web` 에 테스트 러너가 없고, 순수 함수를 코어 테스트로 옮기면 "코어 변경 0" 이 깨진다. 앱에 러너를 붙이는 것은 범위 밖 결정이라 임의로 하지 않았다 |

---

## U45~U52 — UI·동작 개선 16건 (착수 전 검토) · plan-reviewer · 2026-09-03

전부 **비차단**. 차단 사항은 `QUESTIONS.md` **Q68 · Q69 · Q70 · Q72** 로 올렸고,
아래는 그 밖에서 "묻지 않고 정한 것" 이다.
스펙 전문: `_workspace/70_plan-reviewer_spec_UIBehavior0903.md`.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U45** | ⭐ **C-2 는 사용자에게 재현 절차를 묻지 않는다.** 원인이 코드에 실재한다 — `apps/web/src/store.ts:407` · `routes/CanvasRoute.tsx:414` · `:642` 세 곳이 `globalStyle: DEFAULT_GLOBAL_STYLE` 로 하드코딩돼 있어, 렌더(`CanvasRoute.tsx:474~478`)만 `labelScale` 이 반영된 `balloonRadius` 를 쓴다 | 이 하나로 사용자 원문의 두 문구가 **동시에** 설명된다 — *"영역이 보이는 것과 다름"*(히트 반경이 34 고정) + *"결함 이동 스냅"*(자동배치 라벨의 드래그 시작점·정렬 후보 좌표가 어긋남). 되돌리는 비용 0(가정이 틀렸다면 사용자에게 다시 물으면 된다) |
| **U46** | **C-2 수정은 `canvas-core` 를 건드리지 않는다**(테스트만 추가). `DrawingRef` 에 `labelScale` 을 넣는 대신 **`AppState.labelScale`** 을 신설해 어댑터에서 주입한다 | 코어 타입에 앱 전용 표시설정을 넣으면 RN 재사용 경계가 흐려진다. 이미 `hitProfile` 을 같은 방식(`ReduceContext` optional 주입)으로 처리한 선례가 있다. 되돌리는 비용 낮음 |
| **U47** | **`labelScale === 1 \|\| null` 이면 `DEFAULT_GLOBAL_STYLE` 을 `같은 객체 참조`로 돌려준다** | "기본값 도면에서는 동작이 한 글자도 안 바뀐다" 를 코드로 보장하려는 것이다. 새 객체를 만들면 `useMemo` 의존성이 매번 바뀌어 캔버스가 통째로 다시 그려진다. 기존 `locationMap.globalStyleFor:294` 도 이미 같은 방식이다. 되돌리는 비용 0 |
| **U48** | **C-1 은 `shapes.ts` 의 `Math.min(head, len*0.5)` 를 지우지 않고, 호출부에서 "전체 꺾은선 길이 × 0.5" 상한을 먼저 적용한다**(Q67 B안으로 선행) | 클램프를 지우면 급하게 꺾인 짧은 화살표에서 촉이 몸통을 삼킨다 — 원래 클램프의 목적이 그것이다. 되돌리는 비용 0(Q67 답이 A면 클램프 한 줄 제거) |
| **U49** | **T-8 사이드바 폭은 `localStorage` 에 저장한다.** `meta` KV(`lastView:` 같은 프로젝트 스코프)가 아니다 | 기기별 UI 선호값이지 프로젝트 데이터가 아니다. `uiMode`(U-4)가 이미 `localStorage` 를 쓴다. 서버 동기화 대상에서도 자동으로 빠진다. 되돌리는 비용 낮음 |
| **U50** | **T-8 롱프레스 파라미터: 진입 1000ms · 이동 허용 8px · 히트띠 12px · 폭 범위 260~min(화면×0.6, 560)px** | 원문이 "1초 이상" 만 지정했다. 8px 는 이 코드베이스의 `CLICK_SLOP_PX` 정신과 같은 계열이고, 12px 띠는 상시 드래그가 아니므로 넓힐 필요가 없다. 되돌리는 비용 0(상수) |
| **U51** | **T-8 의 "사이드바 내용 UI 최적화" 는 CSS 로만 한다** — 컨테이너 쿼리/`clamp()` 로 열 수·버튼 높이·사진 타일 크기를 `--inspector-w` 에 반응시킨다. JS 로 폭을 읽어 리렌더하지 않는다 | 드래그 중 매 프레임 React 리렌더가 돌면 태블릿에서 끊긴다. `T2-7`(터치 프로파일)도 **CSS만 수정, 컴포넌트 구조 0변경** 으로 처리한 선례가 있다. 되돌리는 비용 낮음 |
| **U52** | **T-9 는 `<Minimap>` 렌더만 제거하고 `shell/Minimap.tsx` 파일은 남긴다** | "사용 안 함" 이지 "영원히 없앤다" 가 아니다. 파일을 지우면 되살리는 비용이 커진다(T2-4에서 세로형 도면 좌표 버그까지 잡아 둔 코드다). 되돌리는 비용 0(한 줄) |
| **U53** | **C-1 고스트(드래그 중 미리보기)는 `interaction.ts` 를 고치지 않는다.** 스펙 §C-1-3 이 지시한 `interaction.ts:326` 수정을 하지 않았다 | 고스트도 `renderModel.ghostOps` → **같은 `arrowOps`** 를 타고(`arrowHead: g.head`, `zoom: 1`), 거기서 이미 전체 길이 상한이 걸린다. `interaction.ts` 에서 한 번 더 깎으면 `arrowOps` 의 `Math.max(6, …)` 바닥과 이중으로 겹쳐 오히려 커밋본과 어긋난다. 즉 **고치지 않는 쪽이 "그릴 때와 손 뗀 뒤가 같다"** 를 보장한다. 되돌리는 비용 0(한 줄 추가) |
| **U54** | **촉이 첫 구간을 통째로 덮으면(`|next−tip| ≤ head×0.9`) 첫 구간 몸통 선을 그리지 않는다** (`renderModel.arrowOps`) | 전체 길이 기준으로 촉을 고정하면 급하게 꺾인 화살표에서 몸통 끝점이 `next` 를 지나쳐 **방향이 뒤집힌 짧은 선**이 그려진다. 그 선은 촉에 완전히 가려 보이지도 않는다. 나머지 구간(`points[1]` 이후)은 그대로 그리므로 지시선은 끊기지 않는다. 되돌리는 비용 0(조건문 한 줄) |

> **범위 밖으로 명시적으로 두고 온 것 3가지 (지적만 하고 안 고친다):**
> ① `defectGeom.ts:366` 의 `balloonR` 무클램프 vs `renderModel.ts:398` 의 `Math.max(4, …)` 기준 불일치
> ② `interaction.ts:1540` — 소프트 리밋 경계에서 가이드선은 그려지는데 라벨이 그 선에 안 붙는 경우(§2-8-c)
> ③ `labelScale` 이 `markRadius`·`markStroke`·`arrowHead`·`leaderWidth` 에는 안 걸린다(번호만 커진다)

---

## U55~U60 — 밝은/어두운 테마 + 필기메모 색상 (2026-09-03, 리더 직접 구현)

요청 원문: *"tablet에서 밝은테마/어두운테마 선택할 수 있게 해줘. 필기메모 색상 선택할 수 있게
해줘. 대표(빨강, 파랑, 초록, 검정)"*. 전부 **비차단** — 정하고 진행했다.

| # | 가정 | 근거 · 뒤집는 비용 |
|---|---|---|
| **U55** | **테마 토글은 프로젝트 목록 화면 우하단에만 둔다**(기존 `[로컬 데이터 초기화]` 옆) — 별도 설정 화면을 새로 만들지 않았다 | 이 앱에 "기기 설정" 전용 화면이 아직 없다. `[로컬 데이터 초기화]` 도 같은 자리 하나뿐이라 그 선례를 따랐다. 되돌리는 비용 낮음(화면 어디서든 누르게 하려면 배치만 바꾸면 된다) |
| **U56** | **시스템 설정(`prefers-color-scheme`)을 따르지 않는다** — 사용자가 직접 고르기 전엔 항상 밝은 테마 | PC 는 지금까지 밝은 테마 하나뿐이었다. 태블릿 OS 다크모드를 자동으로 따라가면 아무도 안 건드렸는데 화면이 갑자기 바뀌는 것처럼 보인다. 되돌리는 비용 낮음(미디어쿼리 한 줄 추가) |
| **U57** | **다크 팔레트는 1차 반영** — 토큰(`--bg`·`--surface`·`--text`·`--border`·`--accent`·`--ok/warn/error` 등) 기반 요소는 전부 반영했지만, `styles.css` 안에 하드코딩된 파스텔 hex(호버·배지 배경 등 33곳, `#dbe8fc`·`#fdecea`·`#fdf3e0` 등)는 손대지 않았다 — 다크에서도 밝은 톤 그대로 남는다 | 33곳을 전부 다듬으려면 각각의 의미를 확인해야 해서 "간단한 수정" 범위를 넘는다. 화면 대부분(캔버스 무대·사이드바·인스펙터·목록·다이얼로그)은 토큰만으로 이미 어두워진다. **직접 확인 후 거슬리는 곳을 짚어주시면 그 자리만 좁게 고친다** |
| **U58** | **결함 상태색(`--defect-*`)은 다크에서도 절대 안 바꾼다** | `styles.css` 자체 주석이 "`canvas-core` 의 `STATUS_COLOR` 와 같은 값이어야 한다"고 못박아 뒀다. 목록 배지는 자체 배경 + 고정 흰 글자(`color:#fff`)라 테마와 무관하게 항상 또렷해 바꿀 이유도 없다. 도면 이미지(스캔·사진) 자체도 안 건드린다 |
| **U59** | **필기메모 색상은 이미 있던 `Memo.style.color` 필드를 재사용한다** — 새 필드를 만들지 않았다(`DB_VERSION` 유지) | `MemoStyle.color` 는 옛 텍스트 메모 전용으로만 쓰이고 있었고 필기 메모엔 한 번도 안 쓰였다. `resolveMemoStyle` 에 종류별 기본색(필기=`MEMO_INK` 앰버 · 텍스트=`MEMO_TEXT`)만 넘기게 고쳐 재사용했다. 되돌리는 비용 0(필드 추가가 아니라 읽기 로직만 바뀜) |
| **U60** | **기본 앰버 그대로 그리면 여전히 `style: null`** — 프리셋 4색 중 하나를 고를 때만 `{color}` 를 저장한다. 색은 "다음 획"에만 적용되고 **이미 그린 메모는 재색칠할 수 없다**(다시 그려야 한다) | 기존 저장 형태(`style:null` = "기본 상속")를 그대로 지키면 안 건드린 메모는 한 글자도 안 바뀐다. 이미 그린 메모의 색을 나중에 바꾸는 UI는 요청에 없었고, 붙이려면 컨텍스트 툴바급 UI가 하나 더 필요해 범위가 커진다. 되돌리는 비용 낮음(다음 요청으로) |
